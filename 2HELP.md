
# DB-GPT 使用说明书

## 文档说明

本说明书面向准备安装、配置、使用和排障 DB-GPT 0.8.2 的用户、数据分析师、管理员与二次开发者。内容包括整体功能概述、每一个功能的详细使用说明、完整配置说明，以及 100 个常见问题。操作示例以官方 README 与仓库 configs、scripts、skills、web 实际能力为准。默认服务地址为 http://localhost:5670。推荐操作系统为 macOS 或 Linux，Python 3.10 及以上，包管理器优先 uv。Windows 可通过 WSL2 或 Docker 使用。

DB-GPT 是开源 Agentic AI 数据分析智能助手。你可以用自然语言连接数据、生成 SQL、运行 Python、加载技能、产出图表和报告，并在沙箱中控制执行风险。它也提供知识库问答、可视化工作流 AWEL、多模型接入、会话管理、反馈与评测。对最终用户，它是一个数据分析工作区；对平台团队，它是可扩展的 Agent 与 RAG 平台。

## 第一章 整体功能概述

### 1.1 工作区与对话

在浏览器中创建会话，选择普通对话、数据库对话、知识库对话、Excel 分析或仪表盘等模式，以流式方式查看模型思考、SQL、代码、图表与最终结论。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.2 数据源连接

登记 MySQL、PostgreSQL、SQLite、DuckDB、开源高斯等库，拉取表结构与样例，供 Chat DB 与 Agent 工具使用。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.3 文件与表格分析

上传 CSV、Excel，系统学习列含义后可进行画像、清洗建议、指标计算与可视化。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.4 知识库 RAG

创建知识空间，选择向量后端与索引方法，上传 PDF、Markdown、HTML、Word、代码等，问答时自动引用片段。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.5 自主 SQL

用自然语言提问，由场景或 Agent 生成 SQL，可自动执行并解释结果，也可只生成不执行。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.6 代码解释器

在沙箱中运行 Python（常见为 pandas、绘图），把数值分析变成图与文件产物。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.7 Skills 技能

把可复用的分析方法封装为技能，支持内置技能、本地技能与 GitHub 导入。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.8 多智能体规划

Planner 拆解任务，必要时并行派遣子智能体，汇总为报告。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.9 AWEL 流程

用可视化 DAG 编排检索、模型、转换与 HTTP 触发，把重复流水线产品化。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.10 多模型

通过 TOML 接入 OpenAI、通义、DeepSeek、Kimi、MiniMax、Ollama、vLLM、llama.cpp 等。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.11 沙箱安全

限制代码与工具的文件系统与网络，保护主机与生产数据。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.12 会话与反馈

历史对话分页、点赞点踩、用于后续提示词与评测改进。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.13 定时任务

把分析流程或同步任务按计划执行并留下运行记录。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.14 评测

对检索与问答进行离线或在线评估。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.15 可观测性

查看追踪跨度、耗时、token 与 GPU 指标。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.16 客户端 SDK

用 Python dbgpt-client 以 OpenAI 兼容风格调用聊天、知识库、数据源与流程。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.17 前端可视化

思维链、代码、仪表盘、异常检测、波动分析等 vis 标签渲染。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.18 国际化

界面与部分提示词支持中英日等，可通过 DBGPT_LANG 切换。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.19 安装器

scripts/install/install.sh 可按 profile 一键克隆、安装依赖并生成配置。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

### 1.20 Docker

使用 docker-compose 快速拉起依赖与服务。

该功能在产品工作流中的位置是：先解决数据可达，再解决规划执行，再解决沉淀复用。使用时请先确认对应模型与向量服务已在配置中启用，否则界面可能能打开但问答会失败。权限上建议开发环境与生产环境使用不同的数据源账号，生产账号默认只读。性能上首次嵌入或首次加载本地模型会较慢，属于正常现象，应通过日志观察进度而不是反复刷新。与其他功能的组合方式通常是：知识库加数据库做混合问答，技能加沙箱做行业报告，流程加定时任务做日报。若功能入口在界面中不可见，多半是前端构建未包含最新页面或后端路由未挂载，可对比 dbgpt_server.mount_routers 与 web/pages。

## 第二章 安装与启动

### 2.1 一键安装

在 macOS 或 Linux 执行：

```bash
curl -fsSL https://raw.githubusercontent.com/eosphoros-ai/DB-GPT/main/scripts/install/install.sh | bash
```

指定 OpenAI profile：

```bash
curl -fsSL https://raw.githubusercontent.com/eosphoros-ai/DB-GPT/main/scripts/install/install.sh | OPENAI_API_KEY=sk-xxx bash -s -- --profile openai
```

Kimi 使用 MOONSHOT_API_KEY 与 `--profile kimi`，MiniMax 使用 MINIMAX_API_KEY 与 `--profile minimax`。若本地已有仓库，加 `--repo-dir "$(pwd)" --yes` 避免重复克隆到 ~/.dbgpt/DB-GPT。安装完成后：

```bash
cd ~/.dbgpt/DB-GPT && uv run dbgpt start webserver --profile <profile>
```

浏览器打开 http://localhost:5670。建议先下载脚本用 less 审阅再执行，避免未知网络脚本直接改机器。

### 2.2 PyPI 安装

```bash
uv pip install dbgpt-app
# 或
pip install dbgpt-app
```

默认包含 CLI、FastAPI、Agent、OpenAI 兼容 LLM、DashScope、RAG 解析与 Chroma。需要更多数据源或向量库时按包 extra 安装，例如存储 milvus、数据源 mysql。Python 必须 3.10+。

### 2.3 源码启动

克隆仓库后使用 uv sync 安装工作区成员包，准备一份 TOML profile，执行同样的 dbgpt start webserver。前端如需热更新，进入 web 执行 npm install 与 npm run dev，注意 NODE_OPTIONS 已提高内存上限。Makefile 提供 fmt、testenv 等目标，贡献代码前应格式化。

### 2.4 Docker

阅读 docker-compose.yml 与 docker 目录，按注释配置模型密钥与数据卷，避免把含密钥的镜像推到公共仓库。容器内路径与宿主机映射要与 persist_path、上传目录一致，否则知识库重启后向量消失。

## 第三章 每一个功能的详细使用说明

### 3.1 普通对话

选择通用模型，不绑定数据源，适合问概念、改写文本、解释报错。注意它不会自动查你的库，不要在此模式期望得到生产指标。可以把它当作提示词调试器，确认模型密钥是否可用。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.2 数据库对话（自动执行）

先在数据源管理添加连接并测试。进入 Chat DB 自动执行模式，用中文描述指标，例如「最近七天各渠道 GMV」。等待 SQL 展示并执行。核对表名、过滤条件、时间字段时区。若结果为空，先问「有哪些表、日期列叫什么」做专业问答，再回到自动执行。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.3 数据库专业问答

只解释结构与写法，不执行。适合权限收紧的生产环境，或给分析师生成 SQL 草稿后自行到数仓运行。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.4 Excel/CSV 分析

上传文件，等待 learning 阶段识别列类型。然后问分布、异常、相关性、分组汇总。系统可能用 DuckDB 或 pandas。大文件注意内存。不要上传含身份证号等敏感列到共享环境。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.5 知识库创建

新建空间，填写名称、描述、向量类型、索引方法、领域类型。名称创建后用于检索过滤，勿频繁改名。选择与模型匹配的 embedding。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.6 文档导入

支持 PDF、Docx、Markdown、HTML、TXT、Excel、CSV 等。扫描件 PDF 无文本层会失败，需 OCR。代码仓库可用代码图相关能力。导入后等切片与嵌入完成再提问。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.7 知识问答

提问后查看引用片段是否真相关。若答非所问，调大 top_k、开启重排或改写，检查切块是否切断标题。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.8 仪表盘生成

对已连接库提出「做一个销售总览看板」，系统会规划多个查询与图表。生成后人工核对口径，不能直接当法定报表。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.9 技能市场与导入

在技能界面查看内置 csv-data-analysis、financial-report-analyzer 等。可从 GitHub 导入。导入后阅读技能说明，补齐它需要的参数与数据文件。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.10 技能执行

在 Agent 对话中让模型 select_skill，或在界面绑定技能。执行在沙箱中，观察日志与产物。参数错误时根据报错改输入而不是反复重试同一错误。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.11 代码解释器

当问题需要计算或绘图时，允许模型写 Python。检查它是否误读列名。超时则拆小问题。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.12 SQL 工具

Agent 模式下 sql_query 工具会走连接器。确保连接是只读账号。复杂查询先 explain。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.13 并行子任务

对互不依赖的多个分析请求，可让系统分派子智能体。注意并发不要压垮源库，可调 max_parallel_subagents。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.14 上下文占用

长对话会显示窗口占用。接近警告阈值时应开新会话或让系统压缩。不要把整本财报反复粘贴，应入库后检索。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.15 AWEL 流程编排

在流程编辑器拖拽算子，配置 HTTP 触发路径，保存后用示例请求调试。升级版本后检查 flow 兼容迁移是否保留视图元数据。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.16 提示词管理

把场景提示词放到 prompt 服务中版本化，避免直接改代码发版。变更后用同一问题回归。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.17 模型管理

在配置中列出 llms 与 embeddings。界面切换模型后确认 embedding 仍可用，否则知识库会静默变差。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.18 会话历史

列表分页展示摘要与时间。删除会话前确认不需要审计。旧数据可能存在消息内嵌格式。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.19 文件与会话文件

上传接口只允许操作上传目录。不要尝试用相对路径跳出。会话结束后按策略清理临时文件。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.20 反馈

对错误回答点踩并备注期望，便于后续评测集构建。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.21 评测任务

准备问答对，跑检索命中率与答案质量。不要只看 demo 效果上线。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.22 定时任务

把日报流程设为每天早上运行，检查时区。失败通知要接到值班渠道。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.23 连接器确认

首次访问生产系统应出现确认步骤，阅读将要执行的操作再批准。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.24 DBGPTS 扩展

从 hub 安装社区应用与算子，安装后重启以使组件注册生效。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.25 Libro/Notebook

适合交互式探索，再把稳定段落沉淀为技能或流程。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.26 追踪排查

回答变慢时看 span：是模型首 token 慢、检索慢还是 SQL 慢。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.27 多语言界面

设置 language 或 DBGPT_LANG。提示词语言应与数据语言一致，中文问题配中文文档效果更好。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.28 OpenAPI

用 /api 文档调试。注意 CORS，生产不要用星号源。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.29 API Key

system.api_keys 非空则请求需带钥。不要把钥写进前端仓库。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.30 客户端 SDK

实例化 Client 后调用 chat、chat_stream、知识库与数据源方法，适合把 DB-GPT 嵌进内部平台。

操作前准备：确认服务已启动、模型可调用、相关数据源或文件已经配置。操作步骤建议采用「小问题验证连通 → 真实问题分析 → 保存结论」三步，避免一开始就丢很长的财报。操作中观察：流式思维链是否卡住、工具参数是否合理、SQL 是否只读、引用是否匹配问题。操作后处理：把有价值的 SQL 与图表存入报告，把可复用步骤存成技能，把失败案例记入反馈。权限建议：开发用样例库，生产用只读账号加 IP 限制。失败回退：同一问题换 professional_qa 或普通对话定位是模型问题还是数据问题；仍失败则查看后端日志与 tracer，而不是在界面连点发送。与配置的关系：该功能依赖的 TOML 段见下一章，缺段时功能入口可能仍在但调用 500。与测试的关系：对应包内 tests 可在改配置后先跑单测再给业务试用。

### 3.31 按后端模块对照的使用入口

#### `BaseModelController` 使用说明

该功能入口名为 `BaseModelController`，定义于 `packages/dbgpt-core/src/dbgpt/model/cluster/controller/controller.py` 第 31 行。其所在子系统的定位是：模型集群，含 Worker、Controller、API Server、注册中心。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `LocalModelController` 使用说明

该功能入口名为 `LocalModelController`，定义于 `packages/dbgpt-core/src/dbgpt/model/cluster/controller/controller.py` 第 63 行。其所在子系统的定位是：模型集群，含 Worker、Controller、API Server、注册中心。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `_RemoteModelController` 使用说明

该功能入口名为 `_RemoteModelController`，定义于 `packages/dbgpt-core/src/dbgpt/model/cluster/controller/controller.py` 第 91 行。其所在子系统的定位是：模型集群，含 Worker、Controller、API Server、注册中心。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ModelRegistryClient` 使用说明

该功能入口名为 `ModelRegistryClient`，定义于 `packages/dbgpt-core/src/dbgpt/model/cluster/controller/controller.py` 第 130 行。其所在子系统的定位是：模型集群，含 Worker、Controller、API Server、注册中心。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ModelControllerAdapter` 使用说明

该功能入口名为 `ModelControllerAdapter`，定义于 `packages/dbgpt-core/src/dbgpt/model/cluster/controller/controller.py` 第 143 行。其所在子系统的定位是：模型集群，含 Worker、Controller、API Server、注册中心。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/libro/serve.py` 第 22 行。其所在子系统的定位是：Notebook/Libro 服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/observability/serve.py` 第 29 行。其所在子系统的定位是：服务侧可观测性。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Serve component for DB-GPT Agent observability.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConnectorServe` 使用说明

该功能入口名为 `ConnectorServe`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/connector/serve.py` 第 24 行。其所在子系统的定位是：连接器服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for the Connector service.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `BaseServe` 使用说明

该功能入口名为 `BaseServe`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/core/serve.py` 第 13 行。其所在子系统的定位是：dbgpt-serve 服务层。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Base serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/evaluate/serve.py` 第 22 行。其所在子系统的定位是：评测服务。结合命名与模块归属，可以将其设计职责概括为：负责提示词模板的存储、渲染与场景绑定。作者注释或文档字符串指出：Serve component  Examples:      Register the serve component to the system app      .. code-block:: python          from fastapi import FastAPI         from dbgpt import SystemApp         from dbgpt.core import PromptTemplate         from dbgpt_serve.prompt.serve import Serve, SERVE_APP_NAME          app = FastAPI()         system_app = SystemApp(app)         system_app.register(Serve, api_prefix="/api/v1/prompt")         system_app.on_init()         # Run before start hook         system_app.be用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ScheduledTaskServe` 使用说明

该功能入口名为 `ScheduledTaskServe`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/scheduled_task/serve.py` 第 56 行。其所在子系统的定位是：定时任务服务。结合命名与模块归属，可以将其设计职责概括为：负责把业务服务暴露为 HTTP REST 接口。作者注释或文档字符串指出：Serve component for scheduled tasks.  Lifecycle:     init_app   - include router, build service/runner/scheduler, init endpoints     on_init    - import Entity classes to register SQLAlchemy metadata     before_start       - create/get DB manager (sync)     async_after_start  - start AsyncIOScheduler + recover jobs from DB     async_before_stop  - shutdown scheduler用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/rag/serve.py` 第 25 行。其所在子系统的定位是：知识库与 RAG 服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SessionFileServe` 使用说明

该功能入口名为 `SessionFileServe`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/session_file/serve.py` 第 49 行。其所在子系统的定位是：会话文件服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component mounting the owner-aware session file API.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/datasource/serve.py` 第 21 行。其所在子系统的定位是：数据源服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/file/serve.py` 第 23 行。其所在子系统的定位是：文件服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/flow/serve.py` 第 25 行。其所在子系统的定位是：AWEL 流程服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/model/serve.py` 第 25 行。其所在子系统的定位是：模型管理服务。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/feedback/serve.py` 第 22 行。其所在子系统的定位是：反馈服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/prompt/serve.py` 第 24 行。其所在子系统的定位是：提示词服务。结合命名与模块归属，可以将其设计职责概括为：负责提示词模板的存储、渲染与场景绑定。作者注释或文档字符串指出：Serve component  Examples:      Register the serve component to the system app      .. code-block:: python          from fastapi import FastAPI         from dbgpt import SystemApp         from dbgpt.core import PromptTemplate         from dbgpt_serve.prompt.serve import Serve, SERVE_APP_NAME          app = FastAPI()         system_app = SystemApp(app)         system_app.register(Serve, api_prefix="/api/v1/prompt")         system_app.on_init()         # Run before start hook         system_app.be用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/conversation/serve.py` 第 23 行。其所在子系统的定位是：会话服务。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT  Message DB-GPT conversation history and provide API for other components to access.  TODO: Move some Http API in app to this component.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConfirmRequest` 使用说明

该功能入口名为 `ConfirmRequest`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/connector/api/endpoints.py` 第 161 行。其所在子系统的定位是：连接器服务。结合命名与模块归属，可以将其设计职责概括为：负责把业务服务暴露为 HTTP REST 接口。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/serve.py` 第 22 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `MultiAgents` 使用说明

该功能入口名为 `MultiAgents`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/controller.py` 第 85 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ModulePlugin` 使用说明

该功能入口名为 `ModulePlugin`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 28 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/utils/_template_files/default_serve_template/serve.py` 第 22 行。其所在子系统的定位是：dbgpt-serve 服务层。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SessionFileApiError` 使用说明

该功能入口名为 `SessionFileApiError`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/session_file/api/endpoints.py` 第 42 行。其所在子系统的定位是：会话文件服务。结合命名与模块归属，可以将其设计职责概括为：负责把业务服务暴露为 HTTP REST 接口。作者注释或文档字符串指出：Deterministic transport error carrying a stable domain code.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `_SessionFileApiRoute` 使用说明

该功能入口名为 `_SessionFileApiRoute`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/session_file/api/endpoints.py` 第 57 行。其所在子系统的定位是：会话文件服务。结合命名与模块归属，可以将其设计职责概括为：负责把业务服务暴露为 HTTP REST 接口。作者注释或文档字符串指出：Route class converting :class:`SessionFileApiError` to envelopes.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SessionFileServiceLike` 使用说明

该功能入口名为 `SessionFileServiceLike`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/session_file/api/endpoints.py` 第 80 行。其所在子系统的定位是：会话文件服务。结合命名与模块归属，可以将其设计职责概括为：负责元数据、向量、缓存或文件的持久化访问。作者注释或文档字符串指出：Storage orchestration seam implemented by the session file registry.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/dbgpts/my/serve.py` 第 21 行。其所在子系统的定位是：DBGPTS 市场。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `Serve` 使用说明

该功能入口名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/dbgpts/hub/serve.py` 第 21 行。其所在子系统的定位是：DBGPTS 市场。结合命名与模块归属，可以将其设计职责概括为：负责服务组件的注册、配置与路由挂载。作者注释或文档字符串指出：Serve component for DB-GPT用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatParam` 使用说明

该功能入口名为 `ChatParam`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/base_chat.py` 第 57 行。其所在子系统的定位是：业务场景实现。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `BaseChat` 使用说明

该功能入口名为 `BaseChat`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/base_chat.py` 第 113 行。其所在子系统的定位是：业务场景实现。结合命名与模块归属，可以将其设计职责概括为：负责提示词模板的存储、渲染与场景绑定。作者注释或文档字符串指出：DB-GPT Chat Service Base Module Include: stream_call():scene + prompt -> stream response nostream_call():scene + prompt -> nostream response用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `AgentCitation` 使用说明

该功能入口名为 `AgentCitation`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py` 第 42 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：One knowledge source cited by a final answer.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `AgentFinalAnswer` 使用说明

该功能入口名为 `AgentFinalAnswer`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py` 第 67 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Pure final content plus independently rendered citations.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `_CitationDraft` 使用说明

该功能入口名为 `_CitationDraft`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py` 第 90 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `FinalAnswerAssembler` 使用说明

该功能入口名为 `FinalAnswerAssembler`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py` 第 109 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Collect citations from an allowlist of knowledge-producing tools.  Tool output is not a citation by default. Each trusted tool has an explicit adapter for its known output shape; malformed or failed output is ignored.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `_AgentStreamingResponse` 使用说明

该功能入口名为 `_AgentStreamingResponse`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` 第 1204 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Streaming response that explicitly closes its owned body iterator.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ShareCreateRequest` 使用说明

该功能入口名为 `ShareCreateRequest`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` 第 3589 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Request body for creating a share link.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ShareCreateResponse` 使用说明

该功能入口名为 `ShareCreateResponse`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` 第 3595 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Response body for share link creation.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ShareConvResponse` 使用说明

该功能入口名为 `ShareConvResponse`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` 第 3603 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Public payload returned when viewing a shared conversation.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `_QuestionReplyBody` 使用说明

该功能入口名为 `_QuestionReplyBody`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/agentic_data_api.py` 第 3930 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `AttachmentInputError` 使用说明

该功能入口名为 `AttachmentInputError`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/attachment_react_adapter.py` 第 68 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Deterministic chat attachment error carrying an HTTP-style mapping.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SessionAttachmentContext` 使用说明

该功能入口名为 `SessionAttachmentContext`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/attachment_react_adapter.py` 第 284 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Resolved attachment state for one chat turn.  ``manifests``/``prompt``/``inspections`` are public and safe to render. ``local_paths``/``primary_local_path`` are per-turn temporary copies: they may reach prompts and tool observations so the model can analyze files with ``code_interpreter``; they are deleted on :meth:`close` and scrubbed at the share boundary (see :func:`scrub_react_history_for_share`). ``files_json_path`` remains internal (child-process handoff only). Storage URIs and hashes neve用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `EditorService` 使用说明

该功能入口名为 `EditorService`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/service.py` 第 40 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `DataNode` 使用说明

该功能入口名为 `DataNode`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/sql_editor.py` 第 7 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责 SQL 生成、校验或执行。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SqlRunData` 使用说明

该功能入口名为 `SqlRunData`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/sql_editor.py` 第 18 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责 SQL 生成、校验或执行。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChartRunData` 使用说明

该功能入口名为 `ChartRunData`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/sql_editor.py` 第 25 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责 SQL 生成、校验或执行。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SettingsEntity` 使用说明

该功能入口名为 `SettingsEntity`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/settings.py` 第 10 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：Settings entity用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SettingsRequest` 使用说明

该功能入口名为 `SettingsRequest`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/settings.py` 第 46 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：Settings request.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SettingsResponse` 使用说明

该功能入口名为 `SettingsResponse`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/settings.py` 第 56 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SettingsDao` 使用说明

该功能入口名为 `SettingsDao`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/settings.py` 第 65 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责实体与请求/响应之间的转换以及数据库 CRUD。作者注释或文档字符串指出：Settings dao.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConvLinksEntity` 使用说明

该功能入口名为 `ConvLinksEntity`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/conv_links.py` 第 10 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConvLinksRequest` 使用说明

该功能入口名为 `ConvLinksRequest`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/conv_links.py` 第 47 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConvLinksResponse` 使用说明

该功能入口名为 `ConvLinksResponse`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/conv_links.py` 第 55 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ConvLinksDao` 使用说明

该功能入口名为 `ConvLinksDao`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/links/conv_links.py` 第 66 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责实体与请求/响应之间的转换以及数据库 CRUD。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `SubTask` 使用说明

该功能入口名为 `SubTask`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/subagent/__init__.py` 第 34 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A single independent sub-task for ``dispatch_parallel_tasks``.  Attributes:     goal: Required. Self-contained objective for the sub-agent.     context: Optional. Upstream results / shared background.     title: Optional. Short label for UI display.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `QuestionOption` 使用说明

该功能入口名为 `QuestionOption`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/tools/question_manager.py` 第 13 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `QuestionInfo` 使用说明

该功能入口名为 `QuestionInfo`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/tools/question_manager.py` 第 19 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `PendingQuestion` 使用说明

该功能入口名为 `PendingQuestion`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/tools/question_manager.py` 第 28 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `QuestionManager` 使用说明

该功能入口名为 `QuestionManager`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/tools/question_manager.py` 第 37 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责大模型请求构造、流式输出、指标采集与多提供商适配。作者注释或文档字符串指出：Global manager for all pending question requests across sessions.  Each question tool invocation: 1. Calls ``create()`` → gets a PendingQuestion with a fresh asyncio.Event. 2. Pushes a ``question.asked`` SSE event to the frontend. 3. Awaits ``pending.event.wait()`` — blocks the tool coroutine. 4. When the user replies via HTTP, ``reply()`` sets the event and stores answers. 5. The tool coroutine wakes up, reads the answers, and returns to the LLM.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatFeedBackDao` 使用说明

该功能入口名为 `ChatFeedBackDao`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/feedback/feed_back_db.py` 第 10 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责实体与请求/响应之间的转换以及数据库 CRUD。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `FeedBackBody` 使用说明

该功能入口名为 `FeedBackBody`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/feedback/feed_back_model.py` 第 6 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责模型注册、推理工作进程、代理协议与集群管理。作者注释或文档字符串指出：conv_uid: conversation id用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatHistory` 使用说明

该功能入口名为 `ChatHistory`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/_chat_history/chat_hisotry_factory.py` 第 21 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `DbHistoryMemory` 使用说明

该功能入口名为 `DbHistoryMemory`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/_chat_history/meta_db_history.py` 第 15 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责会话记忆的写入、检索、压缩与跨轮次复用。作者注释或文档字符串指出：Db history memory storage.  It is deprecated.用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `MemoryStoreType` 使用说明

该功能入口名为 `MemoryStoreType`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/_chat_history/base.py` 第 10 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责会话记忆的写入、检索、压缩与跨轮次复用。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `BaseChatHistoryMemory` 使用说明

该功能入口名为 `BaseChatHistoryMemory`，定义于 `packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/editor/_chat_history/base.py` 第 17 行。其所在子系统的定位是：OpenAPI 与 Agentic Data API。结合命名与模块归属，可以将其设计职责概括为：负责会话记忆的写入、检索、压缩与跨轮次复用。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatNormal` 使用说明

该功能入口名为 `ChatNormal`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_normal/chat.py` 第 9 行。其所在子系统的定位是：普通对话场景。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatDashboard` 使用说明

该功能入口名为 `ChatDashboard`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_dashboard/chat.py` 第 23 行。其所在子系统的定位是：仪表盘生成场景。结合命名与模块归属，可以将其设计职责概括为：负责把查询结果组织成图表与仪表盘。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatKnowledge` 使用说明

该功能入口名为 `ChatKnowledge`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_knowledge/v1/chat.py` 第 30 行。其所在子系统的定位是：知识库问答与精炼摘要场景。结合命名与模块归属，可以将其设计职责概括为：负责知识空间、文档解析、切片与索引构建。作者注释或文档字符串指出：KBQA Chat Module用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ExtractRefineSummary` 使用说明

该功能入口名为 `ExtractRefineSummary`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_knowledge/refine_summary/chat.py` 第 9 行。其所在子系统的定位是：知识库问答与精炼摘要场景。结合命名与模块归属，可以将其设计职责概括为：负责大模型请求构造、流式输出、指标采集与多提供商适配。作者注释或文档字符串指出：extract final summary by llm用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatExcel` 使用说明

该功能入口名为 `ChatExcel`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_data/chat_excel/excel_analyze/chat.py` 第 23 行。其所在子系统的定位是：表格/Excel 数据分析场景。结合命名与模块归属，可以将其设计职责概括为：负责表格文件读取、学习与分析。作者注释或文档字符串指出：a Excel analyzer to analyze Excel Data用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ExcelLearning` 使用说明

该功能入口名为 `ExcelLearning`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_data/chat_excel/excel_learning/chat.py` 第 16 行。其所在子系统的定位是：表格/Excel 数据分析场景。结合命名与模块归属，可以将其设计职责概括为：负责表格文件读取、学习与分析。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatWithDbQA` 使用说明

该功能入口名为 `ChatWithDbQA`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_db/professional_qa/chat.py` 第 12 行。其所在子系统的定位是：数据库对话场景，含自动执行 SQL 与专业问答。结合命名与模块归属，可以将其设计职责概括为：负责 API 请求与响应的结构化校验。作者注释或文档字符串指出：As a DBA, Chat DB Module, chat with combine DB meta schema用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

#### `ChatWithDbAutoExecute` 使用说明

该功能入口名为 `ChatWithDbAutoExecute`，定义于 `packages/dbgpt-app/src/dbgpt_app/scene/chat_db/auto_execute/chat.py` 第 17 行。其所在子系统的定位是：数据库对话场景，含自动执行 SQL 与专业问答。结合命名与模块归属，可以将其设计职责概括为：作为所在分层的业务或基础设施组件，承接上下游接口并完成具体计算或编排。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。用户侧表现为对应菜单或 API。调用时需提供会话 ID、模型名与场景参数。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

## 第四章 配置说明

配置以 TOML 为主，样例在 configs/ 目录。安装器按 profile 复制并写入密钥。下列说明覆盖公共段与各样例文件的意图，生产环境必须用环境变量注入密钥，不要把真实 sk- 提交进 git。

### 4.1 [system]

language 控制默认语言，可用环境变量 DBGPT_LANG。api_keys 为空表示不校验网关钥；生产应设置为随机串列表。encrypt_key 用于敏感字段加密，必须修改默认 your_secret_key，并妥善备份，丢失会导致已加密配置无法解密。

### 4.2 [service.web]

host 默认 0.0.0.0 以便容器访问，本机可改 127.0.0.1。port 默认 5670，冲突时修改并同步前端代理。cors_allowed_origins 默认 *，生产改为明确前端域名列表。

### 4.3 [service.web.agent_context]

max_context_tokens=0 表示从模型元数据自动探测。reserved_tokens 默认 4096，给生成留空。warning_threshold、error_threshold、critical_threshold 为 0.70/0.90/0.95。min_keep_recent_rounds 至少保留 3 轮，避免压缩后完全失忆。max_observation_age_rounds 与 truncated_observation_max_chars 控制旧工具输出的保留。min_keep_tokens 防止压缩过头。max_compact_failures 避免压缩死循环。max_parallel_subagents 默认 3，可用环境变量 DBGPT_MAX_PARALLEL_SUBAGENTS 覆盖。

### 4.4 [service.web.database]

开发默认 sqlite 路径 pilot/meta_data/dbgpt.db。生产建议 MySQL，注意字符集 utf8mb4 与备份。切换库前先迁移。

### 4.5 [rag.storage.vector]

默认 chroma + persist_path=pilot/data。更换 milvus 等需安装对应 extra 并填写连接地址。嵌入模型变更后通常需要重建索引。

### 4.6 [models]

[[models.llms]] 与 [[models.embeddings]] 可重复多条。name、provider、api_base、api_key 支持 ${env:...} 展开。provider 形如 proxy/openai、proxy/tongyi，本地则为对应 local 实现。

### 4.7 样例文件 `dbgpt-app-config.example.toml`

该文件路径为 `configs/dbgpt-app-config.example.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
log_level = "INFO"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670
# CORS allowed origins: '*' allows all; set comma-separated origins to restrict.
cors_allowed_origins = "${env:DBGPT_CORS_ALLOWED_ORIGINS:-*}"

[service.web.agent_context]
# Agent context-window budget. Set max_context_tokens to 0 to auto-detect from
# the selected model's metadata. The effective budget shown in the UI is
# max_context_tokens - reserved_tokens.
max_context_tokens = 0
reserved_tokens = 4096
warning_threshold = 0.70
error_threshold = 0.90
critical_threshold = 0.95
min_keep_recent_rounds = 3
max_observation_age_rounds = 5
truncated_observation_max_chars = 200
min_keep_tokens = 10000
max_compact_failures = 3
# Per dispatch_parallel_tasks call. DBGPT_MAX_PARALLEL_SUBAGENTS overrides it.
max_parallel_subagents = 3

# Session file upload limits
[dbgpt.serve.session_file]
max_owner_bytes = -1

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[app]
temperature = 0.6

[[app.configs]]
name = "chat_excel"
temperature = 0.1
duckdb_extensions_dir = []
force_install = true

[[app.configs]]
name = "chat_normal"
memory = {type="token", max_token_limit=20000}

[[app.configs]]
name = "chat_with_db_qa"
schema_retrieve_top_k = 50
memory = {type="token", max_token_limit=20000}

# Model Configurations
[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/openai}"
api_base = "${env:OPENAI_API_BASE:-https://api.openai.com/v1}"
api_key = "${env:OPENAI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/openai}"
api_url = "${env:EMBEDDING_MODEL_API_URL:-https://api.openai.com/v1/embeddings}"
api_key = "${env:OPENAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.8 样例文件 `dbgpt-bm25-rag.toml`

该文件路径为 `configs/dbgpt-bm25-rag.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
log_level = "INFO"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"


[rag]
chunk_size=1000
chunk_overlap=100
similarity_top_k=5
similarity_score_threshold=0.0
max_chunks_once_load=10
max_threads=1
rerank_top_k=3

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

[rag.storage.full_text]
type = "elasticsearch"
host="127.0.0.1"
port=9200


# Model Configurations
[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/openai}"
api_base = "${env:OPENAI_API_BASE:-https://api.openai.com/v1}"
api_key = "${env:OPENAI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/openai}"
api_url = "${env:EMBEDDING_MODEL_API_URL:-https://api.openai.com/v1/embeddings}"
api_key = "${env:OPENAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.9 样例文件 `dbgpt-cloud-storage.example.toml`

该文件路径为 `configs/dbgpt-cloud-storage.example.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
log_level = "INFO"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[[serves]]
type = "file"
# Default backend for file server
default_backend = "s3"

[[serves.backends]]
type = "oss"
endpoint = "https://oss-cn-beijing.aliyuncs.com"
region = "oss-cn-beijing"
access_key_id = "${env:OSS_ACCESS_KEY_ID}"
access_key_secret = "${env:OSS_ACCESS_KEY_SECRET}"
fixed_bucket = "{your_bucket_name}"

[[serves.backends]]
# Use Tencent COS s3 compatible API as the file server
type = "s3"
endpoint = "https://cos.ap-beijing.myqcloud.com"
region = "ap-beijing"
access_key_id = "${env:COS_SECRETID}"
access_key_secret = "${env:COS_SECRETKEY}"
fixed_bucket = "{your_bucket_name}"

# Model Configurations
[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/openai}"
api_base = "${env:OPENAI_API_BASE:-https://api.openai.com/v1}"
api_key = "${env:OPENAI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/openai}"
api_url = "${env:EMBEDDING_MODEL_API_URL:-https://api.openai.com/v1/embeddings}"
api_key = "${env:OPENAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.10 样例文件 `dbgpt-graphrag.toml`

该文件路径为 `configs/dbgpt-graphrag.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
log_level = "INFO"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"


[rag]
chunk_size=1000
chunk_overlap=0
similarity_top_k=5
similarity_score_threshold=0.0
max_chunks_once_load=10
max_threads=1
rerank_top_k=3

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

[rag.storage.graph]
type = "tugraph"
host="127.0.0.1"
port=7687
username="admin"
password="73@TuGraph"

# enable_summary="True"
# community_topk=20
# community_score_threshold=0.3

# triplet_graph_enabled="True"
# extract_topk=20

# document_graph_enabled="True"
# knowledge_graph_chunk_search_top_size=20
# knowledge_graph_extraction_batch_size=20

# enable_similarity_search="True"
# knowledge_graph_embedding_batch_size=20
# similarity_search_topk=5
# extract_score_threshold=0.7

# enable_text_search="True"
# text2gql_model_enabled="True"
# text2gql_model_name="qwen2.5:latest"



# Model Configurations
[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/openai}"
api_base = "${env:OPENAI_API_BASE:-https://api.openai.com/v1}"
api_key = "${env:OPENAI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/openai}"
api_url = "${env:EMBEDDING_MODEL_API_URL:-https://api.openai.com/v1/embeddings}"
api_key = "${env:OPENAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.11 样例文件 `dbgpt-local-glm.toml`

该文件路径为 `configs/dbgpt-local-glm.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "THUDM/glm-4-9b-chat-hf"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/THUDM/glm-4-9b-chat-hf"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/BAAI/bge-large-zh-v1.5"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.12 样例文件 `dbgpt-local-llama-cpp-server.toml`

该文件路径为 `configs/dbgpt-local-llama-cpp-server.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "DeepSeek-R1-Distill-Qwen-1.5B"
provider = "llama.cpp.server"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# https://huggingface.co/bartowski/DeepSeek-R1-Distill-Qwen-1.5B-GGUF
# path = "the-model-path-in-the-local-file-system"
path = "models/DeepSeek-R1-Distill-Qwen-1.5B-Q4_K_M.gguf"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/BAAI/bge-large-zh-v1.5"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.13 样例文件 `dbgpt-local-llama-cpp.toml`

该文件路径为 `configs/dbgpt-local-llama-cpp.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "DeepSeek-R1-Distill-Qwen-1.5B"
provider = "llama.cpp"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# https://huggingface.co/bartowski/DeepSeek-R1-Distill-Qwen-1.5B-GGUF
# path = "the-model-path-in-the-local-file-system"
path = "models/DeepSeek-R1-Distill-Qwen-1.5B-Q4_K_M.gguf"


[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/BAAI/bge-large-zh-v1.5"


```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.14 样例文件 `dbgpt-local-mlx.toml`

该文件路径为 `configs/dbgpt-local-mlx.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "Qwen/Qwen3-0.6B-MLX-4bit"
provider = "mlx"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# https://huggingface.co/Qwen/Qwen3-0.6B-MLX-4bit
# path = "the-model-path-in-the-local-file-system"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.15 样例文件 `dbgpt-local-qwen.toml`

该文件路径为 `configs/dbgpt-local-qwen.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "Qwen2.5-Coder-0.5B-Instruct"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/Qwen2.5-Coder-0.5B-Instruct"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/BAAI/bge-large-zh-v1.5"


```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.16 样例文件 `dbgpt-local-qwen3.example.toml`

该文件路径为 `configs/dbgpt-local-qwen3.example.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "Qwen/Qwen3-14B"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
# Force the model to be used in non-thinking mode, set to false
# reasoning_model = false

[[models.embeddings]]
name = "Qwen/Qwen3-Embedding-0.6B"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"

[[models.rerankers]]
name = "Qwen/Qwen3-Reranker-0.6B"
provider = "qwen"
```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.17 样例文件 `dbgpt-local-vllm.toml`

该文件路径为 `configs/dbgpt-local-vllm.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "DeepSeek-R1-Distill-Qwen-1.5B"
provider = "vllm"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/DeepSeek-R1-Distill-Qwen-1.5B"
# dtype = "float32"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "/data/models/bge-large-zh-v1.5"


```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.18 样例文件 `dbgpt-proxy-aimlapi.toml`

该文件路径为 `configs/dbgpt-proxy-aimlapi.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "proxy/aimlapi"
api_key = "${env:AIMLAPI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "proxy/aimlapi"
api_url = "https://api.aimlapi.com/v1/embeddings"
api_key = "${env:AIMLAPI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.19 样例文件 `dbgpt-proxy-burncloud.toml`

该文件路径为 `configs/dbgpt-proxy-burncloud.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"
[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
# Claude Models
[[models.llms]]
name = "claude-sonnet-4-20250514"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

[[models.llms]]
name = "claude-3-7-sonnet-20250219"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

[[models.llms]]
name = "claude-opus-4-20250514"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

[[models.llms]]
name = "gpt-5"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

[[models.llms]]
name = "gpt-5-chat-latest"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

# Gemini Models
[[models.llms]]
name = "gemini-2.5-pro"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"

# DeepSeek Models
[[models.llms]]
name = "DeepSeek-V3"
provider = "proxy/burncloud"
api_key = "${env:BURNCLOUD_API_KEY}"
```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.20 样例文件 `dbgpt-proxy-deepseek.toml`

该文件路径为 `configs/dbgpt-proxy-deepseek.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"
[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "deepseek-v4-pro"
# name = "deepseek-reasoner"
# name = "deepseek-chat"
provider = "proxy/deepseek"
# Disable V4-Pro thinking mode so ReAct responses stay parseable.
thinking_enabled = false
api_key = "your_deepseek_api_key"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/bge-large-zh-v1.5"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.21 样例文件 `dbgpt-proxy-infiniai.toml`

该文件路径为 `configs/dbgpt-proxy-infiniai.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"
[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "deepseek-v3"
provider = "proxy/infiniai"
api_key = "${env:INFINIAI_API_KEY}"

[[models.embeddings]]
name = "bge-m3"
provider = "proxy/openai"
api_url = "https://cloud.infini-ai.com/maas/v1/embeddings"
api_key = "${env:INFINIAI_API_KEY}"

[[models.rerankers]]
name = "bge-reranker-v2-m3"
provider = "proxy/infiniai"
api_key = "${env:INFINIAI_API_KEY}" 
```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.22 样例文件 `dbgpt-proxy-litellm.toml`

该文件路径为 `configs/dbgpt-proxy-litellm.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"
[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
#
# LiteLLM is used here as an embedded AI gateway (the Python SDK), NOT as a
# separate proxy server — DB-GPT imports litellm directly and routes every
# completion through litellm.acompletion(). Specify the model with a provider
# prefix (e.g. "anthropic/...", "vertex_ai/...", "bedrock/...", "azure/...",
# "groq/...") and set the matching provider environment variable
# (ANTHROPIC_API_KEY, OPENAI_API_KEY, AWS_ACCESS_KEY_ID, AZURE_API_KEY, ...).
# See https://docs.litellm.ai/docs/providers for the full list.
[models]
[[models.llms]]
name = "anthropic/claude-3-5-sonnet-20241022"
provider = "proxy/litellm"
# api_key and api_base are usually unnecessary — LiteLLM resolves them per
# provider from environment variables. Override only when using a custom or
# OpenAI-compatible endpoint.
# api_key = "your_anthropic_api_key"
# api_base = "https://api.anthropic.com"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "hf"
# If not provided, the model will be downloaded from the Hugging Face model hub
# uncomment the following line to specify the model path in the local file system
# path = "the-model-path-in-the-local-file-system"
path = "models/bge-large-zh-v1.5"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.23 样例文件 `dbgpt-proxy-ollama.toml`

该文件路径为 `configs/dbgpt-proxy-ollama.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "deepseek-r1:1.5b"
provider = "proxy/ollama"
api_base = "http://localhost:11434"
api_key = ""

[[models.embeddings]]
name = "bge-m3:latest"
provider = "proxy/ollama"
api_url = "http://localhost:11434"
api_key = ""

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.24 样例文件 `dbgpt-proxy-openai.toml`

该文件路径为 `configs/dbgpt-proxy-openai.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670
# CORS allowed origins: '*' allows all; set comma-separated origins to restrict.
cors_allowed_origins = "${env:DBGPT_CORS_ALLOWED_ORIGINS:-*}"

[service.web.agent_context]
# Agent context-window budget. Set max_context_tokens to 0 to auto-detect from
# the selected model's metadata. The effective budget shown in the UI is
# max_context_tokens - reserved_tokens.
max_context_tokens = 0
reserved_tokens = 4096
warning_threshold = 0.70
error_threshold = 0.90
critical_threshold = 0.95
min_keep_recent_rounds = 3
max_observation_age_rounds = 5
truncated_observation_max_chars = 200
min_keep_tokens = 10000
max_compact_failures = 3
# Per dispatch_parallel_tasks call. DBGPT_MAX_PARALLEL_SUBAGENTS overrides it.
max_parallel_subagents = 3

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-gpt-4o}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/openai}"
api_base = "${env:OPENAI_API_BASE:-https://api.openai.com/v1}"
api_key = "${env:OPENAI_API_KEY}"

[[models.embeddings]]
name = "${env:EMBEDDING_MODEL_NAME:-text-embedding-3-small}"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/openai}"
api_url = "${env:EMBEDDING_MODEL_API_URL:-https://api.openai.com/v1/embeddings}"
api_key = "${env:OPENAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.25 样例文件 `dbgpt-proxy-orcarouter.toml`

该文件路径为 `configs/dbgpt-proxy-orcarouter.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-openai/gpt-4o}"
provider = "proxy/orcarouter"
api_key = "${env:ORCAROUTER_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.26 样例文件 `dbgpt-proxy-siliconflow-mysql.toml`

该文件路径为 `configs/dbgpt-proxy-siliconflow-mysql.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670
# CORS allowed origins: '*' allows all; set comma-separated origins to restrict.
cors_allowed_origins = "${env:DBGPT_CORS_ALLOWED_ORIGINS:-*}"

[service.web.database]
type = "mysql"
host = "${env:MYSQL_HOST:-127.0.0.1}"
port = "${env:MYSQL_PORT:-3306}"
database = "${env:MYSQL_DATABASE:-dbgpt}"
user = "${env:MYSQL_USER:-root}"
password ="${env:MYSQL_PASSWORD:-aa123456}"

[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "Qwen/Qwen2.5-Coder-32B-Instruct"
provider = "proxy/siliconflow"
api_key = "${env:SILICONFLOW_API_KEY}"

[[models.embeddings]]
name = "BAAI/bge-m3"
provider = "proxy/siliconflow"
api_key = "${env:SILICONFLOW_API_KEY}"

[[models.rerankers]]
name = "BAAI/bge-reranker-v2-m3"
provider = "proxy/siliconflow"
api_key = "${env:SILICONFLOW_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.27 样例文件 `dbgpt-proxy-siliconflow.toml`

该文件路径为 `configs/dbgpt-proxy-siliconflow.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-zh}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670
# CORS allowed origins: '*' allows all; set comma-separated origins to restrict.
cors_allowed_origins = "${env:DBGPT_CORS_ALLOWED_ORIGINS:-*}"

[service.web.agent_context]
# Agent context-window budget. Non-positive values fall back to the default
# budget. The effective budget shown in the UI is
# max_context_tokens - reserved_tokens.
max_context_tokens = 120000
reserved_tokens = 4096
warning_threshold = 0.70
error_threshold = 0.90
critical_threshold = 0.95
min_keep_recent_rounds = 3
max_observation_age_rounds = 5
truncated_observation_max_chars = 200
min_keep_tokens = 10000
max_compact_failures = 3
# Per dispatch_parallel_tasks call. DBGPT_MAX_PARALLEL_SUBAGENTS overrides it.
max_parallel_subagents = 3

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"
[service.model.worker]
host = "127.0.0.1"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "Qwen/Qwen2.5-Coder-32B-Instruct"
provider = "proxy/siliconflow"
api_key = "${env:SILICONFLOW_API_KEY}"

[[models.embeddings]]
name = "BAAI/bge-large-zh-v1.5"
provider = "proxy/openai"
api_url = "https://api.siliconflow.cn/v1/embeddings"
api_key = "${env:SILICONFLOW_API_KEY}"

[[models.rerankers]]
name = "BAAI/bge-reranker-v2-m3"
provider = "proxy/siliconflow"
api_key = "${env:SILICONFLOW_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.28 样例文件 `dbgpt-proxy-synthorai.toml`

该文件路径为 `configs/dbgpt-proxy-synthorai.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

[service.web]
host = "0.0.0.0"
port = 5670

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

[models]
[[models.llms]]
name = "${env:LLM_MODEL_NAME:-claude-opus-5}"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/synthorai}"
api_key = "${env:SYNTHORAI_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

### 4.29 样例文件 `dbgpt-proxy-tongyi.toml`

该文件路径为 `configs/dbgpt-proxy-tongyi.toml`，用于对应 profile 的开箱配置。复制后改名放入实际配置目录，不要直接改仓库样例除非你在贡献新 profile。文件内容意图如下：根据其中的 provider、模型名与注释选择匹配的密钥环境变量。若文件名含 proxy，则走云端代理；含 local，则需本机 GPU 或 llama.cpp。含 rag、bm25、graphrag 的样例会额外打开检索相关段。含 mysql 的样例演示元数据或业务库连接。使用时逐步验证：先只配 LLM 能聊天，再配 embedding 能入库，再配业务数据源。

关键内容摘录与解释：每一行配置都对应运行时一个开关或地址，错误的 api_base 会导致 401/404，错误的 persist_path 会导致重启丢索引，错误的 database.path 会导致会话历史看似「被清空」。

```toml
[system]
# Load language from environment variable(It is set by the hook)
language = "${env:DBGPT_LANG:-en}"
api_keys = []
encrypt_key = "your_secret_key"

# Server Configurations
[service.web]
host = "0.0.0.0"
port = 5670
# CORS allowed origins: '*' allows all; set comma-separated origins to restrict.
cors_allowed_origins = "${env:DBGPT_CORS_ALLOWED_ORIGINS:-*}"

[service.web.agent_context]
# Agent context-window budget. Set max_context_tokens to 0 to auto-detect from
# the selected model's metadata. The effective budget shown in the UI is
# max_context_tokens - reserved_tokens.
max_context_tokens = 0
reserved_tokens = 4096
warning_threshold = 0.70
error_threshold = 0.90
critical_threshold = 0.95
min_keep_recent_rounds = 3
max_observation_age_rounds = 5
truncated_observation_max_chars = 200
min_keep_tokens = 10000
max_compact_failures = 3
# Per dispatch_parallel_tasks call. DBGPT_MAX_PARALLEL_SUBAGENTS overrides it.
max_parallel_subagents = 3

# Session file upload limits
[dbgpt.serve.session_file]
max_owner_bytes = -1

[service.web.database]
type = "sqlite"
path = "pilot/meta_data/dbgpt.db"

[rag.storage]
[rag.storage.vector]
type = "chroma"
persist_path = "pilot/data"

# Model Configurations
[models]
[[models.llms]]
name = "qwen-plus"
provider = "${env:LLM_MODEL_PROVIDER:-proxy/tongyi}"
api_base = "https://dashscope.aliyuncs.com/compatible-mode/v1"
api_key = "${env:DASHSCOPE_API_KEY}"

[[models.embeddings]]
name = "text-embedding-v3"
provider = "${env:EMBEDDING_MODEL_PROVIDER:-proxy/tongyi}"
api_url = "https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings"
api_key = "${env:DASHSCOPE_API_KEY}"

```

落地检查清单：密钥是否来自环境变量；CORS 是否收紧；SQLite 是否仅用于开发；向量目录是否在持久卷；encrypt_key 是否已轮换；日志级别是否在生产改为 INFO 而非 DEBUG；是否避免把该文件提交到公共分支。若启动报未知字段，对照当前版本 ApplicationConfig 而不是旧博客。

## 第五章 常见问题 100 个

### FAQ 1. 安装脚本能否在生产服务器直接 curl | bash？

不建议。应先下载审阅，固定 commit，再在变更管理下执行。生产更推荐镜像或内部包。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 2. 必须用 uv 吗？

官方推荐 uv，pip 也可以安装 dbgpt-app。源码工作区用 uv workspace 更不容易版本漂移。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 3. Python 3.9 能用吗？

不能，requires-python 为 >=3.10。请升级或使用容器。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 4. 默认端口被占用怎么办？

修改 service.web.port，同时改前端代理与防火墙。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 5. 打开 5670 白屏

检查是否完成 web 构建、静态资源路径、浏览器控制台 404。源码模式需先 npm run build 或 dev。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 6. 提示 OPENAI_API_KEY 缺失

导出环境变量或写入 profile 后重启，不要只改界面缓存。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 7. Kimi 配好了仍 401

确认用 MOONSHOT_API_KEY 且 profile 为 kimi，检查 api_base 是否为月之暗面开放平台。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 8. MiniMax 怎么配

MINIMAX_API_KEY 加 --profile minimax，其接口为 OpenAI 兼容。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 9. 本地 Ollama 连不上

确认 Ollama 监听地址可被 DB-GPT 进程访问，容器网络不要用 localhost 指向宿主机时漏了 host.docker.internal。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 10. 聊天一直转圈

看后端日志：模型代理超时、流式被缓冲、检索卡住。用 tracer 看首 token。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 11. 回答与数据库无关

你可能在普通对话模式。切换 Chat DB 并选择数据源。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 12. SQL 执行失败

把错误信息发回专业问答模式，检查方言、表名大小写、权限。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 13. 会不会 DROP 表？

应使用只读账号。并在连接器与工具层限制语句类型。发现可写权限立即收回。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 14. Excel 上传失败

检查大小限制、格式是否真实 xlsx、列名是否异常编码。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 15. 中文乱码

统一 UTF-8。MySQL 用 utf8mb4。CSV 尝试 gbk 再转码。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 16. 知识库问答没有引用

文档未切完、embedding 失败、或检索 top_k 为 0。查看知识空间文档状态。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 17. PDF 问答很差

可能是扫描件。先 OCR。或切块切断了表格，改切分参数。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 18. 换了 embedding 模型后效果崩了

必须重建向量索引，新旧向量不在同一空间。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 19. Chroma 重启数据没了

persist_path 没挂卷或指到临时目录。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 20. 如何改用 Milvus

安装 storage_milvus extra，改 rag.storage.vector.type 与连接配置。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 21. Agent 不调用工具

模型太弱或不遵循 ReAct。换更强模型或检查提示词是否要求 Action 格式。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 22. Action Input JSON 解析失败

已知问题：特殊 token 泄漏。升级含清洗逻辑的版本，或换不会泄漏协议的网关。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 23. terminate 之后还在跑

解析器未识别终止动作。查看原始输出前缀是否被翻译成中文「行动」。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 24. 子智能体把库打满

降低 max_parallel_subagents，给源库加连接限制。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 25. 上下文警告一直出现

开新会话，或把材料入库检索，不要在对话里贴全书。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 26. 压缩后 Agent 忘记目标

提高 min_keep_recent_rounds，把目标写入长期记忆或系统提示。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 27. 技能从 GitHub 导入失败

检查网络、仓库结构是否符合 skill 规范、是否需要 token。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 28. 沙箱里没 pandas

安装包含分析依赖的镜像或 extra，不要在只读系统 Python 里期望齐全。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 29. 代码解释器超时

增大超时、抽样数据、避免笛卡尔积。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 30. 想读 /etc/passwd

会被文件客户端与沙箱拒绝。不要绕过，这是安全设计。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 31. CORS 报错

设置 cors_allowed_origins 为前端实际 origin，含协议与端口。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 32. 如何加 API 鉴权

配置 system.api_keys，网关层再加 SSO。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 33. 多用户如何隔离

当前多租户字段存在但产品默认偏单机助手。需要在 serve 层按 user_name/sys_code 过滤，并配合鉴权。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 34. 会话列表空白

元数据库路径变化导致连到空库。核对 database.path。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 35. 升级后旧流程打不开

flow 兼容迁移需返回视图元数据，检查相关修复是否包含在版本中。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 36. 如何备份

备份 SQLite/MySQL、向量目录、上传目录、TOML。恢复时版本要匹配。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 37. 日志在哪

LOGDIR 配置与启动参数。容器要挂日志卷。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 38. GPU 占满

本地模型与 embedding 同卡。限制并发或拆机器。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 39. CPU 也能跑吗

可以跑代理云模型。本地大模型会极慢，仅建议小模型或 llama.cpp 量化。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 40. 和 LangChain 什么关系

切分等实现有参考，但产品是独立的 Agent+AWEL+RAG 平台。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 41. 是否兼容 OpenAI SDK

提供兼容风格接口与 Python client，但场景参数是 DB-GPT 自己的。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 42. 如何关掉自动执行 SQL

用专业问答模式或在工具策略禁用 sql 执行。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 43. 仪表盘数字不对

核对 SQL 时间窗口、汇率、是否未过滤测试数据。不能无校验用于对外披露。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 44. 如何添加新数据源

在 dbgpt-ext 实现连接器并注册，安装驱动 extra。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 45. DuckDB 锁文件

并发分析同一上传文件时注意连接生命周期，避免未关闭。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 46. i18n 不生效

设置 language 与前端 locales，重启。部分提示词仍可能英文。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 47. Windows 路径问题

优先 WSL2。注意反斜杠与权限。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 48. npm 构建内存不足

脚本已设 max_old_space_size，仍不够则加大机器或关闭并行。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 49. 前端改了不见

确认连的是 dev 端口还是后端静态旧构建。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 50. 如何贡献代码

阅读 CONTRIBUTING，ruff 格式化，补测试，走 PR 模板。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 51. License 是什么

MIT。注意第三方模型与数据许可证另算。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 52. 能否完全离线

本地模型 + 本地向量 + 本地元数据可以。不要配代理 provider。首次仍需下载权重。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 53. HuggingFace 下载慢

使用镜像环境变量或预置模型目录。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 54. token 消耗太大

减小历史轮次、关闭无用重写、检索 top_k 不要过大、选小 embedding。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 55. 如何限流

在网关或模型代理侧限流，应用内注意并行子智能体上限。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 56. SSO 怎么接

前端 next-auth 可扩展，后端 API 加中间件校验 JWT，不要只藏按钮。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 57. 审计 SQL 去哪查

会话消息、tracer、数据库审计日志应三合一，生产自己接 SIEM。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 58. 如何写自己的 skill

参考 skills/README 与 skill_implementation_guide.py，提供清晰输入输出与样例。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 59. skill 能访问外网吗

取决于沙箱策略。默认应谨慎开放，防止数据外带。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 60. GraphRAG 怎么开

参考 dbgpt-graphrag.toml 与文档，确认图存储依赖已装。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 61. BM25 与向量能否混合

有 bm25 样例。混合检索要调融合权重，需评测。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 62. 代码仓库问答

使用代码切分与 codegraph 相关服务，先同步 git repo。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 63. 定时任务没跑

进程必须常驻。容器被杀则调度消失。检查 APScheduler 日志。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 64. 反馈提交失败

检查反馈表迁移是否执行。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 65. 评测指标看不懂

先看检索命中再看生成，不要用单一分数决定上线。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 66. 模型列表是空的

TOML 未加载或 provider 初始化失败，看启动日志。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 67. embedding 维度不匹配

库是旧维度。重建。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 68. Chroma 版本冲突

按当前包依赖安装，不要强行升级 chromadb 大版本。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 69. SQLAlchemy 版本被锁

core 注释说明 2.0.29 与 duckdb 不兼容，不要随意放开上界。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 70. 如何设置只读 SQL

数据库账号只读 + 工具层拦截。双保险。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 71. Agent 编造表名

把 schema 检索做好，提示词要求必须使用提供的表，执行失败要回喂。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 72. 多表 join 很慢

先限制日期，建索引，或让 Agent 分步聚合。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 73. 时区错一天

统一存储 UTC，展示用业务时区，提示词写明时区。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 74. 货币单位混乱

在知识库放口径文档，技能里写死单位转换。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 75. 如何分享报告

导出 HTML/PDF 时注意内网链接与权限，不要把密钥截进图。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 76. html2canvas 截图空白

跨域图片与 WebGL 限制，改同源或服务端出图。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 77. 移动端体验差

产品偏桌面分析工作台，复杂编排请用桌面浏览器。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 78. 如何关闭遥测

检查 tracer 配置，生产可改采样率，但建议保留错误追踪。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 79. OpenTelemetry 怎么接

core 已有 opentelemetry 模块，按组织 collector 地址配置。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 80. 能否多实例

无状态 API 可水平扩，但调度器、SQLite、本地向量需要粘滞或外置。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 81. Redis 需要吗

不是最小依赖。缓存与会话外置是进阶方案。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 82. 如何改上传大小

反向代理 client_max_body_size 与应用 multipart 限制一起改。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 83. 病毒文件怎么办

上传扫描不在默认范围，应在网关加杀毒。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 84. GDPR 删除用户数据

需同时删会话、消息、文件、向量 metadata。当前要按表手工或写脚本。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 85. 中国大陆网络访问 GitHub 技能失败

用镜像或内网仓库导入。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 86. 公司代理 HTTPS 拦截

为 Python 与 Node 配置 REQUESTS/SSL 证书包。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 87. 如何固定模型版本

name 写具体版本不要 latest，并记录评测基线。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 88. 温度参数在哪

模型请求参数随场景 config 与界面高级选项，过高会导致 SQL 更不稳定。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 89. 流式输出重复字

代理缓冲或前端拼接 bug，抓原始 SSE 定位。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 90. Markdown 公式不渲染

前端 katex 依赖，检查内容是否合法 TeX。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 91. 暗色主题图表看不清

可视化主题未跟随，可在 vis 或前端主题补丁。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 92. 如何降级版本

备份数据后按 tag 安装，注意迁移不可逆要先试副本。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 93. uv.lock 冲突

以仓库锁文件为准重新 uv sync，不要各改各的。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 94. mypy 报错能否忽略

贡献代码应按 ci，局部可用类型注解而不是全局关闭。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 95. 测试目录 intetration 拼写

历史拼写，运行时按实际路径，不要只搜 integration。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 96. 如何只启动 API 不要前端

仍可由 FastAPI 提供 API，不挂静态或不用浏览器即可。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 97. SDK 超时

分析任务加长 httpx timeout，同时服务端 max_timeout。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 98. WebSocket 还是 SSE

对话流式以 HTTP SSE/chunk 为主，不要假设必须 WS。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 99. 能否接入企业内部 LLM 网关

走 OpenAI 兼容 proxy，改 api_base。验证 tool call 是否被网关改写。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

### FAQ 100. 出现「create space error, find more than 1 or 0 space」

知识空间重名或创建未提交成功，查表清理后重试。

排查顺序建议：先复现并记录请求时间、会话 ID、模型名、场景；再看浏览器网络面板状态码；再看后端异常栈；再看 tracer 耗时；再看配置是否加载了错误 profile。不要同时改模型、切分参数和提示词，否则无法归因。若问题涉及数据错误，先在源库用同一 SQL 验证。若问题涉及权限，用最小权限账号对照。若问题涉及前端，区分 dev 与生产静态资源。把结论写回团队知识库，避免重复踩坑。与本说明书其他章节交叉：安装见第二章，功能见第三章，配置见第四章。仍未解决时，准备最小复现、脱敏日志与版本号再向社区或内部平台组提问。

## 第六章 使用建议与安全须知

把 DB-GPT 当分析助手而不是自动运维机器人。默认假设模型会幻觉表名与口径。生产库只读、敏感文件不上传公有环境、密钥走环境变量、CORS 与 API Key 在暴露公网时必须启用。技能与代码执行等于在跑未知程序，只安装可信来源。重要报告要有人工复核。定期备份元数据与向量。关注上下文窗口，长会话及时归档。版本升级先在副本跑测试，特别是文件授权、flow 迁移、ReAct 解析相关用例。