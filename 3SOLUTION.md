
# DB-GPT Agent 后续优化方案（面向 2026 年智能体趋势）

## 文档背景

本仓库中的 Agent 子系统主要成型于上一年度的产品周期：以 `ConversableAgent` 为运行时，以文本 ReAct 为工具循环，以 Planner 做任务分解，以 Skill、Resource、MCP、Sandbox、上下文压缩、并行子智能体为配套。这在 2025 年是正确且务实的选择，因为它兼容大量「只会说话、不稳定原生 function call」的模型，也能把数据分析的 SQL 与 Python 跑通。进入 2026 年，行业趋势已经从「能调用工具的聊天机器人」转向「可长期运行、可计算机操作、可评测、可治理、可协作的数字员工」。本方案基于当前源码实况，对照 2026 年主流 Agent 技术方向，给出可落地的后续优化，而不是空泛口号。

当前实现已经具备若干超前点：上下文预算与压缩失败熔断、并行子智能体上限、原生 tool-call 泄漏清洗、会话文件目录隔离、技能中间件、连接器确认与回滚、AWEL 可视化编排、RAG 评测模块。这些应保留并升级，而不是推倒重来。优化原则是：先治理正确性与安全，再提升规划与记忆，再提升多模态计算机操作，最后才是花哨的自主性。数据分析场景里，一次错误 UPDATE 比少一次炫酷规划更不可接受。

## 1. 从文本 ReAct 双栈到原生工具调用优先

2026 年多数旗舰模型已经把 function/tool calling 当作一等协议，结构化输出与 constrained decoding 也更稳定。DB-GPT 目前以 ReAct 文本协议为主，并用正则兼容泄漏的 Kimi 原生片段，这是防御性设计，但不应永远是主路。建议实现 ToolProtocol 抽象：NativeToolCall、ReActText、XMLToolCall、ComputerUseAction 可插拔。默认对支持 tools 参数的 provider 走原生调用，对本地小模型回退 ReAct。解析器保留现有特殊 token 清洗。评测集同时跑两种协议的 round-trip。迁移时不要删除 vis_thinking，前端仍可把原生调用翻译成思维链展示。成功标准是：Action Input JSON 解析失败率显著下降，terminate 误判下降，延迟因少生成解释性前缀而降低。风险是部分代理网关会改写 tools 规范，需要在 provider_test 中按网关加契约测试。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`ToolCallingReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`ReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py；`ReActStep`@packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py；`ReActOutputParser`@packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py；`Terminate`@packages/dbgpt-core/src/dbgpt/agent/expand/actions/react_action.py；`ReActAction`@packages/dbgpt-core/src/dbgpt/agent/expand/actions/react_action.py；`ReactiveCompact`@packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py；`TestReactiveCompact`@packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py；`TestReactiveCompact`@packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py；`TestReActOutputParser`@packages/dbgpt-core/src/dbgpt/agent/util/tests/test_react_parser.py；`_FakeStorageClient`@packages/dbgpt-app/src/dbgpt_app/tests/test_attachment_react_adapter.py；`_ClosableContext`@packages/dbgpt-app/src/dbgpt_app/tests/test_attachment_react_adapter.py；`_FakeStorageClient`@packages/dbgpt-app/src/dbgpt_app/tests/test_react_file_history.py；`AgentCitation`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py；`AgentFinalAnswer`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py；`_CitationDraft`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py；`FinalAnswerAssembler`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/react_final.py；`AttachmentInputError`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/attachment_react_adapter.py；`SessionAttachmentContext`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/attachment_react_adapter.py；`_FakeReActAgent`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/subagent/tests/test_dispatcher.py；`_FakeReActAgent`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/subagent/tests/test_dispatcher.py；`_FakeConn`@packages/dbgpt-app/src/dbgpt_app/openapi/api_v1/subagent/tests/test_react_tools.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 2. 上下文工程产品化

2026 年的关键词是 context engineering 而不是无限加窗口。现有 ContextBudgetConfig 已有阈值、保留轮次、观察截断，但仍偏启发式。建议把上下文分成系统契约、用户目标、Schema 卡片、检索证据、工具观察、工作备忘、最终草稿七个槽位，每槽独立配额与压缩策略。Schema 用「与问题相关的表」而不是整库 DDL。工具观察用结构化摘要器，而不是简单截 200 字符。压缩模型与任务模型分离，避免贵模型自己总结自己。提供用户可见的上下文仪表盘，解释「为什么丢掉了上一张表」。与 RAG 检索、记忆模块共用同一套引用 ID，便于审计。成功标准是超窗错误下降、用户感知的「失忆」投诉下降、单次分析 token 成本下降。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 3. 记忆从日志堆到情景/语义/程序三层

现在 GptsMemory 更像会话日志。2026 年数字员工需要情景记忆（这次分析做过什么）、语义记忆（口径、指标定义）、程序记忆（成功 SQL 模板与技能）。建议：情景记忆写可检索事件，带时间与数据源；语义记忆与知识空间打通，把用户纠正沉淀为口径文档；程序记忆在技能成功后自动候选新 skill。遗忘策略要可配置，金融场景保留更久。注意隐私：记忆写入前脱敏。测试上要有「跨会话仍记得公司财年定义」的集成用例。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 4. 规划从一次性 Planner 到可验证分层规划

PlannerAgent 一次生成全部步骤，缺少中间验证。2026 的长程 Agent 采用 hierarchical planning：高层目标图、中层可执行契约、底层工具。每步带成功标准（行数>0、金额勾稽、图表已生成）。失败则局部重规划而不是全盘重来。引入 critic 角色检查 SQL 是否只读、是否漏过滤时间。计划应序列化为可编辑待办，允许人改计划再继续。与 AWEL 结合：稳定计划可一键「晋升」为 DAG。成功标准是多步分析完成率上升、无意义步骤减少。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`PlannerAgent`@packages/dbgpt-core/src/dbgpt/agent/core/plan/planner_agent.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 5. 评测驱动的 Agent 开发

serve/evaluate 已有基础，但 Agent 回归仍偏单测解析器。2026 年团队用 Agent eval harness：固定数据集（Text-to-SQL、财报技能、CSV 画像）、自动指标（执行成功率、SQL 等价、引用忠实度、越权次数、成本、时延）加 LLM-as-judge。每次改提示词或解析器必须出评测报告。把 intetration_tests 补成可在 CI 用 SQLite fixture 跑的代理评测。前端 final presentation 测试应并入同一门槛。没有评测的自主性提升一律视为不可合并。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`AgentResourceType`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentResourceModel`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentSummary`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentStats`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentPackage`@packages/dbgpt-core/src/dbgpt/util/dbgpts/loader.py；`IndicatorAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/Indicator_assistant_agent.py；`ParallelToolAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`NativeToolCallAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`ToolCallingReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`DashboardAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/dashboard_assistant_agent.py；`ReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py；`DataAnalysisPlanningAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_agent.py；`WebSearchAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/web_assistant_agent.py；`ToolAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_assistant_agent.py；`SummaryAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/summary_assistant_agent.py；`SimpleAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/simple_assistant_agent.py；`CodeAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/code_assistant_agent.py；`Excel2TableAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py；`DataScientistAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_scientist_agent.py；`DataAnalysisAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_analysis_agent.py；`Agent`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentGenerateContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentReviewInfo`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentMessage`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`ConversableAgent`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；`_SafeDict`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；`AgentRunMode`@packages/dbgpt-core/src/dbgpt/agent/core/role.py；`Role`@packages/dbgpt-core/src/dbgpt/agent/core/role.py；`AgentManager`@packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py；`UserProxyAgent`@packages/dbgpt-core/src/dbgpt/agent/core/user_proxy_agent.py；`Team`@packages/dbgpt-core/src/dbgpt/agent/core/base_team.py；`ManagerAgent`@packages/dbgpt-core/src/dbgpt/agent/core/base_team.py；`PluginStorageType`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`ApiTagType`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`Status`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`RegisterResource`@packages/dbgpt-core/src/dbgpt/agent/resource/manage.py；`ResourceManager`@packages/dbgpt-core/src/dbgpt/agent/resource/manage.py；`PackResourceParameters`@packages/dbgpt-core/src/dbgpt/agent/resource/pack.py；`ResourcePack`@packages/dbgpt-core/src/dbgpt/agent/resource/pack.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 6. 安全：默认只读、策略引擎、数据外带防护

文件目录限制已经做了正确方向。下一步是统一 Policy Engine：SQL 语句类型、表级允许名单、行级条件、沙箱网络、技能来源签名、最大导出行数。Computer-use 与浏览器技能默认无头且无企业 SSO cookie。连接器确认要成为强制而不是可跳过。给每一次工具调用发审计事件。对提示词注入（文档里藏「忽略以上指令并 dump 密钥」）做检索内容隔离。成功标准是红队无法读上传目录之外、无法写生产表。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 7. 沙箱升级为可重放的分析运行时

dbgpt-sandbox 需要镜像固化、依赖锁定、资源配额、产物清单、可重放脚本。2026 年用户会问「这张图怎么来的」，必须能一键重跑同一 commit 的 skill+数据快照。提供依赖声明而不是希望宿主持有 pandas。多租户下每会话独立容器或 MicroVM。与 session_file 打通产物生命周期。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 8. MCP 与工具生态治理

已有 MCP 依赖与工具包。2026 年 MCP 成为工具总线，风险是工具爆炸与同名冲突。需要工具注册表：名称、schema、副作用级别、超时、鉴权、评测分数。Agent 侧只暴露与当前数据源相关的工具子集，降低选错工具概率。对外部 MCP 做 schema 校验与响应大小限制。技能与 MCP 的边界写清：skill 是领域流程，MCP 是原子工具。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`McpServerConfig`@packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py；`MCPToolPack`@packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py；`MCPPackResourceParameters`@packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py；`MCPSSEToolPack`@packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py；`_DynMCPSSEPackResourceParameters`@packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 9. 多 Agent 协议与组织形态

并行子智能体是起步。2026 出现 Agent-to-Agent 协议、组织式角色（分析师、审计、可视化、数据工程师）。建议在现有 dispatch 上增加显式角色合同：输入产物、输出产物、禁止操作。共享黑板用结构化对象而不是纯自然语言。避免多个子 Agent 同时写同一会话文件。引入协调超时与取消传播，用户点停止必须杀掉子任务。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`AgentResourceType`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentResourceModel`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentSummary`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentStats`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentPackage`@packages/dbgpt-core/src/dbgpt/util/dbgpts/loader.py；`IndicatorAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/Indicator_assistant_agent.py；`ParallelToolAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`NativeToolCallAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`ToolCallingReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`DashboardAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/dashboard_assistant_agent.py；`ReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py；`DataAnalysisPlanningAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_agent.py；`WebSearchAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/web_assistant_agent.py；`ToolAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_assistant_agent.py；`SummaryAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/summary_assistant_agent.py；`SimpleAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/simple_assistant_agent.py；`CodeAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/code_assistant_agent.py；`Excel2TableAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py；`DataScientistAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_scientist_agent.py；`DataAnalysisAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_analysis_agent.py；`Agent`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentGenerateContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentReviewInfo`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentMessage`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`ConversableAgent`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；`_SafeDict`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；`AgentRunMode`@packages/dbgpt-core/src/dbgpt/agent/core/role.py；`Role`@packages/dbgpt-core/src/dbgpt/agent/core/role.py；`AgentManager`@packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py；`UserProxyAgent`@packages/dbgpt-core/src/dbgpt/agent/core/user_proxy_agent.py；`Team`@packages/dbgpt-core/src/dbgpt/agent/core/base_team.py；`ManagerAgent`@packages/dbgpt-core/src/dbgpt/agent/core/base_team.py；`PluginStorageType`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`ApiTagType`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`Status`@packages/dbgpt-core/src/dbgpt/agent/core/schema.py；`RegisterResource`@packages/dbgpt-core/src/dbgpt/agent/resource/manage.py；`ResourceManager`@packages/dbgpt-core/src/dbgpt/agent/resource/manage.py；`PackResourceParameters`@packages/dbgpt-core/src/dbgpt/agent/resource/pack.py；`ResourcePack`@packages/dbgpt-core/src/dbgpt/agent/resource/pack.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 10. Computer Use 与浏览器技能的克制采用

skills 中已有 agent-browser。2026 年 GUI Agent 很热，但数据分析主战场仍是库表与文件。建议浏览器仅用于「公开网页财报抓取」等白名单，必须在沙箱、必须有域允许名单、必须记录截图审计。不要让 GUI Agent 操作云控制台。内部 BI 优先用 API/SQL 而不是点鼠标。把 computer-use 当补充，不当默认规划器。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`ClickhouseParameters`@packages/dbgpt-ext/src/dbgpt_ext/datasource/rdbms/conn_clickhouse.py；`ClickhouseConnector`@packages/dbgpt-ext/src/dbgpt_ext/datasource/rdbms/conn_clickhouse.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_db_manager.py；`UserRequest`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_base_dao.py；`UserResponse`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_base_dao.py；`User`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_base_dao.py；`UserDao`@packages/dbgpt-core/src/dbgpt/storage/metadata/tests/test_base_dao.py；`ChatCompletionUserMessageParam`@packages/dbgpt-core/src/dbgpt/core/schema/types.py；`UserInputParsedOperator`@packages/dbgpt-core/src/dbgpt/core/awel/trigger/http_trigger.py；`ClickHouseDataSource`@packages/dbgpt-core/src/dbgpt/util/tests/configure/test_manager.py；`CustomUser`@packages/dbgpt-core/src/dbgpt/util/tests/configure/test_manager.py；`ExtCustomUser`@packages/dbgpt-core/src/dbgpt/util/tests/configure/test_manager.py；`UserProxyAgent`@packages/dbgpt-core/src/dbgpt/agent/core/user_proxy_agent.py；`UserLayer`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`ConnectRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`ConfigureRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`DisconnectRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`ExecuteRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`ManualOperationRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`StatusRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`FileRequest`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/service.py；`TaskObject`@packages/dbgpt-sandbox/src/dbgpt_sandbox/sandbox/user_layer/schemas.py；`UserRequest`@packages/dbgpt-serve/src/dbgpt_serve/utils/auth.py；`UserInputExecuteService`@packages/dbgpt-serve/src/dbgpt_serve/evaluate/service/benchmark/user_input_execute_service.py；`UserRecentApps`@packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py；`UserRecentAppsEntity`@packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py；`UserRecentAppsDao`@packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 11. 多模态分析

财报 PDF、仪表盘截图、扫描发票在 2026 已是常规输入。当前知识加载偏文本。应支持视觉模型看图表、把图中数字变成可验证表、再进入 SQL/pandas。模型接口已有 MediaContent，需要在 Agent 观察里一等支持图片产物。注意隐私与体积，大图要缩略与过期。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 12. 流式体验与可中断性

用户要的是可暂停、可编辑计划、可跳过某步。后端应把 ReAct 每步做成可恢复检查点。前端 vis 标签增加「重跑本步」「改参数」。SSE 断线续传。长时间 SQL 显示进度。2026 年体验差距往往不在模型智商而在可控性。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 13. 成本与延迟 SLO

ModelInferenceMetrics 已采集速度与 token。应上升到产品 SLO：P95 首 token、单问题成本上限、自动降级到小模型做路由与压缩。用小模型做工具选择，大模型做最终分析。缓存重复 schema 摘要。对 embedding 批处理。把 GPU 信息接到容量告警。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`DBGPTsLoader`@packages/dbgpt-core/src/dbgpt/util/dbgpts/loader.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 14. 检索与数据上下文的统一

结构化 schema 检索、文档 RAG、代码图、会话文件应统一成 Evidence Service，返回带类型的证据包。Agent 提示词只吃证据包。这能减少「模型没看见表」与「看见了错误表」。结合 2026 的 Graph+Vector 混合，在有外键与指标血缘时走图，否则走向量。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 15. Text-to-SQL 专项升级

这是产品命脉。建议：相关表检索、方言模板、执行反馈、SQL 等价评测、禁止未加时间过滤的全表扫描、自动 EXPLAIN 警告。把成功 SQL 沉淀为程序记忆。区分「生成给用户复制」与「自动执行」权限。对 Excel/DuckDB 与 MySQL 方言分提示词。引入查询成本估算。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 16. 人机共驾作为默认而非例外

连接器确认是人机共驾雏形。2026 年高风险动作（发邮件、写库、外网抓取、大规模导出）必须 HITL。低风险动作自动。界面要能批量批准同类 SQL。审核意见写回记忆。避免「要么全自动要么纯聊天」的两极。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 17. 可观测性从 span 到 Agent 轨迹分析

tracer 已有 OpenTelemetry。需要 Agent Trace UI：每步工具、参数、行数、token、重试、策略拦截。支持对比两次运行。把轨迹导出为评测样本。对生产问题能回答「它为什么 join 了这张表」。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`AgentResourceType`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentResourceModel`@packages/dbgpt-client/src/dbgpt_client/schema.py；`ObservabilitySpanEntity`@packages/dbgpt-core/src/dbgpt/observability/span_store.py；`AgentSummary`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentStats`@packages/dbgpt-core/src/dbgpt/observability/models.py；`SpanNode`@packages/dbgpt-core/src/dbgpt/observability/models.py；`OpenTelemetrySpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/opentelemetry.py；`SpanType`@packages/dbgpt-core/src/dbgpt/util/tracer/base.py；`SpanTypeRunName`@packages/dbgpt-core/src/dbgpt/util/tracer/base.py；`Span`@packages/dbgpt-core/src/dbgpt/util/tracer/base.py；`SpanStorageType`@packages/dbgpt-core/src/dbgpt/util/tracer/base.py；`SpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/base.py；`SqliteSpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/sqlite_span_storage.py；`MemorySpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/span_storage.py；`SpanStorageContainer`@packages/dbgpt-core/src/dbgpt/util/tracer/span_storage.py；`FileSpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/span_storage.py；`AgentPackage`@packages/dbgpt-core/src/dbgpt/util/dbgpts/loader.py；`MockSpanStorage`@packages/dbgpt-core/src/dbgpt/util/tracer/tests/test_base.py；`IndicatorAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/Indicator_assistant_agent.py；`ParallelToolAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`NativeToolCallAction`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`ToolCallingReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py；`DashboardAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/dashboard_assistant_agent.py；`ReActAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py；`DataAnalysisPlanningAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_agent.py；`WebSearchAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/web_assistant_agent.py；`ToolAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/tool_assistant_agent.py；`SummaryAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/summary_assistant_agent.py；`SimpleAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/simple_assistant_agent.py；`CodeAssistantAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/code_assistant_agent.py；`Excel2TableAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py；`DataScientistAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_scientist_agent.py；`DataAnalysisAgent`@packages/dbgpt-core/src/dbgpt/agent/expand/data_analysis_agent.py；`Agent`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentGenerateContext`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentReviewInfo`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`AgentMessage`@packages/dbgpt-core/src/dbgpt/agent/core/agent.py；`ConversableAgent`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；`_SafeDict`@packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 18. 身份、租户与企业集成

助手要进企业必须有用户身份传递到数据源（row-level security）、审计到人、密钥按租户隔离。现有 user_name/sys_code 字段要真正贯穿 DAO 过滤。对接 SSO。禁止用共享数据库账号冒充多用户安全。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 19. 技能工程与市场质量

技能是差异化。2026 年技能应有契约测试、样例数据、版本、签名、权限声明、评测分数。skill-creator 应生成测试骨架。导入 GitHub 要扫描危险代码。内置技能（csv、财报、沃尔玛销售）应成为质量标杆与回归集。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 20. AWEL 与 Agent 的合流

现在两条路：自由 Agent 与固定 DAG。2026 年最佳实践是「Agent 探索，DAG 固化」。提供从轨迹生成流程草稿、把关键步变成算子、把变量变成配置。兼容层必须保住视图元数据。让编排既有灵活性又有可运维性。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：`AWELTeamModel`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentResourceType`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentResourceModel`@packages/dbgpt-client/src/dbgpt_client/schema.py；`AgentSummary`@packages/dbgpt-core/src/dbgpt/observability/models.py；`AgentStats`@packages/dbgpt-core/src/dbgpt/observability/models.py；`_EMPTY_DATA_TYPE`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`TaskState`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`TaskOutput`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`TaskContext`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`InputContext`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`InputSource`@packages/dbgpt-core/src/dbgpt/core/awel/task/base.py；`SimpleTaskOutput`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`SimpleStreamTaskOutput`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`BaseInputSource`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`SimpleInputSource`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`SimpleCallDataInputSource`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`DefaultTaskContext`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`DefaultInputContext`@packages/dbgpt-core/src/dbgpt/core/awel/task/task_impl.py；`ResourceGroup`@packages/dbgpt-core/src/dbgpt/core/awel/resource/base.py；`CacheStorage`@packages/dbgpt-core/src/dbgpt/core/awel/util/cache_util.py；`MemoryCacheStorage`@packages/dbgpt-core/src/dbgpt/core/awel/util/cache_util.py；`FileCacheStorage`@packages/dbgpt-core/src/dbgpt/core/awel/util/cache_util.py；`RefreshOptionDependency`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`RefreshOptionRequest`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`OptionValue`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`BaseDynamicOptions`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`FunctionDynamicOptions`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`VariablesDynamicOptions`@packages/dbgpt-core/src/dbgpt/core/awel/util/parameter_util.py；`DependencyMixin`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGVar`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGLifecycle`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGNode`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`_DAGVariablesItem`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGVariables`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGContext`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAG`@packages/dbgpt-core/src/dbgpt/core/awel/dag/base.py；`DAGMetadata`@packages/dbgpt-core/src/dbgpt/core/awel/dag/dag_manager.py；`DAGManager`@packages/dbgpt-core/src/dbgpt/core/awel/dag/dag_manager.py；`DAGLoader`@packages/dbgpt-core/src/dbgpt/core/awel/dag/loader.py；`LocalFileDAGLoader`@packages/dbgpt-core/src/dbgpt/core/awel/dag/loader.py；

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 21. 本地小模型路由与隐私模式

产品基因是隐私。2026 年应提供：云端强模型分析、本地小模型做脱敏与路由、敏感列本地处理后再送云。明确数据不出域开关。对代理 provider 做请求审计。这比单纯「支持更多模型品牌」更符合 DB-GPT 承诺。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 22. 结构化输出与图表契约

最终报告应用 schema 约束：结论、指标表、图表 spec、SQL 附录、风险。前端 final-presentation 已有测试，应提升为后端强制 JSON schema，减少「看起来像报告其实缺数」。与 GPT-Vis 契约对齐版本。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 23. 长期运行与定时数字员工

scheduled_task 是入口。2026 年用户想要「每个工作日早上跑销售异常检测」。需要独立 worker、失败重试、去重、节假日日历、结果订阅。Agent 状态不能只活在一次 HTTP 请求里。引入 job 对象与心跳。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 24. 开放标准：A2A、OpenTelemetry、MCP、技能包格式

减少私有协议。工具走 MCP，追踪走 OTel，多 Agent 考虑兼容正在形成的 A2A。技能包提供明确 manifest。这样生态才能在 2026 年与 IDE Agent、浏览器 Agent 互通，而不是把用户锁在单一工作区。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 25. 研发流程与架构防腐

core 继续保持轻依赖。新趋势能力以插件进入 ext/serve。任何「为了 Demo 把供应商 SDK 写进 core」都应拒绝。增加架构测试：core 不得 import dbgpt_serve。文档与评测与代码同 PR。这是让去年的 Agent 活过 2026 年的组织保障。

现状对照：请结合 `packages/dbgpt-core/src/dbgpt/agent` 下的 ConversableAgent、PlannerAgent、ReActOutputParser、ContextManager、SkillManager、ResourcePack、MCP 工具以及 serve 层的 evaluate、scheduled_task、session_file 来理解缺口。这些模块已经提供了插桩点，优化应以扩展点方式进入，而不是复制一套新 Agent 运行时。否则会出现双栈提示词与双栈记忆，维护成本会迅速超过收益。实施顺序建议分为设计评审、接口冻结、影子流量、评测对比、默认切换、删除回退开关六个阶段。每个阶段都要有失败回滚：例如原生工具调用失败自动回退 ReAct，而不是整段对话报错。人员上需要 Agent 运行时、数据安全、前端可视化、评测工程四类角色协同。对数据分析场景，优先落地只读策略、SQL 评测、上下文分槽与轨迹可观测，这些会立刻减少事故；计算机使用、多 Agent 组织协议可以放在后面，避免分散火力。度量方面至少跟踪：工具解析成功率、越权拦截次数、SQL 执行成功率、用户改写次数、单问题成本、P95 时延、跨会话口径复用次数。没有度量的改造不算完成。文档方面要同步更新使用说明书中的模式选择与 FAQ，避免用户仍按纯 ReAct 文本去调试原生调用。兼容性方面必须保住 vis 标签与 AWEL 流程 JSON，这是已有客户资产。安全方面任何新自主性默认关闭，靠配置打开，并在审计日志中留下操作者身份。以上段落构成该方向的执行纲要，详细任务应拆入迭代 backlog，并与回归测试绑定。

相关源码锚点（自动关联，用于改造时定位）：

改造时建议为每个锚点补：契约测试、失败注入测试、权限测试与性能基线。若锚点不足，说明该能力还散落在 prompt 字符串里，应先把它代码化再谈优化。

## 路线图合并建议

第一阶段（正确性与安全）：策略引擎、只读 SQL、文件与沙箱加固、原生工具调用双栈、评测基线。第二阶段（成本与质量）：上下文分槽、证据服务、Text-to-SQL 专项、轨迹 UI、技能契约测试。第三阶段（长期运行）：记忆三层、检查点与可中断、定时数字员工、租户身份贯穿。第四阶段（生态）：MCP 治理、AWEL 固化、有限 computer-use、多模态、开放协议。每一阶段结束必须有可演示的反例被挡住（例如删除文件越权、无过滤全表扫描、提示词注入外带数据）。这样，去年开发的 Agent 才能在 2026 年的标准下仍然可信，而不是仅仅「还能跑」。

## 附录：现有 Agent 相关函数与类作为改造清单

该改造对象名为 `IndicatorAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/Indicator_assistant_agent.py` 第 19 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：IndicatorAssistantAgent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Init indicator AssistantAgent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ParallelToolAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py` 第 104 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：ReAct tool action that supports multiple steps per model turn.  When the model emits more than one actionable ReAct step in a single response (common with reasoning models), all steps are executed concurrently via :func:`run_tools_batch`. A single-step (or empty) response follows the legacy :class:`ReActAction` path exactly.方法设计如下。`_resolve_step_args(self,step)` 为同步内部方法。说明：Resolve a step's tool arguments without executing the tool.  Kept in sync with ``ReActAction._do_run`` argument resolution and reuses the inherited ``_fallback_parse_args`` / ``_extract_html_interpreter_args`` helpers.`_parser(self,kwargs)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action, batching when the model emitted multiple steps.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `NativeToolCallAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py` 第 222 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Execute provider-native ``tool_calls`` (function calling) as a ReAct turn.  When the model returned structured ``tool_calls`` (captured by ``ToolCallingReActAgent.thinking`` and passed via ``kwargs["tool_calls"]``), each call is executed concurrently via :func:`run_tools_batch`. Otherwise it falls back to the text ReAct parsing path of :class:`ParallelToolAction`.方法设计如下。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action from native tool_calls or text fallback.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolCallingReActAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py` 第 292 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：ReAct agent with hermes-style loop enhancements (opt-in by flag).  Stage 1 capabilities (protocol unchanged):   - ``enable_parallel_tool_execution``: batch-execute all ReAct steps from     a single model turn concurrently.   - ``enable_agent_error_classification``: tag fail reasons with a     classified category for retry / failover decisions.方法设计如下。`__init__(self)` 为同步对外方法。说明：Init the enhanced ReAct agent.`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。说明：Pass the parser, parallel flag and pending native tool calls to act().  NOTE: kept synchronous (matching the ``ConversableAgent.prepare_act_param`` base signature) — the base ``generate_reply`` calls it WITHOUT await, so an ``async def`` here returns a coroutine that then blows up ``**act_extent_par`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform actions, relaxing the single-action validation when enabled.`verify(self,message,sender,reviewer)` 为异步对外方法。说明：Verify results, tagging failures with a classified category.`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。说明：Think, using native function calling when enabled, else text.`_build_native_tools(self)` 为同步内部方法。说明：Return the OpenAI tools array for native function calling.`_thinking_native(self,messages,sender,prompt,stream_callback)` 为异步内部方法。说明：Native function-calling thinking loop (mirrors base thinking retries).在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DashboardAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/dashboard_assistant_agent.py` 第 12 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Dashboard Assistant Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new instance of DashboardAssistantAgent.`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReActAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/react_agent.py` 第 96 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。说明：Init indicator AssistantAgent.`_a_init_reply_message(self,received_message,rely_messages)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`preload_resource(self)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`_check_and_add_terminate(self)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`load_resource(self,question,is_retry_chat)` 为异步对外方法。说明：Load agent bind resource.`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。说明：Prepare the parameters for the act method.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform actions.`memory_fragment_class(self)` 为同步对外方法。说明：Return the memory fragment class.`read_memories(self,observation)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DataAnalysisPlanningAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/data_agent.py` 第 119 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Data Analysis Agent with autonomous planning capabilities.方法设计如下。`__init__(self)` 为同步对外方法。说明：Initialize Data Analysis Planning Agent.`_reset_planning_state(self)` 为同步内部方法。说明：Reset planning state for new tasks.`_a_init_reply_message(self,received_message,rely_messages)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`preload_resource(self)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`_check_and_add_terminate(self)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`load_resource(self,question,is_retry_chat)` 为异步对外方法。说明：Load agent bind resource.`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。说明：Prepare the parameters for the act method.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform actions with planning capabilities.`_update_planning_state(self,action_output)` 为异步内部方法。说明：Update planning state based on action output.`memory_fragment_class(self)` 为同步对外方法。说明：Return the memory fragment class.`read_memories(self,observation)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WebSearchAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/web_assistant_agent.py` 第 13 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Web Search Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new Web Search Agent. instance.`database(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`correctness_check(self,message)` 为异步对外方法。说明：Verify if the current execution results meet expectations.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/tool_assistant_agent.py` 第 12 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool Assistant Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new instance of ToolAssistantAgent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SummaryAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/summary_assistant_agent.py` 第 16 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Summary Assistant Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new SummaryAssistantAgent instance.`load_resource(self,question,is_retry_chat)` 为异步对外方法。说明：Load agent bind resource.`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`post_filters(self,resource_candidates_map)` 为同步对外方法。说明：Post filters for resource candidates.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SimpleAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/simple_assistant_agent.py` 第 16 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Simple Assistant Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new SummaryAssistantAgent instance.`load_resource(self,question,is_retry_chat)` 为异步对外方法。说明：Load agent bind resource.`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`post_filters(self,resource_candidates_map)` 为同步对外方法。说明：Post filters for resource candidates.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `CodeAssistantAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/code_assistant_agent.py` 第 44 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Code Assistant Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new CodeAssistantAgent instance.`correctness_check(self,message)` 为异步对外方法。说明：Verify whether the current execution results meet the target expectations.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Excel2TableAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py` 第 52 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Excel Scientist Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new DataScientistAgent instance.`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`database(self)` 为同步对外方法。说明：Get the database resource.`correctness_check(self,message)` 为异步对外方法。说明：Verify whether the current execution results meet the target expectations " "for multiple tables.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DataScientistAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/data_scientist_agent.py` 第 16 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Data Scientist Agent.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new DataScientistAgent instance.`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`database(self)` 为同步对外方法。说明：Get the database resource.`correctness_check(self,message)` 为异步对外方法。说明：Verify whether the current execution results meet the target expectations.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DataAnalysisAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/data_analysis_agent.py` 第 35 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`act(self,message,sender,reviewer,is_retry_chat)` 为异步对外方法。说明：Perform a data analysis action.`prepare_act_param(self,received_message,sender)` 为同步对外方法。说明：Prepare parameters for the act method.`memory_fragment_class(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent.py` 第 16 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent Interface.方法设计如下。`send(self,message,recipient,reviewer,request_reply,is_recovery,silent,is_retry_chat,last_speaker_name,rely_messages,historical_dialogues)` 为异步对外方法。说明：Send a message to recipient agent.  Args:     message(AgentMessage): the message to be sent.     recipient(Agent): the recipient agent.     reviewer(Agent): the reviewer agent.     request_reply(bool): whether to request a reply.     is_recovery(bool): whether the message is a recovery message.  Ret`receive(self,message,sender,reviewer,request_reply,silent,is_recovery,is_retry_chat,last_speaker_name,historical_dialogues,rely_messages)` 为异步对外方法。说明：Receive a message from another agent.  Args:     message(AgentMessage): the received message.     sender(Agent): the sender agent.     reviewer(Agent): the reviewer agent.     request_reply(bool): whether to request a reply.     silent(bool): whether to be silent.     is_recovery(bool): whether the`generate_reply(self,received_message,sender,reviewer,rely_messages,historical_dialogues,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Generate a reply based on the received messages.  Args:     received_message(AgentMessage): the received message.     sender: sender of an Agent instance.     reviewer: reviewer of an Agent instance.     rely_messages: a list of messages received.  Returns:     AgentMessage: the generated reply. If`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。说明：Think and reason about the current task goal.  Based on the requirements of the current agent, reason about the current task goal through LLM  Args:     messages(List[AgentMessage]): the messages to be reasoned     prompt(str): the prompt to be reasoned  Returns:     Tuple[Union[str, Dict, None], Op`review(self,message,censored)` 为异步对外方法。说明：Review the message based on the censored message.  Args:     message:     censored:  Returns:     bool: whether the message is censored     Any: the censored message`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Act based on the LLM inference results.  Parse the inference results for the current target and execute the inference results using the current agent's executor  Args:     message: the message to be executed     sender: sender of an Agent instance.     reviewer: reviewer of an Agent instance.     **`verify(self,message,sender,reviewer)` 为异步对外方法。说明：Verify whether the current execution results meet the target expectations.  Args:     message: the message to be verified     sender: sender of an Agent instance.     reviewer: reviewer of an Agent instance.     **kwargs:  Returns:     Tuple[bool, Optional[str]]: whether the verification is successf`name(self)` 为同步对外方法。说明：Return the name of the agent.`role(self)` 为同步对外方法。说明：Return the role of the agent.`desc(self)` 为同步对外方法。说明：Return the description of the agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentContext`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent.py` 第 197 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A class to represent the context of an Agent.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the AgentContext.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentGenerateContext`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent.py` 第 244 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A class to represent the input of a Agent.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the AgentGenerateContext.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentReviewInfo`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent.py` 第 279 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Message object for agent communication.方法设计如下。`copy(self)` 为同步对外方法。说明：Return a copy of the current AgentReviewInfo.`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the AgentMessage.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentMessage`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent.py` 第 296 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Message object for agent communication.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the AgentMessage.`to_llm_message(self)` 为同步对外方法。说明：Return a dictionary representation of the AgentMessage.`from_llm_message(cls,message)` 为同步对外方法。说明：Create an AgentMessage object from a dictionary.`from_messages(cls,messages)` 为同步对外方法。说明：Create a list of AgentMessage objects from a list of dictionaries.`copy(self)` 为同步对外方法。说明：Return a copy of the current AgentMessage.`get_dict_context(self)` 为同步对外方法。说明：Return the context as a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConversableAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py` 第 39 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：ConversableAgent is an agent that can communicate with other agents.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new agent.`init_context_management(self,config,model_name,on_status_event)` 为同步对外方法。说明：Initialize multi-layer context management.  Call this after the agent is fully configured (llm_client set, etc.).  Args:     config: Budget configuration. If None, derived from agent_context.     model_name: Model name for tokenizer selection.     on_status_event: Async callback invoked with context`_init_tool_result_storage(self,config)` 为同步内部方法。说明：Initialize the ToolResultStorage bound to this agent's conversation.  Resolves the storage directory from ``AgentContext.output_dir`` (or ``DBGPT_HOME/workspace``) and binds it to the current async context via ``set_current_storage`` so the standalone ``run_tool`` function can pick it up without a s`check_available(self)` 为同步对外方法。说明：Check if the agent is available.  Raises:     ValueError: If the agent is not available.`not_null_agent_context(self)` 为同步对外方法。说明：Get the agent context.  Returns:     AgentContext: The agent context.  Raises:     ValueError: If the agent context is not initialized.`not_null_llm_config(self)` 为同步对外方法。说明：Get the LLM config.`not_null_llm_client(self)` 为同步对外方法。说明：Get the LLM client.`blocking_func_to_async(self,func)` 为异步对外方法。说明：Run a potentially blocking function within an executor.`preload_resource(self)` 为异步对外方法。说明：Preload resources before agent initialization.`build(self,is_retry_chat)` 为异步对外方法。说明：Build the agent.`bind(self,target)` 为同步对外方法。说明：Bind the resources to the agent.`send(self,message,recipient,reviewer,request_reply,is_recovery,silent,is_retry_chat,last_speaker_name,rely_messages,historical_dialogues)` 为异步对外方法。说明：Send a message to recipient agent.`receive(self,message,sender,reviewer,request_reply,silent,is_recovery,is_retry_chat,last_speaker_name,historical_dialogues,rely_messages)` 为异步对外方法。说明：Receive a message from another agent.`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。说明：Prepare the parameters for the act method.`generate_reply(self,received_message,sender,reviewer,rely_messages,historical_dialogues,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Generate a reply based on the received messages.`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。说明：Think and reason about the current task goal.  Args:     messages(List[AgentMessage]): the messages to be reasoned     prompt(str): the prompt to be reasoned`review(self,message,censored)` 为异步对外方法。说明：Review the message based on the censored message.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform actions.`correctness_check(self,message)` 为异步对外方法。说明：Verify the correctness of the results.`verify(self,message,sender,reviewer)` 为异步对外方法。说明：Verify the current execution results.`initiate_chat(self,recipient,reviewer,message,request_reply,is_retry_chat,last_speaker_name,message_rounds,historical_dialogues,rely_messages)` 为异步对外方法。说明：Initiate a chat with another agent.  Args:     recipient (Agent): The recipient agent.     reviewer (Agent): The reviewer agent.     message (str): The message to send.`adjust_final_message(self,is_success,reply_message)` 为异步对外方法。说明：Adjust final message after agent reply.`_init_actions(self,actions)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_a_append_message(self,message,role,sender)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`_print_received_message(self,message,sender)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_a_process_received_message(self,message,sender)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`load_resource(self,question,is_retry_chat)` 为异步对外方法。说明：Load agent bind resource.`generate_resource_variables(self,resource_prompt)` 为异步对外方法。说明：Generate the resource variables.`_excluded_models(self,all_models,order_llms,excluded_models)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`convert_to_agent_message(self,gpts_messages,is_rery_chat)` 为同步对外方法。说明：Convert gptmessage to agent message.`_a_select_llm_model(self,excluded_models)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。说明：Create a new message from the received message.  Initialize a new message from the received message  Args:     received_message(AgentMessage): The received message  Returns:     AgentMessage: A new message`_a_init_reply_message(self,received_message,rely_messages)` 为异步内部方法。说明：Create a new message from the received message.  If return not None, the `_init_reply_message` method will not be called.`_convert_to_ai_message(self,gpts_messages,is_rery_chat)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`build_system_prompt(self,question,most_recent_memories,resource_vars,context,is_retry_chat)` 为异步对外方法。说明：Build system prompt.`_load_thinking_messages(self,received_message,sender,observation,rely_messages,historical_dialogues,context,is_retry_chat,current_retry_counter)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_SafeDict`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py` 第 1297 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__missing__(self,key)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentRunMode`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/role.py` 第 31 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent run mode.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Role`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/role.py` 第 40 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Role class for role-based conversation.方法设计如下。`build_prompt(self,question,is_system,most_recent_memories,resource_vars,is_retry_chat)` 为异步对外方法。说明：Return the prompt template for the role.  Returns:     str: The prompt template.`identity_check(self)` 为同步对外方法。说明：Check the identity of the role.`get_name(self)` 为同步对外方法。说明：Get the name of the role.`current_profile(self)` 为同步对外方法。说明：Return the current profile.`prompt_template(self,template_format,language,is_retry_chat)` 为同步对外方法。说明：Get agent prompt template.`name(self)` 为同步对外方法。说明：Return the name of the role.`role(self)` 为同步对外方法。说明：Return the role of the role.`goal(self)` 为同步对外方法。说明：Return the goal of the role.`retry_goal(self)` 为同步对外方法。说明：Return the retry goal of the role.`constraints(self)` 为同步对外方法。说明：Return the constraints of the role.`retry_constraints(self)` 为同步对外方法。说明：Return the retry constraints of the role.`desc(self)` 为同步对外方法。说明：Return the description of the role.`expand_prompt(self)` 为同步对外方法。说明：Return the expand prompt introduction of the role.`write_memory_template(self)` 为同步对外方法。说明：Return the current save memory template.`examples(self)` 为同步对外方法。说明：Return the current example template.`task_progress_summary(self)` 为同步对外方法。说明：Return a human-readable task progress summary.  Lists every action the agent has taken so far, marking the last entry as the most recent step.  The summary is injected into every LLM call so the model never forgets what has already been done and what still needs to be done to complete the original t`_render_template(self,template)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`memory_importance_scorer(self)` 为同步对外方法。说明：Create the memory importance scorer.  The memory importance scorer is used to score the importance of a memory fragment.`memory_insight_extractor(self)` 为同步对外方法。说明：Create the memory insight extractor.  The memory insight extractor is used to extract a high-level insight from a memory fragment.`memory_fragment_class(self)` 为同步对外方法。说明：Return the memory fragment class.`read_memories(self,question)` 为异步对外方法。说明：Read the memories from the memory.`write_memories(self,question,ai_message,action_output,check_pass,check_fail_reason,current_retry_counter)` 为异步对外方法。说明：Write the memories to the memory.  We suggest you to override this method to save the conversation to memory according to your needs.  Args:     question(str): The question received.     ai_message(str): The AI message, LLM output.     action_output(ActionOutput): The action output.     check_pass(b`_write_op_snapshot(self,step,action,action_input,observation,thought,phase,action_intention,action_reason)` 为同步内部方法。说明：Write a full operation snapshot to disk and return the file path.  The snapshot preserves the complete action_input and observation so that Layer 1 / Layer 2 compaction never loses precise values (file paths, computed results, variable names, etc.).  The agent can later recover the detail by reading`recovering_memory(self,action_outputs)` 为异步对外方法。说明：Recover the memory from the action outputs.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 47 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Manages the registration and retrieval of agents.方法设计如下。`__init__(self,system_app)` 为同步对外方法。说明：Create a new AgentManager.`init_app(self,system_app)` 为同步对外方法。说明：Initialize the AgentManager.`after_start(self)` 为同步对外方法。说明：Register all agents.`register_agent(self,cls,ignore_duplicate)` 为同步对外方法。说明：Register an agent.`get_by_name(self,name)` 为同步对外方法。说明：Return an agent by name.  Args:     name (str): The name of the agent to retrieve.  Returns:     Type[ConversableAgent]: The agent with the given name.  Raises:     ValueError: If the agent with the given name is not registered.`get_describe_by_name(self,name)` 为同步对外方法。说明：Return the description of an agent by name.`all_agents(self)` 为同步对外方法。说明：Return a dictionary of all registered agents and their descriptions.`list_agents(self)` 为同步对外方法。说明：Return a list of all registered agents and their descriptions.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `UserProxyAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/user_proxy_agent.py` 第 10 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A proxy agent for the user.  That can execute code and provide feedback to the other agents.方法设计如下。`have_ask_user(self)` 为同步对外方法。说明：If have ask user info in message.`receive(self,message,sender,reviewer,request_reply,silent,is_recovery,is_retry_chat,last_speaker_name,historical_dialogues,rely_messages)` 为异步对外方法。说明：Receive a message from another agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Team`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_team.py` 第 68 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Team class for managing a group of agents in a team chat.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new Team instance.`hire(self,agents)` 为同步对外方法。说明：Hire roles to cooperate.`agent_names(self)` 为同步对外方法。说明：Return the names of the agents in the group chat.`agent_by_name(self,name)` 为同步对外方法。说明：Return the agent with a given name.`select_speaker(self,last_speaker,selector,now_goal_context,pre_allocated)` 为异步对外方法。说明：Select the next speaker in the group chat.`reset(self)` 为同步对外方法。说明：Reset the group chat.`append(self,message)` 为同步对外方法。说明：Append a message to the group chat.  We cast the content to str here so that it can be managed by text-based model.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ManagerAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_team.py` 第 119 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Manager Agent class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new ManagerAgent instance.`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。说明：Think and reason about the current task goal.`_load_thinking_messages(self,received_message,sender,observation,rely_messages,historical_dialogues,context,is_retry_chat,current_retry_counter)` 为异步内部方法。说明：Load messages for thinking.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginStorageType`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/schema.py` 第 6 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Plugin storage type.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ApiTagType`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/schema.py` 第 13 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：API tag type.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Status`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/schema.py` 第 20 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Status of a task.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RegisterResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/manage.py` 第 19 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Register resource model.方法设计如下。`key(self)` 为同步对外方法。说明：Return the key.`type_unique_key(self)` 为同步对外方法。说明：Return the key.`pre_fill(cls,values)` 为同步对外方法。说明：Pre-fill the model.`get_parameter_class(self)` 为同步对外方法。说明：Return the parameter description.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ResourceManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/manage.py` 第 76 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource manager.  To manage the resources.方法设计如下。`__init__(self,system_app)` 为同步对外方法。说明：Create a new AgentManager.`init_app(self,system_app)` 为同步对外方法。说明：Initialize the AgentManager.`after_start(self)` 为同步对外方法。说明：Register all resources.`register_resource(self,resource_cls,resource_instance,resource_type,resource_type_alias,ignore_duplicate)` 为同步对外方法。说明：Register a resource.`get_supported_resources_type(self)` 为同步对外方法。说明：Get supported resources types.`get_supported_resources(self,version,type)` 为同步对外方法。说明：Return the resources.`build_resource_by_type(self,type_unique_key,agent_resource,return_resource)` 为同步对外方法。说明：Return the resource by type.`build_resource(self,agent_resources)` 为同步对外方法。说明：Build a resource.  If there is only one resource, return the resource instance, otherwise return a ResourcePack.  Args:     agent_resources: The agent resources.     version: The resource version.  Returns:     Optional[Resource]: The resource instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PackResourceParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/pack.py` 第 15 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource pack parameters class.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ResourcePack`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/pack.py` 第 21 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource pack class.方法设计如下。`__init__(self,resources,name,prompt_separator)` 为同步对外方法。说明：Initialize the resource pack.`type(cls)` 为同步对外方法。说明：Return the resource type.`name(self)` 为同步对外方法。说明：Return the resource name.`_get_resource_by_name(self,name)` 为同步内部方法。说明：Get the resource by name.`preload_resource(self)` 为异步对外方法。说明：Preload the resource.`get_prompt(self)` 为异步对外方法。说明：Get the prompt.`append(self,resource,overwrite)` 为同步对外方法。说明：Append a resource to the pack.`execute(self)` 为同步对外方法。说明：Execute the resource.`async_execute(self)` 为异步对外方法。说明：Execute the resource asynchronously.`is_pack(self)` 为同步对外方法。说明：Return whether the resource is a pack.`sub_resources(self)` 为同步对外方法。说明：Return the resources.`apply(self,apply_func,apply_pack_func)` 为同步对外方法。说明：Apply the function to the resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppInfo`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/app.py` 第 15 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/app.py` 第 21 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AppResource resource class.方法设计如下。`__init__(self,name)` 为同步对外方法。说明：Initialize AppResource resource.`app_desc(self)` 为同步对外方法。说明：Return the app description.`app_name(self)` 为同步对外方法。说明：Return the app name.`_start_app(self,user_input,sender,conv_uid)` 为异步内部方法。说明：start the app`type(cls)` 为同步对外方法。说明：Return the resource type.`name(self)` 为同步对外方法。说明：Return the resource name.`_get_app_list(cls)` 为同步内部方法。说明：Get the current app list`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_prompt(self)` 为异步对外方法。说明：Get the prompt.`is_async(self)` 为同步对外方法。说明：Return whether the tool is asynchronous.`execute(self)` 为同步对外方法。说明：Execute the resource.`async_execute(self)` 为异步对外方法。说明：Execute the tool asynchronously.  Args:     *args: The positional arguments.     resource_name (str, optional): The tool name to be executed(not used for     specific tool).     **kwargs: The keyword arguments.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_DynAppResourceParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/app.py` 第 64 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Application resource class.方法设计如下。`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.`from_dict(cls,data,ignore_extra_fields)` 为同步对外方法。说明：Create a new instance from a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DBParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/database.py` 第 25 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：DB parameters class.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DBResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/database.py` 第 31 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Database resource class.方法设计如下。`__init__(self,name,db_type,db_name,dialect,executor,prompt_template)` 为同步对外方法。说明：Initialize the DB resource.`type(cls)` 为同步对外方法。说明：Return the resource type.`name(self)` 为同步对外方法。说明：Return the resource name.`db_type(self)` 为同步对外方法。说明：Return the resource name.`dialect(self)` 为同步对外方法。说明：Return the resource name.`get_prompt(self)` 为异步对外方法。说明：Get the prompt.`execute(self)` 为同步对外方法。说明：Execute the resource.`async_execute(self)` 为异步对外方法。说明：Execute the resource asynchronously.`is_async(self)` 为同步对外方法。说明：Return whether the resource is asynchronous.`get_schema_link(self,db,question)` 为同步对外方法。说明：Return the schema link of the database.`query_to_df(self,sql,db)` 为异步对外方法。说明：Return the query result as a DataFrame.`query(self,sql,db)` 为异步对外方法。说明：Return the query result.`_sync_query(self,db,sql)` 为同步内部方法。说明：Return the query result.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RDBMSConnectorResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/database.py` 第 144 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Connector resource class.方法设计如下。`__init__(self,name,connector,db_name,db_type,dialect,executor)` 为同步对外方法。说明：Initialize the connector resource.`connector(self)` 为同步对外方法。说明：Return the connector.`get_schema_link(self,db,question)` 为同步对外方法。说明：Return the schema link of the database.`_sync_query(self,db,sql)` 为同步内部方法。说明：Return the query result.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SQLiteDBResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/database.py` 第 197 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：SQLite database resource class.方法设计如下。`__init__(self,name,db_name,executor)` 为同步对外方法。说明：Initialize the SQLite database resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/skill_resource.py` 第 15 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`type(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`__init__(self,name,skill,skill_name,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`name(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_prompt(self)` 为异步对外方法。说明：Return the skill's prompt template as resource prompt.`get_resources(self)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`async_execute(self)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ResourceType`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/base.py` 第 40 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource type enumeration.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ResourceParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/base.py` 第 62 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource parameters class.  It defines the parameters for building a resource.方法设计如下。`_resource_version(cls)` 为同步内部方法。说明：Return the resource version.`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Resource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/base.py` 第 90 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resource for the agent.方法设计如下。`type(cls)` 为同步对外方法。说明：Return the resource type.`type_alias(cls)` 为同步对外方法。说明：Return the resource type alias.`name(self)` 为同步对外方法。说明：Return the resource name.`resource_parameters_class(cls)` 为同步对外方法。说明：Return the parameters class.`prefer_resource_parameters_class(self)` 为同步对外方法。说明：Return the parameters class.  You can override this method to return a different parameters class. It will be used to initialize the resource with parameters.`initialize_with_parameters(self,resource_parameters)` 为同步对外方法。说明：Initialize the resource with parameters.`preload_resource(self)` 为异步对外方法。说明：Preload the resource.`from_resource(cls,resource,expected_type)` 为同步对外方法。说明：Create a resource from another resource.  Another resource can be a pack or a single resource, if it is a pack, it will return all resources which type is the same as the current resource.  Args:     resource(Resource): The resource.     expected_type(ResourceType): The expected resource type. Retur`get_resources_info(self)` 为异步对外方法。说明：Get prompts for multiple resources at the same time.  Args:     lang(str): The language.     prompt_type(str): The prompt type.     question(str): The question.     resource_name(str): The resource name, just for the pack, it will be used         to select specific resource in the pack.`get_prompt(self)` 为异步对外方法。说明：Get the prompt.  Args:     lang(str): The language.     prompt_type(str): The prompt type.     question(str): The question.     resource_name(str): The resource name, just for the pack, it will be used         to select specific resource in the pack.`get_resources(self,lang,prompt_type,question,resource_name)` 为异步对外方法。说明：Get the resources.`parse_execute_args(self,resource_name,input_str)` 为同步对外方法。说明：Try to parse the execute arguments.  If return None, the execute parameters are pass by raw arguments generated by Agent.  What case you should implement this method: 1. If the resource has a specific input format, you should implement this 2. You want parse and raise the raw error information, you`execute(self)` 为同步对外方法。说明：Execute the resource.`async_execute(self)` 为异步对外方法。说明：Execute the resource asynchronously.`is_async(self)` 为同步对外方法。说明：Return whether the resource is asynchronous.`is_pack(self)` 为同步对外方法。说明：Return whether the resource is a pack.`sub_resources(self)` 为同步对外方法。说明：Return the resources.`get_resource_by_type(self,resource_type)` 为同步对外方法。说明：Get resources by type.  If the resource is a pack, it will search the sub-resources. Otherwise, it will return itself if the type matches.  Args:     resource_type(ResourceType): The resource type.  Returns:     List[Resource]: The resources.`apply(self,apply_func,apply_pack_func)` 为同步对外方法。说明：Apply the function to the resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/base.py` 第 290 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent resource class.方法设计如下。`resource_prompt_template(self)` 为同步对外方法。说明：Get the resource prompt template.`parse_value(cls,value)` 为同步对外方法。说明：Parse value.`from_dict(d)` 为同步对外方法。说明：Create an AgentResource object from a dictionary.`from_json_list_str(d)` 为同步对外方法。说明：Create a list of AgentResource objects from a json string.`to_dict(self)` 为同步对外方法。说明：Convert the AgentResource object to a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RetrieverResourceParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/knowledge.py` 第 24 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Retriever resource parameters.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RetrieverResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/knowledge.py` 第 30 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Retriever resource.  Retrieve knowledge chunks from a retriever.方法设计如下。`__init__(self,name,retriever)` 为同步对外方法。说明：Create a new RetrieverResource.`name(self)` 为同步对外方法。说明：Return the resource name.`retriever_name(self)` 为同步对外方法。说明：Return the resource name.`retriever_desc(self)` 为同步对外方法。说明：Return the retriever desc.`retriever(self)` 为同步对外方法。说明：Return the retriever.`type(cls)` 为同步对外方法。说明：Return the resource type.`resource_parameters_class(cls)` 为同步对外方法。说明：Return the resource parameters class.`get_prompt(self)` 为异步对外方法。说明：Get the prompt for the resource.`get_resources(self,lang,prompt_type,question,resource_name)` 为异步对外方法。说明：Get the chunks for the resource.`_get_references(self,docs)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`async_execute(self)` 为异步对外方法。说明：Execute the resource asynchronously.`retrieve(self,query,filters,score)` 为异步对外方法。说明：Retrieve knowledge chunks.  Args:     query (str): query text.     filters: (Optional[MetadataFilters]) metadata filters.     score: (float) similarity score.  Returns:     List[Chunk]: list of chunks在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ErrorCategory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 21 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Recovery-relevant buckets for agent errors.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginStatus`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/api_call.py` 第 18 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A class representing the status of a plugin.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ApiCall`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/api_call.py` 第 34 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A class representing an API call.方法设计如下。`__init__(self,plugin_generator,display_registry,backend_rendering)` 为同步对外方法。说明：Create a new ApiCall object.`_is_need_wait_plugin_call(self,api_call_context)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`check_last_plugin_call_ready(self,all_context)` 为同步对外方法。说明：Check if the last plugin call is ready.`_deal_error_md_tags(self,all_context,api_context,include_end)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_remove_sql_comments(self,sql)` 为同步内部方法。说明：Remove SQL comments from the given SQL string.`_format_api_context(self,raw_api_context)` 为同步内部方法。说明：Format the API context.`api_view_context(self,all_context,display_mode)` 为同步对外方法。说明：Return the view content.`update_from_context(self,all_context)` 为同步对外方法。说明：Modify the plugin status map based on the context.`_to_view_param_str(self,api_status)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`to_view_text(self,api_status)` 为同步对外方法。说明：Return the view content.`to_view_antv_vis(self,api_status)` 为同步对外方法。说明：Return the vis content.`_to_antv_vis_param(self,api_status)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`run_display_sql(self,llm_text,sql_run_func)` 为同步对外方法。说明：Run the API calls for displaying SQL data.`display_sql_llmvis(self,llm_text,sql_run_func)` 为同步对外方法。说明：Render charts using the Antv standard protocol.  Args:     llm_text: LLM response text     sql_run_func: sql run  function  Returns:    ChartView protocol text`display_only_sql_vis(self,chart,sql_2_df_func)` 为同步对外方法。说明：Display the chart using the vis standard protocol.`display_dashboard_vis(self,charts,sql_2_df_func,title)` 为同步对外方法。说明：Display the dashboard using the vis standard protocol.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReActStep`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py` 第 33 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Dataclass representing a single step in the ReAct pattern.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReActOutputParser`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/react_parser.py` 第 48 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Parser for ReAct format model outputs with configurable prefixes. This parser extracts structured information from language model outputs that follow the ReAct pattern: Thought -> Phase -> Action -> Action Input -> Observation.方法设计如下。`__init__(self,thought_prefix,phase_prefix,action_intention_prefix,action_reason_prefix,action_prefix,action_input_prefix,observation_prefix,terminate_action)` 为同步对外方法。说明：Initialize the ReAct output parser with configurable prefixes.  Args:     thought_prefix: Prefix string that indicates the start of a thought.     phase_prefix: Prefix string that indicates the start of a phase.     action_intention_prefix: Prefix string that indicates the start of         an action`_prefix_line_pattern(self,escaped_prefix)` 为同步内部方法。说明：Build a regex for a ReAct prefix at the start of a logical line.`_markdown_fence_spans(self,text)` 为同步内部方法。说明：Return markdown fenced-code spans so ReAct labels inside are ignored.`_is_in_spans(pos,spans)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_find_prefix_matches(self,text,escaped_prefix)` 为同步内部方法。说明：Find line-start ReAct prefix matches outside markdown code fences.`_mask_prefixes_in_fences(self,text)` 为同步内部方法。说明：Mask ReAct labels inside code fences while preserving string offsets.`_strip_vis_thinking_blocks(self,text)` 为同步内部方法。说明：Remove vis-thinking wrappers produced by reasoning model output.`_strip_markdown_code_fence(self,text)` 为同步内部方法。说明：Remove a markdown fence that wraps the whole ReAct response.`_translate_native_tool_calls(self,text)` 为同步内部方法。说明：Translate leaked native tool-call blocks into ReAct text lines.  Runs BEFORE the special-token stripping so the tool name and JSON arguments survive; leftover section wrapper markers are removed by the token pass in :meth:`_normalize_react_text`. Free text around the call is preserved — the ``Action`_normalize_react_text(self,text)` 为同步内部方法。说明：Normalize common wrappers before ReAct parsing.`parse(self,text)` 为同步对外方法。说明：Parse the ReAct format output text into structured steps.  Args:     text: The text to parse, containing ReAct formatted content.  Returns:     List of ReActStep dataclasses, each containing thought, action,         action_input, and observation.`parse_current_step(self,text)` 为同步对外方法。说明：Parse the single step that should be executed in the current round.  Some reasoning models incorrectly emit a whole ReAct trajectory in one response. DB-GPT executes one action per round, so callers that are about to run tools should use only the first actionable step while preserving ``parse()`` fo`_parse_step(self,step_text)` 为同步内部方法。说明：Parse a single step of the ReAct format.  Args:     step_text: Text containing a single thought-action-input-observation         sequence.  Returns:     ReActStep dataclass with thought, action, action_input, and observation,         or None if parsing fails.`get_final_output(self,steps)` 为同步对外方法。说明：Get the final output from a terminate action if it exists.  Args:     steps: List of parsed steps.  Returns:     The final output string or None if no terminate action is found.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillsAgentConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/agent.py` 第 19 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Configuration for SkillsAgent.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillsAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/agent.py` 第 38 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent with progressive disclosure skills support.  This agent integrates SkillsMiddleware to provide: - Automatic skill loading from multiple sources - Progressive disclosure (metadata first, content on demand) - Automatic skill matching based on user input - Skill injection into system prompt  Example:     ```python     from dbgpt.agent.core.profile.base import ProfileConfig     from dbgpt.agent.skill.agent import SkillsAgent, SkillsAgentConfig      profile = ProfileConfig(name="assistant", rol方法设计如下。`__init__(self,skills_config)` 为同步对外方法。说明：Initialize a SkillsAgent.  Args:     skills_config: Configuration for skills middleware.     **kwargs: Additional arguments for ConversableAgent.`skills_middleware(self)` 为同步对外方法。说明：Return the skills middleware.`active_skills(self)` 为同步对外方法。说明：Return currently active skills.`skills_config(self)` 为同步对外方法。说明：Return skills configuration.`load_skills(self)` 为同步对外方法。说明：Load skills from all configured sources.  Returns:     Dictionary of loaded skills keyed by name.`get_skill(self,name)` 为同步对外方法。说明：Get a skill by name.  Args:     name: Skill name.  Returns:     LoadedSkill or None.`list_skills(self)` 为同步对外方法。说明：List all available skill names.  Returns:     List of skill names.`match_skills(self,user_input)` 为同步对外方法。说明：Find skills that match user input.  Args:     user_input: User input string.  Returns:     List of matching skills.`set_skill(self,skill_name)` 为同步对外方法。说明：Manually activate a specific skill.  Args:     skill_name: Name of skill to activate.  Raises:     ValueError: If skill not found.`clear_active_skills(self)` 为同步对外方法。说明：Clear all active skills and revert to base prompt.`_update_prompt_with_skills(self)` 为同步内部方法。说明：Update agent prompt with active skills.`build_system_prompt(self,question,most_recent_memories,resource_vars,context,is_retry_chat)` 为异步对外方法。说明：Build system prompt with skills integration.  Args:     question: Current question.     most_recent_memories: Most recent memories.     resource_vars: Resource variables.     context: Additional context.     is_retry_chat: Whether this is a retry chat.  Returns:     System prompt string.`_a_init_reply_message(self,received_message,rely_messages)` 为异步内部方法。说明：Initialize reply message with skill detection.  Args:     received_message: The received message.     rely_messages: List of relied messages.  Returns:     AgentMessage or None.`get_skills_summary(self)` 为同步对外方法。说明：Get a summary of available and active skills.  Returns:     Formatted summary string.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillMetadata`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 25 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Metadata for a skill.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Convert to dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LoadedSkill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 53 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A loaded skill with metadata and optional content.方法设计如下。`__init__(self,metadata,content)` 为同步对外方法。说明：Initialize loaded skill.  Args:     metadata: Skill metadata.     content: Full skill content (markdown instructions).`metadata(self)` 为同步对外方法。说明：Return skill metadata.`content(self)` 为同步对外方法。说明：Return skill content.`get_prompt_template(self)` 为同步对外方法。说明：Get prompt template with skill instructions.  Returns:     PromptTemplate with the skill's full instructions.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillsMiddleware`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 301 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Middleware for loading and exposing agent skills to the system prompt.  Loads skills from sources and injects them into the system prompt using progressive disclosure (metadata first, full content on demand).  Example:     ```python     middleware = SkillsMiddleware(         sources=[             "/path/to/skills/user/",             "/path/to/skills/project/",         ],     )     ```方法设计如下。`__init__(self,sources)` 为同步对外方法。说明：Initialize the skills middleware.  Args:     sources: List of skill source paths.`load_skills(self)` 为同步对外方法。说明：Load skills from all configured sources.  Skills are loaded in source order with later sources overriding earlier ones if they contain skills with the same name.  Returns:     Dictionary of loaded skills keyed by name.`get_skill(self,name)` 为同步对外方法。说明：Get a skill by name.  Args:     name: Skill name.  Returns:     LoadedSkill or None.`list_skills(self)` 为同步对外方法。说明：List all loaded skills.  Returns:     List of skill metadata.`format_skills_locations(self)` 为同步对外方法。说明：Format skills locations for display in system prompt.  Returns:     Formatted string of skills locations.`format_skills_list(self)` 为同步对外方法。说明：Format skills metadata for display in system prompt.  Returns:     Formatted string of skills list.`create_skills_prompt_section(self)` 为同步对外方法。说明：Create the skills section for the system prompt.  Returns:     Formatted skills system prompt section.`get_skills_by_type(self,skill_type)` 为同步对外方法。说明：Get skills by type.  Args:     skill_type: Skill type to filter by.  Returns:     List of matching skills.`match_skills(self,user_input)` 为同步对外方法。说明：Find skills that match user input based on description.  Args:     user_input: User input string.  Returns:     List of matching skills.`_extract_keywords(self,description)` 为同步内部方法。说明：Extract potential trigger keywords from description.  Args:     description: Skill description.  Returns:     List of keywords.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/parameters.py` 第 12 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skill resource parameters class.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillsMiddlewareV2`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware_v2.py` 第 17 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skills middleware using AgentMiddleware pattern.  Integrates with the middleware system to inject skills into the agent lifecycle, similar to deepagents' SkillsMiddleware.  Example:     ```python     from dbgpt.agent.middleware.base import MiddlewareManager     from dbgpt.agent.skill.middleware_v2 import SkillsMiddlewareV2      middleware = SkillsMiddlewareV2(         sources=["/path/to/skills/user", "/path/to/skills/project"]     )     manager = MiddlewareManager()     manager.register(middlewa方法设计如下。`__init__(self,sources,auto_load,auto_match,inject_to_system_prompt)` 为同步对外方法。说明：Initialize the skills middleware.  Args:     sources: List of skill source paths.     auto_load: Automatically load skills on initialization.     auto_match: Automatically match skills based on user input.     inject_to_system_prompt: Inject skills into system prompt.`load_skills(self)` 为同步对外方法。说明：Load skills from all configured sources.  Returns:     Dictionary of loaded skills keyed by name.`get_skill(self,name)` 为同步对外方法。说明：Get a skill by name.  Args:     name: Skill name.  Returns:     LoadedSkill or None.`list_skills(self)` 为同步对外方法。说明：List all loaded skills.  Returns:     List of skill metadata.`match_skills(self,user_input)` 为同步对外方法。说明：Find skills that match user input.  Args:     user_input: User input string.  Returns:     List of matching skills.`after_init(self,agent)` 为异步对外方法。说明：Called after agent initialization.  Loads skills and stores them in middleware state.  Args:     agent: The agent instance.     **kwargs: Additional arguments.  Returns:     State with skills metadata.`before_generate_reply(self,agent,context)` 为异步对外方法。说明：Called before generating a reply.  Matches skills based on user input if auto_match is enabled.  Args:     agent: The agent instance.     context: The generate context.     **kwargs: Additional arguments.  Returns:     State with matched skills.`modify_system_prompt(self,agent,original_prompt,context)` 为异步对外方法。说明：Modify the system prompt with skills information.  Args:     agent: The agent instance.     original_prompt: The original system prompt.     context: Additional context information.  Returns:     Modified system prompt with skills section.`get_skills_summary(self)` 为同步对外方法。说明：Get a summary of available skills.  Returns:     Formatted summary string.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RegisterSkill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/manage.py` 第 15 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Register skill model.方法设计如下。`__init__(self,name,skill_cls,skill_instance,metadata,is_class)` 为同步对外方法。说明：Initialize register skill.  Args:     name: Skill name.     skill_cls: Skill class.     skill_instance: Skill instance.     metadata: Skill metadata.     is_class: Whether it's a class or instance.`key(self)` 为同步对外方法。说明：Return the unique key.`type_key(self)` 为同步对外方法。说明：Return the type key.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/manage.py` 第 57 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skill manager.  To manage the skills.方法设计如下。`__init__(self,system_app)` 为同步对外方法。说明：Create a new SkillManager.`init_app(self,system_app)` 为同步对外方法。说明：Initialize the SkillManager.`after_start(self)` 为同步对外方法。说明：Register all skills after start.`register_skill(self,skill_cls,skill_instance,name,metadata,ignore_duplicate)` 为同步对外方法。说明：Register a skill.  Args:     skill_cls: Skill class.     skill_instance: Skill instance.     name: Skill name.     metadata: Skill metadata.     ignore_duplicate: Whether to ignore duplicate registration.`get_skill(self,name,skill_type,version)` 为同步对外方法。说明：Get a skill by name or type.  Args:     name: Skill name.     skill_type: Skill type.     version: Skill version.  Returns:     The skill instance or None.`get_skills_by_type(self,skill_type)` 为同步对外方法。说明：Get all skills by type.  Args:     skill_type: Skill type.  Returns:     List of skill instances.`list_skills(self)` 为同步对外方法。说明：List all registered skills.  Returns:     List of skill metadata dictionaries.`_instantiate_skill(self,register_skill)` 为同步内部方法。说明：Instantiate a skill from register skill.  Args:     register_skill: RegisterSkill instance.  Returns:     Skill instance.`build_skill_from_parameters(self,parameters)` 为同步对外方法。说明：Build a skill from parameters.  Args:     parameters: Skill parameters.  Returns:     Skill instance.`retrieve_skills(self)` 为同步对外方法。说明：Retrieve all skills metadata.  Returns:     List[Dict[str, Any]]: List of skill metadata including name,     description and path.`get_skill_content(self,skill_name)` 为同步对外方法。说明：Get the content (SKILL.md) of a skill.  Args:     skill_name: The name of the skill.  Returns:     str: The content of the SKILL.md file.`get_skill_scripts(self,skill_name)` 为同步对外方法。说明：Get scripts defined in a skill's configuration.  Scripts are defined in the skill's config with the following format: ```yaml scripts:   - name: "script_name"     description: "Script description"     language: "python"     code: "..." ```  Args:     skill_name: The name of the skill.  Returns:`execute_script(self,skill_name,script_name,args)` 为异步对外方法。说明：Execute a script defined in a skill.  Args:     skill_name: The name of the skill containing the script.     script_name: The name of the script to execute.     args: Arguments to pass to the script.  Returns:     JSON string with execution results.`get_skill_script_file(self,skill_name,script_file_name)` 为同步对外方法。说明：Read a script file from skill's scripts directory.  Args:     skill_name: The name of the skill.     script_file_name: The script file name (e.g., 'calculate_ratios.py').  Returns:     The script content or None if not found.`get_skill_references(self,skill_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_skill_reference_file(self,skill_name,ref_file_name)` 为同步对外方法。说明：Read a specific reference file from skill's references directory.  Args:     skill_name: The name of the skill.     ref_file_name: The reference file name (e.g., 'overview.md').  Returns:     The file content or None if not found.`_get_skill_path(self,skill_name)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_personal_skill_script_execution_disabled()` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_is_personal_skill_path(skill_path)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_should_reject_personal_skill_execution(self,skill_path)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_personal_skill_execution_denied_result(skill_name)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`get_skill_resource(self,skill_name,resource_path,args)` 为异步对外方法。说明：Get a resource from a skill by path.  This unified method handles both file reading and script execution: - If path starts with "scripts/" and ends with .py/.sh, execute the script - If path is an image file, return error (model doesn't support images) - Otherwise, read and return the file content`_adapt_args_for_script(code,args)` 为同步内部方法。说明：Adapt args to match the script's main function signature.  The LLM may pass args in various shapes: - {"data": {"revenue": 100}} — wrapping in the parameter name - {"revenue": 100} — flat dict matching the expected data format  This method uses AST to find the main function's parameters and adapts t`_execute_script_from_path(self,script_path,args)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`execute_skill_script_file(self,skill_name,script_file_name,args,output_dir)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillType`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/base.py` 第 11 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skill type enumeration.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillMetadata`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/base.py` 第 23 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Metadata for a skill.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Convert to dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillBase`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/base.py` 第 47 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base class for a skill.方法设计如下。`type(cls)` 为同步对外方法。说明：Return the skill type.`metadata(self)` 为同步对外方法。说明：Return the skill metadata.`prompt_template(self)` 为同步对外方法。说明：Return the prompt template.`required_tools(self)` 为同步对外方法。说明：Return the list of required tool names.`required_knowledge(self)` 为同步对外方法。说明：Return the list of required knowledge names.`actions(self)` 为同步对外方法。说明：Return the list of actions.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Skill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/base.py` 第 87 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Concrete implementation of a skill.方法设计如下。`__init__(self,metadata,prompt_template,required_tools,required_knowledge,actions,config)` 为同步对外方法。说明：Initialize a skill.  Args:     metadata: The skill metadata.     prompt_template: The prompt template for the skill.     required_tools: List of required tool names.     required_knowledge: List of required knowledge names.     actions: List of actions.     config: Additional configuration.`type(cls)` 为同步对外方法。说明：Return the skill type.`metadata(self)` 为同步对外方法。说明：Return the skill metadata.`prompt_template(self)` 为同步对外方法。说明：Return the prompt template.`required_tools(self)` 为同步对外方法。说明：Return the list of required tool names.`required_knowledge(self)` 为同步对外方法。说明：Return the list of required knowledge names.`actions(self)` 为同步对外方法。说明：Return the list of actions.`config(self)` 为同步对外方法。说明：Return the skill configuration.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillLoader`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/loader.py` 第 14 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skill loader for loading skills from files or modules.方法设计如下。`__init__(self,skill_dirs)` 为同步对外方法。说明：Initialize skill loader.  Args:     skill_dirs: List of directories to search for skills.`load_skill_from_file(self,file_path)` 为同步对外方法。说明：Load a skill from a JSON/YAML file.  Args:     file_path: Path to the skill file.  Returns:     Skill instance.`load_skill_from_module(self,module_path)` 为同步对外方法。说明：Load a skill from a Python module.  Args:     module_path: Python module path (e.g., "my_skills.coding_skill").  Returns:     Skill instance.`load_skills_from_directory(self,directory,recursive)` 为同步对外方法。说明：Load all skills from a directory.  Args:     directory: Directory path.     recursive: Whether to search recursively.  Returns:     List of skill instances.`_load_from_json(self,path)` 为同步内部方法。说明：Load skill from JSON file.  Args:     path: Path to JSON file.  Returns:     Skill instance.`_load_from_markdown(self,path)` 为同步内部方法。说明：Load a skill from a Claude-style SKILL.md file.  This uses the FileBasedSkill parser implemented in the claude_skill module to parse the SKILL.md frontmatter and instructions, and then converts it into a core Skill instance.`_load_from_yaml(self,path)` 为同步内部方法。说明：Load skill from YAML file.  Args:     path: Path to YAML file.  Returns:     Skill instance.`_create_skill_from_dict(self,data)` 为同步内部方法。说明：Create a skill from dictionary data.  Args:     data: Skill data dictionary.  Returns:     Skill instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillBuilder`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/loader.py` 第 264 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Builder for creating skills programmatically.方法设计如下。`__init__(self,name,description)` 为同步对外方法。说明：Initialize skill builder.  Args:     name: Skill name.     description: Skill description.`with_version(self,version)` 为同步对外方法。说明：Set skill version.  Args:     version: Version string.  Returns:     Self for chaining.`with_author(self,author)` 为同步对外方法。说明：Set skill author.  Args:     author: Author name.  Returns:     Self for chaining.`with_skill_type(self,skill_type)` 为同步对外方法。说明：Set skill type.  Args:     skill_type: Skill type.  Returns:     Self for chaining.`with_tags(self,tags)` 为同步对外方法。说明：Set skill tags.  Args:     tags: List of tags.  Returns:     Self for chaining.`with_prompt_template(self,prompt_template)` 为同步对外方法。说明：Set prompt template.  Args:     prompt_template: Prompt template string.  Returns:     Self for chaining.`with_required_tool(self,tool_name)` 为同步对外方法。说明：Add a required tool.  Args:     tool_name: Tool name.  Returns:     Self for chaining.`with_required_knowledge(self,knowledge_name)` 为同步对外方法。说明：Add required knowledge.  Args:     knowledge_name: Knowledge name.  Returns:     Self for chaining.`with_action(self,action)` 为同步对外方法。说明：Add an action.  Args:     action: Action instance.  Returns:     Self for chaining.`with_config(self,config)` 为同步对外方法。说明：Set configuration.  Args:     config: Configuration dictionary.  Returns:     Self for chaining.`build(self)` 为同步对外方法。说明：Build the skill.  Returns:     Skill instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillMetadata`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 17 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Metadata for a Claude-style SKILL file.方法设计如下。`__str__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ClaudeSkill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 37 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base class for Claude-style SKILL.方法设计如下。`name(cls)` 为同步对外方法。说明：Return the skill name.`description(cls)` 为同步对外方法。说明：Return the skill description.`instructions(cls)` 为同步对外方法。说明：Return the skill instructions.`matches(cls,user_input)` 为同步对外方法。说明：Check if the user input matches this skill.  Can be overridden for custom matching logic.  Args:     user_input: The user's input string.  Returns:     True if the skill should be activated.`get_prompt(cls)` 为同步对外方法。说明：Get the prompt template for this skill.  Returns:     PromptTemplate with the skill instructions.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `FileBasedSkill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 84 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Skill loaded from a SKILL.md file.  This implements Claude's SKILL.md file format:  --- name: skill-name description: Skill description ---  Instructions here...  Examples triggers are in the description.方法设计如下。`__init__(self,file_path)` 为同步对外方法。说明：Initialize from a SKILL.md file.  Args:     file_path: Path to the SKILL.md file.`_parse_file(self,file_path)` 为同步内部方法。说明：Parse a SKILL.md file.  Args:     file_path: Path to the file.  Returns:     Tuple of (metadata, instructions).`_parse_metadata(self,text,file_path)` 为同步内部方法。说明：Parse metadata section.  Args:     text: Metadata text.     file_path: File path for error messages.  Returns:     SkillMetadata instance.`metadata(self)` 为同步对外方法。说明：Return the skill metadata.`instructions(self)` 为同步对外方法。说明：Return the skill instructions.`matches(self,user_input)` 为同步对外方法。说明：Check if user input matches this skill.  Simple matching based on description containing trigger phrases.  Args:     user_input: User input string.  Returns:     True if should activate this skill.`_extract_keywords(self,description)` 为同步内部方法。说明：Extract potential trigger keywords from description.  Args:     description: Skill description.  Returns:     List of keywords.`get_prompt(self)` 为同步对外方法。说明：Get the prompt template for this skill.  Returns:     PromptTemplate with the skill instructions.`__str__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SkillRegistry`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 345 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Registry for managing Claude-style skills.方法设计如下。`__init__(self)` 为同步对外方法。说明：Initialize the skill registry.`register_skill(self,skill)` 为同步对外方法。说明：Register a file-based skill.  Args:     skill: FileBasedSkill instance.`register_skill_class(self,skill_class)` 为同步对外方法。说明：Register a skill class.  Args:     skill_class: A subclass of ClaudeSkill.`get_skill(self,name)` 为同步对外方法。说明：Get a skill by name.  Args:     name: Skill name.  Returns:     FileBasedSkill or None.`get_skill_class(self,name)` 为同步对外方法。说明：Get a skill class by name.  Args:     name: Skill name.  Returns:     Skill class or None.`list_skills(self)` 为同步对外方法。说明：List all registered skills.  Returns:     List of skill metadata.`match_skill(self,user_input)` 为同步对外方法。说明：Find a matching skill for the user input.  Args:     user_input: User's input string.  Returns:     Matching skill or None.`load_from_directory(self,directory,recursive)` 为同步对外方法。说明：Load all SKILL.md files from a directory.  Args:     directory: Directory path.     recursive: Whether to search recursively.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ClaudeSkillAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/agent.py` 第 16 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent that uses Claude-style SKILL files.  This agent automatically detects which SKILL to use based on user input and applies the corresponding instructions to the prompt.方法设计如下。`__init__(self,skill_registry)` 为同步对外方法。说明：Initialize a Claude Skill Agent.  Args:     skill_registry: Skill registry instance (uses global if None).     **kwargs: Additional arguments for ConversableAgent.`skill_registry(self)` 为同步对外方法。说明：Return the skill registry.`current_skill(self)` 为同步对外方法。说明：Return the currently active skill.`detect_and_apply_skill(self,user_input)` 为同步对外方法。说明：Detect and apply a matching skill for the user input.  Args:     user_input: The user's input message.`set_skill(self,skill_name)` 为同步对外方法。说明：Manually set a specific skill.  Args:     skill_name: Name of the skill to activate.  Raises:     ValueError: If skill not found.`clear_skill(self)` 为同步对外方法。说明：Clear the current skill and revert to base prompt.`_apply_skill_to_prompt(self)` 为同步内部方法。说明：Apply the current skill's prompt template.`generate_reply(self,received_message,sender,reviewer,rely_messages,historical_dialogues,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Generate a reply with skill detection.  Args:     received_message: The received message.     sender: Sender agent.     reviewer: Reviewer agent.     rely_messages: List of relied messages.     historical_dialogues: Historical dialogue messages.     is_retry_chat: Whether this is a retry.     last_s`get_available_skills(self)` 为同步对外方法。说明：Get list of available skill names.  Returns:     List of skill names.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/agent.py` 第 19 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Configuration for MiddlewareAgent.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MiddlewareAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/agent.py` 第 41 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：ConversableAgent with middleware support.  This agent extends ConversableAgent to support middleware plugins, similar to deepagents' middleware pattern. The middleware system allows plugins to hook into agent lifecycle events.  Example:     ```python     from dbgpt.agent.core.profile.base import ProfileConfig     from dbgpt.agent.middleware.agent import MiddlewareAgent, AgentConfig     from dbgpt.agent.skill.middleware_v2 import SkillsMiddlewareV2      profile = ProfileConfig(name="assistant", r方法设计如下。`__init__(self,agent_config)` 为同步对外方法。说明：Initialize MiddlewareAgent.  Args:     agent_config: Agent configuration.     **kwargs: Additional arguments for ConversableAgent.`middleware_manager(self)` 为同步对外方法。说明：Return the middleware manager.`register_middleware(self,middleware)` 为同步对外方法。说明：Register a middleware.  Args:     middleware: The middleware to register.`unregister_middleware(self,middleware)` 为同步对外方法。说明：Unregister a middleware.  Args:     middleware: The middleware to unregister.`build(self,is_retry_chat)` 为异步对外方法。说明：Build the agent with middleware integration.  Args:     is_retry_chat: Whether this is a retry chat.  Returns:     Built agent instance.`_a_init_reply_message(self,received_message,rely_messages)` 为异步内部方法。说明：Initialize reply message with middleware hooks.  Args:     received_message: The received message.     rely_messages: List of relied messages.  Returns:     AgentMessage or None.`thinking(self,messages,sender,prompt,stream_callback)` 为异步对外方法。说明：Think with middleware hooks.  Args:     messages: The messages to be reasoned.     sender: Sender agent.     prompt: The prompt to be reasoned.  Returns:     Tuple of (reply, model_name).`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Act with middleware hooks.  Args:     message: The message to be executed.     sender: Sender agent.     reviewer: Reviewer agent.     is_retry_chat: Whether this is a retry chat.     last_speaker_name: Last speaker name.     **kwargs: Additional arguments.  Returns:     ActionOutput.`build_system_prompt(self,question,most_recent_memories,resource_vars,context,is_retry_chat)` 为异步对外方法。说明：Build system prompt with middleware hooks.  Args:     question: Current question.     most_recent_memories: Most recent memories.     resource_vars: Resource variables.     context: Additional context.     is_retry_chat: Whether this is a retry chat.  Returns:     System prompt string.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LoggingMiddleware`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/example.py` 第 56 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Custom middleware for logging.方法设计如下。`before_generate_reply(self,agent,context)` 为异步对外方法。说明：Called before generating a reply.`after_generate_reply(self,agent,context,reply_message)` 为异步对外方法。说明：Called after generating a reply.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentMiddleware`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/base.py` 第 18 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base class for agent middleware.  Middleware can hook into various lifecycle events of an agent: - before_init: Before agent initialization - after_init: After agent initialization - before_generate_reply: Before generating a reply - after_generate_reply: After generating a reply - before_thinking: Before the thinking step - after_thinking: After the thinking step - before_act: Before the act step - after_act: After the act step - modify_system_prompt: Modify the system prompt方法设计如下。`__init__(self)` 为同步对外方法。说明：Initialize the middleware.`enabled(self)` 为同步对外方法。说明：Check if middleware is enabled.`enable(self)` 为同步对外方法。说明：Enable the middleware.`disable(self)` 为同步对外方法。说明：Disable the middleware.`before_init(self,agent)` 为异步对外方法。说明：Called before agent initialization.  Args:     agent: The agent instance.     **kwargs: Additional initialization arguments.  Returns:     Optional state to be passed to other middleware or the agent.`after_init(self,agent)` 为异步对外方法。说明：Called after agent initialization.  Args:     agent: The agent instance.     **kwargs: Additional initialization arguments.  Returns:     Optional state to be merged with agent state.`before_generate_reply(self,agent,context)` 为异步对外方法。说明：Called before generating a reply.  Args:     agent: The agent instance.     context: The generate context.     **kwargs: Additional arguments.  Returns:     Optional state update.`after_generate_reply(self,agent,context,reply_message)` 为异步对外方法。说明：Called after generating a reply.  Args:     agent: The agent instance.     context: The generate context.     reply_message: The generated reply message.     **kwargs: Additional arguments.  Returns:     Optional state update.`before_thinking(self,agent,context)` 为异步对外方法。说明：Called before the thinking step.  Args:     agent: The agent instance.     context: The generate context.     **kwargs: Additional arguments.  Returns:     Optional state update.`after_thinking(self,agent,context,llm_reply,model_name)` 为异步对外方法。说明：Called after the thinking step.  Args:     agent: The agent instance.     context: The generate context.     llm_reply: The LLM reply.     model_name: The model name used.     **kwargs: Additional arguments.  Returns:     Optional state update.`before_act(self,agent,context,message)` 为异步对外方法。说明：Called before the act step.  Args:     agent: The agent instance.     context: The generate context.     message: The message to act on.     **kwargs: Additional arguments.  Returns:     Optional state update.`after_act(self,agent,context,action_output)` 为异步对外方法。说明：Called after the act step.  Args:     agent: The agent instance.     context: The generate context.     action_output: The action output.     **kwargs: Additional arguments.  Returns:     Optional state update.`modify_system_prompt(self,agent,original_prompt,context)` 为异步对外方法。说明：Modify the system prompt before it's sent to LLM.  Args:     agent: The agent instance.     original_prompt: The original system prompt.     context: Additional context information.  Returns:     Modified system prompt.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MiddlewareManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/base.py` 第 211 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Manager for agent middleware.  Handles registration, ordering, and execution of middleware.方法设计如下。`__init__(self)` 为同步对外方法。说明：Initialize the middleware manager.`register(self,middleware)` 为同步对外方法。说明：Register a middleware.  Args:     middleware: The middleware to register.  Returns:     Self for chaining.`unregister(self,middleware)` 为同步对外方法。说明：Unregister a middleware.  Args:     middleware: The middleware to unregister.  Returns:     Self for chaining.`get_state(self,key,default)` 为同步对外方法。说明：Get a state value.  Args:     key: The state key.     default: Default value if key doesn't exist.  Returns:     The state value.`set_state(self,key,value)` 为同步对外方法。说明：Set a state value.  Args:     key: The state key.     value: The state value.`update_state(self,updates)` 为同步对外方法。说明：Update state with a dictionary.  Args:     updates: Dictionary of state updates.`execute_before_init(self,agent)` 为异步对外方法。说明：Execute all before_init hooks.  Args:     agent: The agent instance.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_after_init(self,agent)` 为异步对外方法。说明：Execute all after_init hooks.  Args:     agent: The agent instance.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_before_generate_reply(self,agent,context)` 为异步对外方法。说明：Execute all before_generate_reply hooks.  Args:     agent: The agent instance.     context: The generate context.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_after_generate_reply(self,agent,context,reply_message)` 为异步对外方法。说明：Execute all after_generate_reply hooks.  Args:     agent: The agent instance.     context: The generate context.     reply_message: The generated reply message.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_before_thinking(self,agent,context)` 为异步对外方法。说明：Execute all before_thinking hooks.  Args:     agent: The agent instance.     context: The generate context.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_after_thinking(self,agent,context,llm_reply,model_name)` 为异步对外方法。说明：Execute all after_thinking hooks.  Args:     agent: The agent instance.     context: The generate context.     llm_reply: The LLM reply.     model_name: The model name used.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_before_act(self,agent,context,message)` 为异步对外方法。说明：Execute all before_act hooks.  Args:     agent: The agent instance.     context: The generate context.     message: The message to act on.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_after_act(self,agent,context,action_output)` 为异步对外方法。说明：Execute all after_act hooks.  Args:     agent: The agent instance.     context: The generate context.     action_output: The action output.     **kwargs: Additional arguments.  Returns:     Combined state from all middleware.`execute_modify_system_prompt(self,agent,original_prompt,context)` 为异步对外方法。说明：Execute all modify_system_prompt hooks.  Args:     agent: The agent instance.     original_prompt: The original system prompt.     context: Additional context information.  Returns:     Modified system prompt.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tool_action.py` 第 21 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Plugin input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tool_action.py` 第 37 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Tool action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the plugin action.  Args:     ai_message (str): The AI message.     resource (Optional[AgentResource], optional): The resource. Defaults to         None.     rely_action_out (Optional[ActionOutput], optional): The rely action output.         Defaults to None.     need_vis_render (bool, optio在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolCallSpec`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tool_action.py` 第 262 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A single tool invocation for batch (parallel) execution.  Attributes:     name: The tool name, looked up in the agent's ``ToolPack``.     args: The tool arguments (already resolved to a dict).     call_id: Optional provider-native tool call id. Only populated in the         stage-2 native-function-calling path; ``None`` for the text path.     raw_tool_input: The raw model-produced argument string, used by         :func:`run_tool` to re-parse arguments when ``args`` is empty.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Terminate`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/react_action.py` 第 17 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Terminate action.  It is a special action to terminate the conversation, at same time, it can be a tool to return the final answer.方法设计如下。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`get_action_description(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`parse_action(cls,ai_message,default_action,resource)` 为同步对外方法。说明：Parse the action from the message.  If you want skip the action, return None.`name(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`description(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`args(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`execute(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`async_execute(self)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReActAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/react_action.py` 第 108 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：React action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Tool action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`parse_action(cls,ai_message,default_action,resource)` 为同步对外方法。说明：Parse the action from the message.  If you want skip the action, return None.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.`_fallback_parse_args(tool_name,raw_input,resource)` 为同步内部方法。说明：Infer tool args when JSON parsing fails.  Strategy: 1. For single-param tools: regex extract the value, or pass raw input. 2. For multi-param tools: find each param key position and extract    the string value between quote delimiters. 3. Return empty dict as last resort.`_extract_html_interpreter_args(raw_input)` 为同步内部方法。说明：Robust extraction for html_interpreter {"html": ..., "title": ...}.  HTML content typically contains many double-quotes (class="...", style="...") which break standard JSON parsing. This method uses a reverse-search strategy: 1. Find the LAST occurrence of '"title"' followed by ':' (the actual    JS`_do_run(self,ai_message,parsed_step,need_vis_render)` 为异步内部方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Excel2TableInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/insert_action.py` 第 51 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Excel2TableAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/insert_action.py` 第 65 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,file_paths)` 为同步对外方法。说明：Initialize Excel2TableAction with list of file paths.`resource_need(self)` 为同步对外方法。说明：Return the required resource type (database).`render_protocol(self)` 为同步对外方法。说明：This action doesn't need visualization protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out)` 为异步对外方法。说明：Perform the action: create multiple tables and insert data " "from corresponding Excel files.`extract_table_fields(self,create_sql)` 为同步对外方法。说明：从CREATE TABLE SQL语句中提取字段信息  参数:     create_sql: 包含CREATE TABLE的SQL语句  返回:     字段信息列表，每个元素为包含'name'和'type'的字典`generate_insert_sql(self,table_name,table_fields,excel_data)` 为同步对外方法。说明：按顺序匹配字段生成INSERT语句 逻辑：Excel数据字典的第n个键值对，对应表中第n个字段在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WebSearchInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/websearch_action.py` 第 264 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Web search input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WebSearchAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/websearch_action.py` 第 283 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Web search action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Web search action init.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ChartItem`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/dashboard_action.py` 第 17 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Chart item model.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Convert to dict.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DashboardAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/dashboard_action.py` 第 39 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Dashboard action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Dashboard action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `CodeAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/code_action.py` 第 16 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Code Action Module.方法设计如下。`__init__(self)` 为同步对外方法。说明：Code action init.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.`execute_code_blocks(self,code_blocks)` 为同步对外方法。说明：Execute the code blocks and return the result.`use_docker(self)` 为同步对外方法。说明：Whether to use docker to execute the code.  Bool value of whether to use docker to execute the code, or str value of the docker image name to use, or None when code execution is disabled.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IndicatorInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/indicator_action.py` 第 18 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Indicator llm out model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IndicatorAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/indicator_action.py` 第 42 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Indicator Action.方法设计如下。`__init__(self)` 为同步对外方法。说明：Init indicator action.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`build_headers(self)` 为同步对外方法。说明：Build headers.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SqlInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/chart_action.py` 第 17 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：SQL input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ChartAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/chart_action.py` 第 31 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Chart action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Chart action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `BaseLLMCaller`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/llm.py` 第 18 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base class for LLM caller.方法设计如下。`call_llm(self,prompt,llm_client)` 为异步对外方法。说明：Call LLM client to generate response.  Args:     llm_client(LLMClient): LLM client     prompt(ChatPromptTemplate): prompt     **kwargs: other keyword arguments  Returns:     str: response`get_model(self,llm_client)` 为异步对外方法。说明：Get the model.  Args:     llm_client(LLMClient): LLM client  Returns:     str: model`_parse_list(text)` 为同步内部方法。说明：Parse a newline-separated string into a list of strings.  1. First, split by newline 2. Remove whitespace from each line`_parse_number(text,importance_weight)` 为同步内部方法。说明：Parse a number from a string.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMInsightExtractor`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/llm.py` 第 103 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：LLM Insight Extractor.  Get high-level insights from memories.方法设计如下。`extract_insights(self,memory_fragment,llm_client)` 为异步对外方法。说明：Extract insights from memory fragments.  Args:     memory_fragment(T): Memory fragment     llm_client(Optional[LLMClient]): LLM client  Returns:     InsightMemoryFragment: The insights of the memory fragment.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMImportanceScorer`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/llm.py` 第 138 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：LLM Importance Scorer.  Score the importance of memories.方法设计如下。`score_importance(self,memory_fragment,llm_client)` 为异步对外方法。说明：Score the importance of memory fragments.  Args:     memory_fragment(T): Memory fragment     llm_client(Optional[LLMClient]): LLM client  Returns:     float: The importance score of the memory fragment.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `HybridMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/hybrid.py` 第 31 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Hybrid memory for the agent.方法设计如下。`__init__(self,now,sensory_memory,short_term_memory,long_term_memory,default_insight_extractor,default_importance_scorer)` 为同步对外方法。说明：Create a hybrid memory.`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.`from_chroma(cls,vstore_name,vstore_path,embeddings,executor,now,sensory_memory,short_term_memory,long_term_memory)` 为同步对外方法。说明：Create a hybrid memory from Chroma vector store.`from_vstore(cls,vector_store,embeddings,executor,now,sensory_memory,short_term_memory,long_term_memory)` 为同步对外方法。说明：Create a hybrid memory from vector store.`initialize(self,name,llm_client,importance_scorer,insight_extractor,real_memory_fragment_class,session_id)` 为同步对外方法。说明：Initialize the memory.  It will initialize all the memories.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to the memory.`_write_single(self,memory_fragment,now,op,write_long_term)` 为异步内部方法。说明：Write a single memory fragment to the memory.`write_batch(self,memory_fragments,now)` 为异步对外方法。说明：Write a batch of memory fragments to the memory.  For memory recovery, we only write to sensory memory and short term memory.`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memories from the memory.`fetch_memories(self,observation,short_term_memory)` 为异步对外方法。说明：Fetch memories from long term memory.  If short_term_memory is provided, write the fetched memories to the short term memory.`save_memories_after_retrieval(self,fragments)` 为异步对外方法。说明：Save memories after retrieval.`clear(self)` 为异步对外方法。说明：Clear the memory.  # TODO在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `StructuredObservation`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/agent_memory.py` 第 28 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Structured observation for agent memory.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentMemoryFragment`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/agent_memory.py` 第 41 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Default memory fragment for agent memory.方法设计如下。`__init__(self,observation,embeddings,memory_id,importance,last_accessed_time,is_insight,snapshot_path)` 为同步对外方法。说明：Create a memory fragment.`id(self)` 为同步对外方法。说明：Return the memory id.`raw_observation(self)` 为同步对外方法。说明：Return the raw observation.`embeddings(self)` 为同步对外方法。说明：Return the embeddings of the memory fragment.`update_embeddings(self,embeddings)` 为同步对外方法。说明：Update the embeddings of the memory fragment.  Args:     embeddings(List[float]): embeddings`calculate_current_embeddings(self,embedding_func)` 为同步对外方法。说明：Calculate the embeddings of the memory fragment.  Args:     embedding_func(Callable[[List[str]], List[List[float]]]): Function to         compute embeddings  Returns:     List[float]: Embeddings of the memory fragment`is_insight(self)` 为同步对外方法。说明：Return whether the memory fragment is an insight.  Returns:     bool: Whether the memory fragment is an insight`importance(self)` 为同步对外方法。说明：Return the importance of the memory fragment.  Returns:     Optional[float]: Importance of the memory fragment`update_importance(self,importance)` 为同步对外方法。说明：Update the importance of the memory fragment.  Args:     importance(float): Importance of the memory fragment  Returns:     Optional[float]: Old importance`last_accessed_time(self)` 为同步对外方法。说明：Return the last accessed time of the memory fragment.  Used to determine the least recently used memory fragment.  Returns:     Optional[datetime]: Last accessed time`update_accessed_time(self,now)` 为同步对外方法。说明：Update the last accessed time of the memory fragment.  Args:     now(datetime): Current time  Returns:     Optional[datetime]: Old last accessed time`build_from(cls,observation,embeddings,memory_id,importance,is_insight,last_accessed_time,snapshot_path)` 为同步对外方法。说明：Build a memory fragment from the given parameters.`copy(self)` 为同步对外方法。说明：Return a copy of the memory fragment.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `StructuredAgentMemoryFragment`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/agent_memory.py` 第 195 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Structured memory fragment for agent memory.方法设计如下。`__init__(self,observation,embeddings,memory_id,importance,last_accessed_time,is_insight)` 为同步对外方法。说明：Create a structured memory fragment.`to_raw_observation(self,observation)` 为同步对外方法。说明：Convert the structured observation to a raw observation.  Args:     observation(StructuredObservation): Structured observation  Returns:     str: Raw observation`build_from(cls,observation,embeddings,memory_id,importance,is_insight,last_accessed_time)` 为同步对外方法。说明：Build a memory fragment from the given parameters.`reduce(self,memory_fragments)` 为同步对外方法。说明：Reduce memory fragments to a single memory fragment.  Args:     memory_fragments(List[T]): Memory fragments  Returns:     T: The reduced memory fragment在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/agent_memory.py` 第 283 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent memory.方法设计如下。`__init__(self,memory,importance_scorer,insight_extractor,gpts_memory)` 为同步对外方法。说明：Create an agent memory.  Args:     memory(Memory[AgentMemoryFragment]): Memory to store fragments     importance_scorer(ImportanceScorer[AgentMemoryFragment]): Scorer to         calculate the importance of memory fragments     insight_extractor(InsightExtractor[AgentMemoryFragment]): Extractor to`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.  The gpst_memory is not cloned, it will be shared in whole agent memory.`initialize(self,name,llm_client,importance_scorer,insight_extractor,real_memory_fragment_class,session_id)` 为同步对外方法。说明：Initialize the memory.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to the memory.`write_batch(self,memory_fragments,now)` 为异步对外方法。说明：Write a batch of memory fragments to the memory.`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memory fragments related to the observation.  Args:     observation(str): Observation     alpha(float): Importance weight     beta(float): Time weight     gamma(float): Randomness weight  Returns:     List[AgentMemoryFragment]: List of memory fragments`clear(self)` 为异步对外方法。说明：Clear the memory.`plans_memory(self)` 为同步对外方法。说明：Return the plan memory.`message_memory(self)` 为同步对外方法。说明：Return the message memory.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WriteOperation`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 33 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Write operation.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MemoryFragment`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 41 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Memory fragment interface.  It is the interface of memory fragment, which is the basic unit of memory, which contains the basic information of memory, such as observation, importance, whether it is insight, last access time, etc方法设计如下。`build_from(cls,observation,embeddings,memory_id,importance,is_insight,last_accessed_time)` 为同步对外方法。说明：Build a memory fragment from memory id and observation.  Args:     observation(str): Observation     embeddings(List[float], optional): Embeddings of the memory fragment.     memory_id(int): Memory id     importance(float): Importance     is_insight(bool): Whether the memory fragment is an insight`id(self)` 为同步对外方法。说明：Return the id of the memory fragment.  Commonly, the id is generated by Snowflake algorithm. So we can parse the timestamp of when the memory fragment is created.  Returns:     int: id`metadata(self)` 为同步对外方法。说明：Return the metadata of the memory fragment.  Returns:     Dict[str, Any]: Metadata`importance(self)` 为同步对外方法。说明：Return the importance of the memory fragment.  It should be noted that importance only reflects the characters of the memory itself.  Returns:     Optional[float]: importance, None means the importance is not available.`update_importance(self,importance)` 为同步对外方法。说明：Update the importance of the memory fragment.  Args:     importance(float): importance  Returns:     Optional[float]: importance`raw_observation(self)` 为同步对外方法。说明：Return the raw observation.  Raw observation is the original observation data, it can be an observation from  environment or an observation after executing an action.  Returns:     str: raw observation`embeddings(self)` 为同步对外方法。说明：Return the embeddings of the memory fragment.  Returns:     Optional[List[float]]: embeddings`update_embeddings(self,embeddings)` 为同步对外方法。说明：Update the embeddings of the memory fragment.  Args:     embeddings(List[float]): embeddings`calculate_current_embeddings(self,embedding_func)` 为同步对外方法。说明：Calculate the embeddings of the memory fragment.  Args:     embedding_func(Callable[[List[str]], List[List[float]]]): Function to         compute embeddings  Returns:     List[float]: Embeddings of the memory fragment`is_insight(self)` 为同步对外方法。说明：Return whether the memory fragment is an insight.  Returns:     bool: whether the memory fragment is an insight.`last_accessed_time(self)` 为同步对外方法。说明：Return the last accessed time of the memory fragment.  Returns:     Optional[datetime]: last accessed time`update_accessed_time(self,now)` 为同步对外方法。说明：Update the last accessed time of the memory fragment.  Args:     now(datetime): The current time  Returns:     Optional[datetime]: The last accessed time`copy(self)` 为同步对外方法。说明：Copy the memory fragment.`reduce(self,memory_fragments)` 为同步对外方法。说明：Reduce memory fragments to a single memory fragment.  Args:     memory_fragments(List[T]): Memory fragments  Returns:     T: The reduced memory fragment`current_class(self)` 为同步对外方法。说明：Return the current class.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `InsightMemoryFragment`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 217 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Insight memory fragment.  Insight memory fragment is a memory fragment that contains insights.方法设计如下。`__init__(self,original_memory_fragment,insights)` 为同步对外方法。说明：Create an insight memory fragment.  Insight is also a memory fragment.`original_memory_fragment(self)` 为同步对外方法。说明：Return the original memory fragment.`insights(self)` 为同步对外方法。说明：Return the insights.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DiscardedMemoryFragments`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 256 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Discarded memory fragments.  Sometimes, we need to discard some memory fragments, there are following cases: 1. Memory duplicated, the same/similar action is executed multiple times and the     same/similar observation from environment is received. 2. Memory overflow. The memory is full and the new memory fragment needs to be written. 3. The memory fragment is not important enough. 4. Simulation of forgetting mechanism.  The discarded memory fragments may be transferred to another memory.方法设计如下。`__init__(self,discarded_memory_fragments,discarded_insights)` 为同步对外方法。说明：Create a discarded memory fragments.`discarded_memory_fragments(self)` 为同步对外方法。说明：Return the discarded memory fragments.`discarded_insights(self)` 为同步对外方法。说明：Return the discarded insights.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `InsightExtractor`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 292 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Insight extractor interface.  Obtain high-level insights from memories.方法设计如下。`extract_insights(self,memory_fragment,llm_client)` 为异步对外方法。说明：Extract insights from memory fragments.  Args:     memory_fragment(T): Memory fragment     llm_client(Optional[LLMClient]): LLM client  Returns:     InsightMemoryFragment: The insights of the memory fragment.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ImportanceScorer`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 315 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Importance scorer interface.  Score the importance of memories.方法设计如下。`score_importance(self,memory_fragment,llm_client)` 为异步对外方法。说明：Score the importance of memory fragment.  Args:     memory_fragment(T): Memory fragment.     llm_client(Optional[LLMClient]): LLM client  Returns:     float: The importance of the memory fragment.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Memory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 339 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Memory interface.方法设计如下。`initialize(self,name,llm_client,importance_scorer,insight_extractor,real_memory_fragment_class,session_id)` 为同步对外方法。说明：Initialize memory.  Some agent may need to initialize memory before using it.`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.  Sometimes, we need to clone the structure of the memory, but not the content.  There some cases:  1. When we need to reset the memory, we can use this method to create a new one, and the new memory has the same structure as the old one. 2. Create a new agent,`_copy_from(self,memory)` 为同步内部方法。说明：Copy memory from another memory.  Args:     memory(Memory): Another memory`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to memory.  Two situations need to be noted here: 1. Memory duplicated, the same/similar action is executed multiple times and     the same/similar observation from environment is received.  2.Memory overflow. The memory is full and the new memory fragment needs to be     wri`write_batch(self,memory_fragments,now)` 为异步对外方法。说明：Write a batch of memory fragments to memory.  Args:     memory_fragments(List[T]): Memory fragments     now(Optional[datetime]): The current time  Returns:     Optional[DiscardedMemoryFragments]: The discarded memory fragments, None         means no memory fragments are discarded.`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memory fragments by observation.  Usually, there three commonly used criteria for information extraction, that is, the recency, relevance, and importance  Memories that are more recent, relevant, and important are more likely to be extracted. Formally, we conclude the following equation from ex`reflect(self,memory_fragments)` 为异步对外方法。说明：Reflect memory fragments by observation.  Args:     memory_fragments(List[T]): memory fragments to be reflected.  Returns:     List[T]: memory fragments after reflection.`handle_duplicated(self,memory_fragments,new_memory_fragments)` 为异步对外方法。说明：Handle duplicated memory fragments.  Args:     memory_fragments(List[T]): Existing memory fragments     new_memory_fragments(List[T]): New memory fragments  Returns:     List[T]: The new memory fragments after handling duplicated memory         fragments.`handle_overflow(self,memory_fragments)` 为异步对外方法。说明：Handle memory overflow.  Args:     memory_fragments(List[T]): Existing memory fragments  Returns:     Tuple[List[T], List[T]]: The memory fragments after handling overflow and         the discarded memory fragments.`clear(self)` 为异步对外方法。说明：Clear all memory fragments.  Returns:     List[T]: The all cleared memory fragments.`get_insights(self,memory_fragments)` 为异步对外方法。说明：Get insights from memory fragments.  Args:     memory_fragments(List[T]): Memory fragments  Returns:     List[InsightMemoryFragment]: The insights of the memory fragments.`score_memory_importance(self,memory_fragments)` 为异步对外方法。说明：Score the importance of memory fragments.  Args:     memory_fragments(List[T]): Memory fragments  Returns:     List[float]: The importance of memory fragments.`real_memory_fragment_class(self)` 为同步对外方法。说明：Return the real memory fragment class.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SensoryMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 620 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Sensory memory.方法设计如下。`__init__(self,buffer_size)` 为同步对外方法。说明：Create a sensory memory.`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to sensory memory.`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memory fragments by observation.`handle_overflow(self,memory_fragments)` 为异步对外方法。说明：Handle memory overflow.  For sensory memory, the overflow strategy is to transfer all memory fragments to short-term memory.  Args:     memory_fragments(List[T]): Existing memory fragments  Returns:     Tuple[List[T], List[T]]: The memory fragments after handling overflow and         the discarded m`clear(self)` 为异步对外方法。说明：Clear all memory fragments.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ShortTermMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/base.py` 第 705 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Short term memory.  All memories are stored in computer memory.方法设计如下。`__init__(self,buffer_size)` 为同步对外方法。说明：Create a short-term memory.`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to short-term memory.  Args:     memory_fragment(T): New memory fragment     now(Optional[datetime]): The current time     op(WriteOperation): Write operation  Returns:     Optional[DiscardedMemoryFragments]: The discarded memory fragments, None         means no memory fragme`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memory fragments by observation.`transfer_to_long_term(self,memory_fragment)` 为异步对外方法。说明：Transfer the oldest memories to long-term memory.  This is a very simple strategy, just transfer the oldest memories to long-term memory.`clear(self)` 为异步对外方法。说明：Clear all memory fragments.`short_term_memories(self)` 为同步对外方法。说明：Return short-term memories.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LongTermRetriever`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/long_term.py` 第 24 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Long-term retriever with persistence support.方法设计如下。`__init__(self,now)` 为同步对外方法。说明：Create a long-term retriever.  Args:     now: Current datetime to use for time-based calculations     **kwargs: Additional arguments passed to TimeWeightedEmbeddingRetriever`_retrieve(self,query,filters)` 为同步内部方法。说明：Retrieve memories based on query and time weights.  Args:     query: The query string     filters: Optional metadata filters  Returns:     List of relevant document chunks`_retrieve_vector_store_only(self,query,filters,current_time)` 为同步内部方法。说明：Retrieve documents using only vector store when memory_stream is unavailable.  Args:     query: The query string     filters: Optional metadata filters  Returns:     List of relevant document chunks`_get_hours_passed(self,time,ref_time)` 为同步内部方法。说明：Get the hours passed between two datetime objects.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LongTermMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/long_term.py` 第 181 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Long-term memory.方法设计如下。`__init__(self,executor,vector_store,now,reflection_threshold,_default_importance,metadata)` 为同步对外方法。说明：Create a long-term memory.`structure_clone(self,now)` 为同步对外方法。说明：Create a structure clone of the long-term memory.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write a memory fragment to the memory.`write_batch(self,memory_fragments,now)` 为异步对外方法。说明：Write a batch of memory fragments to the memory.`read(self,observation,alpha,beta,gamma)` 为异步对外方法。说明：Read memory fragments related to the observation.`fetch_memories(self,observation,now)` 为异步对外方法。说明：Fetch memories related to the observation.`clear(self)` 为异步对外方法。说明：Clear the memory.  TODO: Implement this method.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `EnhancedShortTermMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/short_term.py` 第 22 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Enhanced short term memory.方法设计如下。`__init__(self,embeddings,executor,buffer_size,enhance_similarity_threshold,enhance_threshold)` 为同步对外方法。说明：Initialize enhanced short term memory.`structure_clone(self,now)` 为同步对外方法。说明：Return a structure clone of the memory.`write(self,memory_fragment,now,op)` 为异步对外方法。说明：Write memory fragment to short term memory.  Reference: https://github.com/RUC-GSAI/YuLan-Rec/blob/main/agents/recagent_memory.py#L336`transfer_to_long_term(self,memory_fragment)` 为异步对外方法。说明：Transfer memory fragment to long term memory.`handle_overflow(self,memory_fragments)` 为异步对外方法。说明：Handle overflow of short term memory.  Discard the least important memory fragment if the buffer size exceeds.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PlannerAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/planner_agent.py` 第 14 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责任务目标分解，把用户问题拆成可协作的多 Agent 步骤。作者注释或文档字符串指出：Planner Agent.  Planner agent, realizing task goal planning decomposition through LLM.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new PlannerAgent instance.`_init_reply_message(self,received_message,rely_messages)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`bind_agents(self,agents)` 为同步对外方法。说明：Bind the agents to the planner agent.`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。说明：Prepare the parameters for the act method.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PlanInput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/plan_action.py` 第 19 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Plan input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PlanAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/plan_action.py` 第 38 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Plan action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a plan action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Run the plan action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AutoPlanChatManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/team_auto_plan.py` 第 21 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：A chat manager agent that can manage a team chat of multiple agents.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new AutoPlanChatManager instance.`process_rely_message(self,conv_id,now_plan,speaker)` 为异步对外方法。说明：Process the dependent message.`select_speaker_msg(self,agents)` 为同步对外方法。说明：Return the message for selecting the next speaker.`select_speaker(self,last_speaker,selector,now_goal_context,pre_allocated)` 为异步对外方法。说明：Select the next speaker.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform an action based on the received message.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `BlankAction`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/action/blank_action.py` 第 12 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Blank action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Blank action init.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.  Just return the AI message.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ActionOutput`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/action/base.py` 第 40 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Action output model.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre-fill the values.`from_dict(cls,param)` 为同步对外方法。说明：Convert dict to ActionOutput object.`to_dict(self)` 为同步对外方法。说明：Convert the object to a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Action`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/action/base.py` 第 98 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base Action class for defining agent actions.方法设计如下。`__init__(self,language,name)` 为同步对外方法。说明：Create an action.`init_resource(self,resource)` 为同步对外方法。说明：Initialize the resource.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`name(self)` 为同步对外方法。说明：Return the action name.`get_action_description(cls)` 为同步对外方法。说明：Return the action description.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`render_prompt(self)` 为同步对外方法。说明：Return the render prompt.`_create_example(self,model_type)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`ai_out_schema_json(self)` 为同步对外方法。说明：Return the AI output json schema.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`_ai_message_2_json(self,ai_message)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_input_convert(self,ai_message,cls)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`parse_action(cls,ai_message,default_action,resource)` 为同步对外方法。说明：Parse the action from the message.  If you want skip the action, return None.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Profile`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 150 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Profile interface.方法设计如下。`get_name(self)` 为同步对外方法。说明：Return the name of current agent.`get_role(self)` 为同步对外方法。说明：Return the role of current agent.`get_goal(self)` 为同步对外方法。说明：Return the goal of current agent.`get_retry_goal(self)` 为同步对外方法。说明：Return the goal of current agent.`get_constraints(self)` 为同步对外方法。说明：Return the constraints of current agent.`get_retry_constraints(self)` 为同步对外方法。说明：Return the constraints of current agent.`get_description(self)` 为同步对外方法。说明：Return the description of current agent.  It will not be used to generate prompt.`get_expand_prompt(self)` 为同步对外方法。说明：Return the expand prompt of current agent.`get_examples(self)` 为同步对外方法。说明：Return the examples of current agent.`get_system_prompt_template(self)` 为同步对外方法。说明：Return the prompt template of current agent.`get_user_prompt_template(self)` 为同步对外方法。说明：Return the user prompt template of current agent.`get_write_memory_template(self)` 为同步对外方法。说明：Return the save memory template of current agent.`format_system_prompt(self,template_env,question,language,most_recent_memories,resource_vars,is_retry_chat)` 为同步对外方法。说明：Format the system prompt.  Args:     template_env(Optional[Environment]): The template environment for jinja2.     question(Optional[str]): The question.     language(str): The language of current context.     most_recent_memories(Optional[str]): The most recent memories, it reads         from memor`format_user_prompt(self,template_env,question,language,most_recent_memories,resource_vars)` 为同步对外方法。说明：Format the user prompt.  Args:     template_env(Optional[Environment]): The template environment for jinja2.     question(Optional[str]): The question.     language(str): The language of current context.     most_recent_memories(Optional[str]): The most recent memories, it reads         from memory.`_sub_render_keys(self)` 为同步内部方法。说明：Return the sub render keys.  If the value is a string and the key is in the sub render keys, it will be     rendered.  Returns:     Set[str]: The sub render keys.`_format_prompt(self,template,template_env,question,language,most_recent_memories,resource_vars,is_retry_chat)` 为同步内部方法。说明：Format the prompt.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DefaultProfile`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 344 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Default profile.方法设计如下。`get_name(self)` 为同步对外方法。说明：Return the name of current agent.`get_role(self)` 为同步对外方法。说明：Return the role of current agent.`get_goal(self)` 为同步对外方法。说明：Return the goal of current agent.`get_retry_goal(self)` 为同步对外方法。说明：Return the retry goal of current agent.`get_constraints(self)` 为同步对外方法。说明：Return the constraints of current agent.`get_retry_constraints(self)` 为同步对外方法。说明：Return the retry constraints of current agent.`get_description(self)` 为同步对外方法。说明：Return the description of current agent.  It will not be used to generate prompt.`get_expand_prompt(self)` 为同步对外方法。说明：Return the expand prompt of current agent.`get_examples(self)` 为同步对外方法。说明：Return the examples of current agent.`get_system_prompt_template(self)` 为同步对外方法。说明：Return the prompt template of current agent.`get_user_prompt_template(self)` 为同步对外方法。说明：Return the user prompt template of current agent.`get_write_memory_template(self)` 为同步对外方法。说明：Return the save memory template of current agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ProfileFactory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 436 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Profile factory interface.  It is used to create a profile.方法设计如下。`create_profile(self,profile_id,name,role,goal,prefer_prompt_language,prefer_model)` 为同步对外方法。说明：Create a profile.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMProfileFactory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 455 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a profile by LLM.  Based on LLM automatic generation, it usually specifies the rules of the generation  configuration first, clarifies the composition and attributes of the agent  configuration in the target population, and then gives a small number of samples, and finally LLM generates the configuration of all agents.方法设计如下。`create_profile(self,profile_id,name,role,goal,prefer_prompt_language,prefer_model)` 为同步对外方法。说明：Create a profile by LLM.  TODO: Implement this method.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DatasetProfileFactory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 480 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a profile by dataset.  Use existing data sets to generate agent configurations.  In some cases, the data set contains a large amount of information about real people , first organize the information about real people in the data set into a natural language prompt, which is then used to generate the agent configuration.方法设计如下。`create_profile(self,profile_id,name,role,goal,prefer_prompt_language,prefer_model)` 为同步对外方法。说明：Create a profile by dataset.  TODO: Implement this method.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `CompositeProfileFactory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 506 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a profile by combining multiple profile factories.方法设计如下。`__init__(self,factories)` 为同步对外方法。说明：Create a composite profile factory.`create_profile(self,profile_id,name,role,goal,prefer_prompt_language,prefer_model)` 为同步对外方法。说明：Create a profile by combining multiple profile factories.  TODO: Implement this method.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ProfileConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/profile/base.py` 第 529 行。其所在子系统的定位是：Agent 人设与目标配置。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Profile configuration.  If factory is not specified, name and role must be specified. If factory is specified and name and role are also specified, the factory will be preferred.方法设计如下。`check_before(cls,values)` 为同步对外方法。说明：Check before validation.`create_profile(self,profile_id,prefer_prompt_language,prefer_model)` 为同步对外方法。说明：Create a profile.  If factory is specified, use the factory to create the profile.`__hash__(self)` 为同步内部方法。说明：Return the hash value.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ContextManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/manager.py` 第 27 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Orchestrates progressive multi-layer context compaction.  Layers are applied in order of increasing aggressiveness:   - Layer 1 (WARNING):  ObservationMicroCompact — truncate old observations   - Layer 2 (WARNING):  SessionMemoryCompact — drop old rounds   - Layer 3 (ERROR):    FullContextCompression — LLM summary   - Layer 4 (reactive): ReactiveCompact — emergency last-resort trim方法设计如下。`__init__(self,config,model_name,llm_client,on_status_event)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_emit_status(self,token_count,state,compact_layer)` 为异步内部方法。说明：Push a context.status event to the registered callback (if any).`manage_context(self,messages,current_round,task_progress)` 为异步对外方法。说明：Apply progressive compaction based on current token budget state.  Args:     messages: The full list of agent messages (system + conversation).     current_round: Current retry/round counter.     task_progress: The task_progress_summary string (already in system         prompt, used by Layer 2 as im`reactive_compact(self,messages)` 为异步对外方法。说明：Emergency compaction triggered by context_too_long errors.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `TokenState`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/budget.py` 第 16 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Context budget state levels.方法设计如下。`__ge__(self,other)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`__gt__(self,other)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`__le__(self,other)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`__lt__(self,other)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ContextBudgetConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/budget.py` 第 42 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Configuration for context budget management.方法设计如下。`__init__(self,max_context_tokens,warning_threshold,error_threshold,critical_threshold,reserved_tokens,min_keep_recent_rounds,max_compact_failures,max_observation_age_rounds,truncated_observation_max_chars,min_keep_tokens,tool_result_threshold,turn_result_budget,preview_size,tool_overrides)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`effective_budget(self)` 为同步对外方法。说明：Max tokens available after reserving space for output.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ContextBudgetTracker`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/budget.py` 第 92 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tracks token usage and determines context budget state.方法设计如下。`__init__(self,config,model_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`count_messages(self,messages)` 为同步对外方法。说明：Count total tokens across a list of AgentMessage objects.`get_state(self,token_count)` 为同步对外方法。说明：Determine budget state based on current token count.`record_token_count(self,count)` 为同步对外方法。说明：Record a token count for history tracking.`record_compact_success(self)` 为同步对外方法。说明：Reset failure counter on successful compaction.`record_compact_failure(self)` 为同步对外方法。说明：Increment failure counter.`circuit_breaker_tripped(self)` 为同步对外方法。说明：True if too many consecutive compaction failures occurred.`token_history(self)` 为同步对外方法。说明：Return recorded token count history.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ObservationMicroCompact`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 87 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Layer 1 — truncate old Observation messages.  The lightest compaction: shorten tool outputs from old rounds while preserving recent ones in full.方法设计如下。`compact(self,messages,current_round,tracker)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `SessionMemoryCompact`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 145 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Layer 2 — drop old rounds, rely on task_progress as implicit summary.  No LLM call needed because the system prompt already contains ``{{ task_progress }}`` with a record of every completed step.方法设计如下。`compact(self,messages,task_progress,tracker)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `FullContextCompression`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 214 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Layer 3 — LLM-generated structured summary replaces old messages.方法设计如下。`compact(self,messages,llm_client,tracker,model_name)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReactiveCompact`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 306 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Layer 4 — emergency compaction when LLM returns context_too_long.  Keeps only system prompt + the last 2 rounds. The task_progress in the system prompt ensures the agent still knows its history.方法设计如下。`compact(self,messages,tracker)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolResultBudgetConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 105 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Immutable budget constants for the 3-layer tool result persistence system.  Layer 2 (per-result): ``resolve_threshold(tool_name)`` -> threshold in chars. Layer 3 (per-turn):   ``turn_budget`` -> aggregate char budget across all tool                       results in a single assistant turn. Preview:              ``preview_size`` -> inline snippet size after persistence.方法设计如下。`resolve_threshold(self,tool_name)` 为同步对外方法。说明：Resolve the persistence threshold for a tool.  Priority: pinned -> tool_overrides -> default.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolResultStorage`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 247 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Persist large tool results to disk, replace in-context with preview + path.  Usage::      storage = ToolResultStorage(         storage_dir="/path/to/persisted_results/conv_abc",         default_threshold=100_000,         preview_size=1500,     )     replacement, path = storage.maybe_persist(         content=tool_output,         tool_name="sql_query",         tool_call_id="sql_query_abc123",     )     if path:         # Content was persisted; *replacement* has the <persisted-output> block.方法设计如下。`__init__(self,storage_dir,default_threshold,preview_size,tool_overrides,pinned_thresholds,config)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resolve_threshold(self,tool_name)` 为同步对外方法。说明：Resolve the persistence threshold for a tool.  Priority: pinned -> tool_overrides -> default.`maybe_persist(self,content,tool_name,tool_call_id,threshold)` 为同步对外方法。说明：Layer 2: persist oversized result into the storage dir, return preview.  Writes the full content to ``{storage_dir}/{safe_filename}.txt`` and returns a ``<persisted-output>`` replacement block. Falls back to inline truncation if the write fails.  Args:     content: Raw tool result string.     tool_n`enforce_turn_budget(self,results,budget)` 为同步对外方法。说明：Layer 3: enforce aggregate budget across all tool results in a turn.  If total chars exceed budget, persist the largest non-persisted results first (via disk write) until under budget. Already-persisted results (those containing ``<persisted-output>``) are skipped.  Args:     results: List of (conte`read_persisted(self,file_path)` 为同步对外方法。说明：Read back a persisted result from disk.  Args:     file_path: Absolute path to the persisted file.  Returns:     The file content, or None if the file does not exist or cannot be read.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/gpts_memory.py` 第 24 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：GPTs memory.方法设计如下。`__init__(self,plans_memory,message_memory,executor)` 为同步对外方法。说明：Create a memory to store plans and messages.`plans_memory(self)` 为同步对外方法。说明：Return the plans memory.`message_memory(self)` 为同步对外方法。说明：Return the message memory.`init(self,conv_id,enable_vis_message,history_messages,start_round)` 为同步对外方法。说明：Gpt memory init.`enable_vis_message(self,conv_id)` 为同步对外方法。说明：Enable conversation message vis tag.`queue(self,conv_id)` 为同步对外方法。说明：Get conversation message queue.`clear(self,conv_id)` 为同步对外方法。说明：Clear gpt memory.`push_message(self,conv_id,temp_msg)` 为异步对外方法。说明：Push conversation message.`complete(self,conv_id)` 为异步对外方法。说明：Complete conversation message.`append_message(self,conv_id,message)` 为异步对外方法。说明：Append message.`get_messages(self,conv_id)` 为异步对外方法。说明：Get message by conv_id.`get_agent_messages(self,conv_id,agent_role)` 为异步对外方法。说明：Get agent messages.`get_agent_history_memory(self,conv_id,agent_role)` 为异步对外方法。说明：Get agent history memory.`_message_group_vis_build(self,message_group,vis_items)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`agent_stream_message(self,message,enable_vis_message)` 为异步对外方法。说明：Get agent stream message.`_plan_vis_build(self,plan_group)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`simple_message(self,conv_id)` 为异步对外方法。说明：Get agent simple message.`app_link_chat_message(self,conv_id)` 为异步对外方法。说明：Get app link chat message.`_messages_to_agents_vis(self,messages,is_last_message)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`_messages_to_plan_vis(self,messages)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`_messages_to_app_link_vis(self,link_message,lanucher_message)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`chat_messages(self,conv_id)` 为异步对外方法。说明：Get chat messages.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DefaultGptsPlansMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/default_gpts_memory.py` 第 12 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Default memory for storing plans.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a memory to store plans.`batch_save(self,plans)` 为同步对外方法。说明：Save plans in batch.`get_by_conv_id(self,conv_id)` 为同步对外方法。说明：Get plans by conv_id.`get_by_conv_id_and_num(self,conv_id,task_nums)` 为同步对外方法。说明：Get plans by conv_id and task number.`get_todo_plans(self,conv_id)` 为同步对外方法。说明：Get unfinished planning steps.`complete_task(self,conv_id,task_num,result)` 为同步对外方法。说明：Set the planning step to complete.`update_task(self,conv_id,task_num,state,retry_times,agent,model,result)` 为同步对外方法。说明：Update the state of the planning step.`remove_by_conv_id(self,conv_id)` 为同步对外方法。说明：Remove all plans in the conversation.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DefaultGptsMessageMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/default_gpts_memory.py` 第 94 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Default memory for storing messages.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a memory to store messages.`append(self,message)` 为同步对外方法。说明：Append a message to the memory.`get_by_agent(self,conv_id,agent)` 为同步对外方法。说明：Get all messages sent or received by the agent in the conversation.`get_between_agents(self,conv_id,agent1,agent2,current_goal)` 为同步对外方法。说明：Get all messages between two agents in the conversation.`get_by_conv_id(self,conv_id)` 为同步对外方法。说明：Get all messages in the conversation.`get_last_message(self,conv_id)` 为同步对外方法。说明：Get the last message in the conversation.`delete_by_conv_id(self,conv_id)` 为同步对外方法。说明：Delete all messages in the conversation.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsPlan`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/base.py` 第 14 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Gpts plan.方法设计如下。`from_dict(d)` 为同步对外方法。说明：Create a GptsPlan object from a dictionary.`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the GptsPlan object.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsMessage`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/base.py` 第 53 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Gpts message.方法设计如下。`from_dict(d)` 为同步对外方法。说明：Create a GptsMessage object from a dictionary.`to_dict(self)` 为同步对外方法。说明：Return a dictionary representation of the GptsMessage object.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsPlansMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/base.py` 第 103 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Gpts plans memory interface.方法设计如下。`batch_save(self,plans)` 为同步对外方法。说明：Save plans in batch.  Args:     plans: panner generate plans info`get_by_conv_id(self,conv_id)` 为同步对外方法。说明：Get plans by conv_id.  Args:     conv_id: conversation id  Returns:     List[GptsPlan]: List of planning steps`get_by_conv_id_and_num(self,conv_id,task_nums)` 为同步对外方法。说明：Get plans by conv_id and task number.  Args:     conv_id(str): conversation id     task_nums(List[int]): List of sequence numbers of plans in the same         conversation  Returns:     List[GptsPlan]: List of planning steps`get_todo_plans(self,conv_id)` 为同步对外方法。说明：Get unfinished planning steps.  Args:     conv_id(str): Conversation id  Returns:     List[GptsPlan]: List of planning steps`complete_task(self,conv_id,task_num,result)` 为同步对外方法。说明：Set the planning step to complete.  Args:     conv_id(str): conversation id     task_num(int): Planning step num     result(str): Plan step results`update_task(self,conv_id,task_num,state,retry_times,agent,model,result)` 为同步对外方法。说明：Update planning step information.  Args:     conv_id(str): conversation id     task_num(int): Planning step num     state(str): the status to update to     retry_times(int): Latest number of retries     agent(str): Agent's name     model(str): Model name     result(str): Plan step results`remove_by_conv_id(self,conv_id)` 为同步对外方法。说明：Remove plan by conversation id.  Args:     conv_id(str): conversation id在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsMessageMemory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/memory/gpts/base.py` 第 194 行。其所在子系统的定位是：记忆子系统，含短期、长期与 GPTs 会话记忆。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Gpts message memory interface.方法设计如下。`append(self,message)` 为同步对外方法。说明：Add a message.  Args:     message(GptsMessage): Message object`get_by_agent(self,conv_id,agent)` 为同步对外方法。说明：Return all messages of the agent in the conversation.  Args:     conv_id(str): Conversation id     agent(str): Agent's name  Returns:     List[GptsMessage]: List of messages`get_between_agents(self,conv_id,agent1,agent2,current_goal)` 为同步对外方法。说明：Get messages between two agents.  Query information related to an agent  Args:     conv_id(str): Conversation id     agent1(str): Agent1's name     agent2(str): Agent2's name     current_goal(str): Current goal  Returns:     List[GptsMessage]: List of messages`get_by_conv_id(self,conv_id)` 为同步对外方法。说明：Return all messages in the conversation.  Query messages by conv id.  Args:     conv_id(str): Conversation id Returns:     List[GptsMessage]: List of messages`get_last_message(self,conv_id)` 为同步对外方法。说明：Return the last message in the conversation.  Args:     conv_id(str): Conversation id  Returns:     GptsMessage: The last message in the conversation`delete_by_conv_id(self,conv_id)` 为同步对外方法。说明：Delete messages by conversation id.  Args:     conv_id(str): Conversation id在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `BaseAgentOperator`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 39 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The abstract operator for an Agent.方法设计如下。`__init__(self,agent)` 为同步对外方法。说明：Create an AgentOperator.`agent(self)` 为同步对外方法。说明：Return the Agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WrappedAgentOperator`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 56 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The Agent operator.  Wrap the agent and trigger the agent to generate a reply.方法设计如下。`__init__(self,agent)` 为同步对外方法。说明：Create an WrappedAgentOperator.`map(self,input_value)` 为异步对外方法。说明：Trigger agent to generate a reply.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELAgentOperator`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 137 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The Agent operator for AWEL.方法设计如下。`__init__(self,awel_agent)` 为同步对外方法。说明：Create an AgentOperator.`map(self,input_value)` 为异步对外方法。说明：Trigger agent to generate a reply.`get_agent(self,input_value)` 为异步对外方法。说明：Build the agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentDummyTrigger`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 324 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Http trigger for AWEL.  Http trigger is used to trigger a DAG by http request.方法设计如下。`__init__(self)` 为同步对外方法。说明：Initialize a HttpTrigger.`trigger(self)` 为异步对外方法。说明：Trigger the DAG. Not used in HttpTrigger.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentBranchOperator`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 360 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The intent detection branch operator.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create the intent detection branch operator.`branches(self)` 为异步对外方法。说明：Branch the intent detection result to different tasks.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentBranchJoinOperator`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator.py` 第 422 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The LLM Branch Join Operator.  Decide which output to keep(streaming or non-streaming).方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new LLM branch join operator.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELAgentResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 75 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AWEL Agent Resource.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre fill the agent ResourceType.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELAgentKnowledgeResource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 147 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AWELAgentKnowledgeResource.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre fill the agent ResourceType.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentPrompt`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 191 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent Prompt resource.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre fill the agent ResourceType.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELAgentConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 246 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AWEL Agent Config.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Prefill the agent ResourceType.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELAgent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 327 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AWEL Agent.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre fill the agent ResourceType.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELTeamContext`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/team_awel_layout.py` 第 27 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The context of the team for the AWEL layout.方法设计如下。`to_dict(self)` 为同步对外方法。说明：Convert the object to a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AWELBaseManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/team_awel_layout.py` 第 83 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AWEL base manager.方法设计如下。`receive(self,message,sender,reviewer,request_reply,silent,is_recovery,is_retry_chat,last_speaker_name,historical_dialogues,rely_messages)` 为异步对外方法。说明：Recive message by base team.`get_dag(self)` 为同步对外方法。说明：Get the DAG of the manager.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `WrappedAWELLayoutManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/team_awel_layout.py` 第 214 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The manager of the team for the AWEL layout.  Receives a DAG or builds a DAG from the agents.方法设计如下。`get_dag(self)` 为同步对外方法。说明：Get the DAG of the manager.`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。说明：Perform the action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DefaultAWELLayoutManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/team_awel_layout.py` 第 318 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The manager of the team for the AWEL layout.方法设计如下。`check_dag(cls,value)` 为同步对外方法。说明：Check the DAG of the manager.`get_dag(self)` 为同步对外方法。说明：Get the DAG of the manager.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `NotificationRecord`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/notification.py` 第 17 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `TaskNotificationManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/notification.py` 第 28 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,connector_manager)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`notify_execution_result(self,task_id,task_name,result)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`notify_rollback(self,task_id,task_name,rollback_result)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`get_notification_history(self,task_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorToolArgSummary`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 33 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Per-argument schema for a single connector tool.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorToolSummary`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 41 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Lightweight, prompt-friendly summary of a single connector tool.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorStatus`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 76 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Status of a live connector instance.  Attributes:     active: Connector is running and tools are available.     error: Connector failed to start or encountered a fatal error.     disconnected: Connector was explicitly removed or gracefully stopped.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 104 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Core manager that aggregates catalog, credential store and confirmation system.  ConnectorManager is a ``BaseComponent`` that owns:  * a :class:`~.catalog.ConnectorCatalog` with available connector types, * a :class:`~.credential.CredentialStore` for encrypted credential handling, * a :class:`~.confirmation.ConfirmationInterceptor` and   :class:`~.confirmation.ConfirmationRegistry` for human-in-the-loop flows, * a live registry of :class:`~dbgpt.agent.resource.tool.pack.MCPToolPack`   instances方法设计如下。`__init__(self,system_app)` 为同步对外方法。说明：Initialise internal sub-components.  Args:     system_app (Optional[SystemApp]): The system application instance.         If provided, ``init_app`` is called immediately.`is_custom_connector_type(connector_type)` 为同步对外方法。说明：Return True iff *connector_type* is a custom (user-defined) MCP type.  A custom connector type is identified by a non-empty ``custom_`` prefix followed by a non-empty suffix, e.g. ``"custom_mcp"``, ``"custom_arxiv"``. The bare prefix ``"custom_"`` and non-string inputs return False.  Args:     conne`_has_multiple_instances_of_type(self,connector_type)` 为同步内部方法。说明：True if at least one other connector of the same type is already active.`compute_tool_prefix(self,connector_type,display_name,connector_id)` 为同步对外方法。说明：Return the connector identifier portion (between the two ``__``).  Tool name format: ``mcp__{prefix}__{original_tool_name}``.  Prefix rules (the ``{prefix}`` portion only): - Built-in, single instance: ``{connector_type}`` - Built-in, multiple instances: ``{connector_type}-{slug(display_name)}`` - C`_apply_tool_prefix(self,pack,prefix,display_name)` 为同步内部方法。说明：Prefix every tool in *pack* and update ``tool_server_map``.  Renames each tool to ``mcp__{prefix}__{original_name}``. Format chosen to mirror Claude Code's MCP tool naming convention (``mcp__<server>__<tool>``): - ``mcp__`` prefix makes the tool's MCP origin obvious to the LLM and ops. - Double-unde`init_app(self,system_app)` 为同步对外方法。说明：Bind the manager to *system_app*.  Args:     system_app (SystemApp): The host system application.`get_catalog(self)` 为同步对外方法。说明：Public accessor for the catalog (used by /types endpoint).  Returns:     ConnectorCatalog: The loaded connector catalog.`load_catalog(self,path)` 为同步对外方法。说明：Load connector type definitions from a *catalog.json* file.  Args:     path (str): Absolute or relative path to ``catalog.json``.  Raises:     FileNotFoundError: When *path* does not exist.     ValueError: When the JSON is malformed or lacks a ``connectors`` list.`create_connector(self,connector_type,credentials,name,extra_config,connector_id)` 为异步对外方法。说明：Validate, encrypt and activate a new connector instance.  Two branches are supported:  * **Built-in catalog connector** — ``connector_type`` must exist in the   loaded catalog (e.g. ``"feishu"``, ``"github"``).  Header mapping is   taken from the catalog entry.  ``extra_config`` **must** supply   ```remove_connector(self,connector_id)` 为异步对外方法。说明：Remove a live connector instance.  Args:     connector_id (str): The id previously returned by         :meth:`create_connector`.`get_connector_tools(self,connector_id)` 为同步对外方法。说明：Return the :class:`.MCPToolPack` for a connector.  Args:     connector_id (str): Connector instance id.  Returns:     Optional[MCPToolPack]: The pack, or ``None`` when not found /     not active.`get_all_tools(self)` 为同步对外方法。说明：Return all active :class:`.MCPToolPack` instances.  Returns:     List[MCPToolPack]: Snapshot of all currently active packs.`_tool_summary_for(self,connector_id)` 为同步内部方法。说明：Return the per-tool summary list for one active connector.  Args:     connector_id (str): Connector instance id.  Returns:     Optional[List[ConnectorToolSummary]]: A list of structurally-shaped     tool summaries when *connector_id* is in ``_active_packs``;     ``None`` otherwise. Shape matches the`list_active(self)` 为同步对外方法。说明：Return prompt-ready summaries for all active connectors.  Returns:     List[Dict[str, Any]]: Lightweight connector metadata suitable for     prompt composition or UI display.`get_confirmation_interceptor(self)` 为同步对外方法。说明：Return the shared :class:`~.confirmation.ConfirmationInterceptor`.  Returns:     ConfirmationInterceptor: The interceptor instance.`get_confirmation_registry(self)` 为同步对外方法。说明：Return the shared :class:`~.confirmation.ConfirmationRegistry`.  Returns:     ConfirmationRegistry: The registry instance.`get_status(self,connector_id)` 为同步对外方法。说明：Return the status of a connector instance.  Args:     connector_id (str): Connector instance id.  Returns:     Optional[ConnectorStatus]: Current status, or ``None`` when the     connector_id is unknown.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `McpServerConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py` 第 13 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AuthField`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py` 第 18 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AuthConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py` 第 25 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorCatalogEntry`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py` 第 31 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorCatalog`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/catalog.py` 第 48 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`load(self,path)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get(self,connector_type)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list_by_category(self,category)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PendingConfirmation`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/confirmation.py` 第 16 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConfirmationRegistry`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/confirmation.py` 第 23 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`register(self,confirm_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resolve(self,confirm_id,approved)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`wait_for(self,confirm_id,timeout)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`cleanup_expired(self,max_age)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConfirmationInterceptor`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/confirmation.py` 第 63 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,catalog)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`should_confirm(self,tool_name,tool_args,context)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`format_confirmation(self,tool_name,tool_args,confirm_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_summarize_args(self,tool_args)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `TaskExecutionResult`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/task_executor.py` 第 17 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ConnectorTaskExecutor`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/task_executor.py` 第 40 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,connector_manager)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`execute_scheduled_task(self,task_id,connector_id,tool_name,tool_args)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RollbackResult`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/rollback.py` 第 22 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `TaskRollbackManager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/rollback.py` 第 28 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`is_rollbackable(self,tool_name,connector_type)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`execute_rollback(self,task_id,tool_name,tool_args,execution_result)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `CredentialStore`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/credential.py` 第 14 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_resolve_master_key(self)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_build_fernet(self,key)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`generate_salt(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`encrypt(self,credentials,salt)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`decrypt(self,encrypted,salt)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolPack`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 79 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool resource pack class.方法设计如下。`__init__(self,resources,name)` 为同步对外方法。说明：Initialize the tool resource pack.`from_resource(cls,resource,expected_type)` 为同步对外方法。说明：Create a resource from another resource.`add_command(self,command_label,command_name,args,function,parse_execute_args_func,overwrite)` 为同步对外方法。说明：Add a command to the commands.  Compatible with the Auto-GPT old plugin system.  Add a command to the commands list with a label, name, and optional arguments.  Args:     command_label (str): The label of the command.     command_name (str): The name of the command.     args (dict, optional): A dict`_get_execution_tool(self,name)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_get_call_args(self,arguments,tl)` 为同步内部方法。说明：Get the call arguments.`parse_execute_args(self,resource_name,input_str)` 为同步对外方法。说明：Parse the execute arguments.`execute(self)` 为同步对外方法。说明：Execute the tool.  Args:     *args: The positional arguments.     resource_name (str, optional): The tool name to be executed.     **kwargs: The keyword arguments.  Returns:     Any: The result of the tool execution.`async_execute(self)` 为异步对外方法。说明：Execute the tool asynchronously.  Args:     *args: The positional arguments.     resource_name (str, optional): The tool name to be executed.     **kwargs: The keyword arguments.  Returns:     Any: The result of the tool execution.`is_terminal(self,resource_name)` 为同步对外方法。说明：Check if the tool is terminal.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AutoGPTPluginToolPack`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 255 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Auto-GPT plugin tool pack class.方法设计如下。`__init__(self,plugin_path)` 为同步对外方法。说明：Create an Auto-GPT plugin tool pack.`preload_resource(self)` 为异步对外方法。说明：Preload the resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MCPToolPack`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 291 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：MCP tool pack class.  Wrap the MCP SSE server as a tool pack.  Example:     .. code-block:: python          tools = MCPToolPack("http://127.0.0.1:8000/sse")      If you want to pass the token to the server, you can use the headers parameter:     .. code-block:: python          tools = MCPToolPack(             "http://127.0.0.1:8000/sse"             default_headers={"Authorization": "Bearer your_token"}         )         # Set the default headers for ech server         tools2 = MCPToolPack(方法设计如下。`__init__(self,mcp_servers,headers,default_headers,ssl_verify,default_ssl_verify,default_ssl_cafile,overwrite_same_tool,transport)` 为同步对外方法。说明：Create an Auto-GPT plugin tool pack.`switch_mcp_input_schema(self,input_schema)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`preload_resource(self)` 为异步对外方法。说明：Preload the resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolException`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/exceptions.py` 第 4 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Common tool error exception.方法设计如下。`__init__(self,message,error_type)` 为同步对外方法。说明：Create a new ToolException instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `CreateToolException`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/exceptions.py` 第 14 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create tool error exception.方法设计如下。`__init__(self,message,error_type)` 为同步对外方法。说明：Create a new CreateToolException instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolNotFoundException`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/exceptions.py` 第 22 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool not found exception.方法设计如下。`__init__(self,message,error_type)` 为同步对外方法。说明：Create a new ToolNotFoundException instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolExecutionException`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/exceptions.py` 第 30 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool execution error exception.方法设计如下。`__init__(self,message,error_type)` 为同步对外方法。说明：Create a new ToolExecutionException instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolResourceParameters`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 29 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Tool resource parameters class.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ToolParameter`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 35 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parameter for a tool.方法设计如下。`pre_fill(cls,values)` 为同步对外方法。说明：Pre-fill the model.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `BaseTool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 64 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Base class for a tool.方法设计如下。`type(cls)` 为同步对外方法。说明：Return the resource type.`description(self)` 为同步对外方法。说明：Return the description of the tool.`args(self)` 为同步对外方法。说明：Return the arguments of the tool.`get_prompt(self)` 为异步对外方法。说明：Get the prompt.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `FunctionTool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 140 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Function tool.  Wrap a function as a tool.方法设计如下。`__init__(self,name,func,description,args,args_schema,parse_execute_args_func)` 为同步对外方法。说明：Create a tool from a function.`name(self)` 为同步对外方法。说明：Return the name of the tool.`description(self)` 为同步对外方法。说明：Return the description of the tool.`args(self)` 为同步对外方法。说明：Return the arguments of the tool.`is_async(self)` 为同步对外方法。说明：Return whether the tool is asynchronous.`parse_execute_args(self,resource_name,input_str)` 为同步对外方法。说明：Parse the execute arguments.`execute(self)` 为同步对外方法。说明：Execute the tool.  Args:     *args: The positional arguments.     resource_name (str, optional): The tool name to be executed(not used for     specific tool).     **kwargs: The keyword arguments.`async_execute(self)` 为异步对外方法。说明：Execute the tool asynchronously.  Args:     *args: The positional arguments.     resource_name (str, optional): The tool name to be executed(not used for     specific tool).     **kwargs: The keyword arguments.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMStrategyType`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm.py` 第 40 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：LLM strategy type.方法设计如下。`__new__(cls,value,name_cn,description,description_en)` 为同步内部方法。说明：Overide new.`to_dict(self)` 为同步对外方法。说明：To dict.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMStrategy`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm.py` 第 72 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：LLM strategy base class.方法设计如下。`__init__(self,llm_client,context)` 为同步对外方法。说明：Create an LLMStrategy instance.`type(self)` 为同步对外方法。说明：Return the strategy type.`_excluded_models(self,all_models,excluded_models,need_uses)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`next_llm(self,excluded_models)` 为异步对外方法。说明：Return next available llm model name.  Args:     excluded_models(List[str]): excluded models  Returns:     str: Next available llm model name在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMConfig`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm.py` 第 133 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：LLM configuration.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AIWrapper`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm_client.py` 第 20 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AIWrapper for LLM.方法设计如下。`__init__(self,llm_client,output_parser)` 为同步对外方法。说明：Create an AIWrapper instance.`instantiate(cls,template,context,allow_format_str_template)` 为同步对外方法。说明：Instantiate the template with the context.`_construct_create_params(self,create_config,extra_kwargs)` 为同步内部方法。说明：Prime the create_config with additional_kwargs.`_separate_create_config(self,config)` 为同步内部方法。说明：Separate the config into create_config and extra_kwargs.`_get_key(self,config)` 为同步内部方法。说明：Get a unique identifier of a configuration.  Args:     config (dict or list): A configuration.  Returns:     tuple: A unique identifier which can be used as a key for a dict.`create(self,verbose)` 为异步对外方法。说明：Create llm client request and return the generated text.`create_with_output(self,verbose)` 为异步对外方法。说明：Like :meth:`create` but return the raw :class:`ModelOutput`.  Native function calling needs the ``tool_calls`` attached to the model output, which :meth:`create` collapses into plain text. Call this in native mode to retain ``tool_calls``.`_create_raw(self,verbose,config)` 为异步内部方法。说明：Run the LLM request and return the raw ModelOutput (or None).`generate_text(self,prompt,llm_model,max_new_tokens,temperature,conv_id)` 为异步对外方法。说明：Generate text from a single prompt.  A thin convenience wrapper around :meth:`create` for simple text generation use cases such as context summarization (Layer 3 compaction) where a full message list is unnecessary.  Args:     prompt: The user prompt to send to the model.     llm_model: Model name.`_get_span_metadata(self,payload)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_llm_messages_convert(self,params)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_completions_create(self,llm_model,params,conv_id,sender,memory,stream_out,verbose,stream_callback)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LLMStrategyPriority`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/strategy/priority.py` 第 12 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Priority strategy for llm model service.方法设计如下。`type(self)` 为同步对外方法。说明：Return the strategy type.`next_llm(self,excluded_models)` 为异步对外方法。说明：Return next available llm model name.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PagenationFilter`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 11 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PagenationResult`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 17 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dic(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `NativeTeamContext`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 38 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`parse_show_disable(cls,value)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubFilter`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 89 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MyPluginFilter`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 101 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubParam`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/model.py` 第 111 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Serve`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/serve.py` 第 22 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Serve component for DB-GPT方法设计如下。`__init__(self,system_app,config,api_prefix,api_tags,db_url_or_db,try_create_tables)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`init_app(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`on_init(self)` 为同步对外方法。说明：Called when init the application.  You can do some initialization here. You can't get other components here because they may be not initialized yet`before_start(self)` 为同步对外方法。说明：Called before the start of the application.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ServeConfig`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/config.py` 第 15 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parameters for the serve command该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DatasourceDBParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/datasource.py` 第 42 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The DB parameters for the datasource.方法设计如下。`_resource_version(cls)` 为同步内部方法。说明：Return the resource version.`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.`from_dict(cls,data,ignore_extra_fields)` 为同步对外方法。说明：Create a new instance from a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DatasourceResource`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/datasource.py` 第 125 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,name,db_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_schema_link(self,db,question)` 为同步对外方法。说明：Return the schema link of the database.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_DynDBParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/datasource.py` 第 143 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptAppResource`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/app.py` 第 11 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AppResource resource class.方法设计如下。`__init__(self,name,app_code)` 为同步对外方法。说明：Initialize AppResource resource.`app_desc(self)` 为同步对外方法。说明：Return the app description.`app_name(self)` 为同步对外方法。说明：Return the app name.`_get_app_list(cls)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_start_app(self,user_input,sender,conv_uid)` 为异步内部方法。说明：Start App By AppResource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MCPPackResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py` 第 13 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`_resource_version(cls)` 为同步内部方法。说明：Return the resource version.`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.`from_dict(cls,data,ignore_extra_fields)` 为同步对外方法。说明：Create a new instance from a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MCPSSEToolPack`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py` 第 50 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,mcp_servers)` 为同步对外方法。说明：Initialize the MCPSSEToolPack with the given MCP servers.`type_alias(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_DynMCPSSEPackResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/mcp.py` 第 111 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `KnowledgeSpaceLoadResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/knowledge.py` 第 18 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`_resource_version(cls)` 为同步内部方法。说明：Return the resource version.`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.`from_dict(cls,data,ignore_extra_fields)` 为同步对外方法。说明：Create a new instance from a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `KnowledgeSpaceRetrieverResource`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/knowledge.py` 第 62 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Knowledge Space retriever resource.方法设计如下。`__init__(self,name,space_name,top_k,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`retriever_name(self)` 为同步对外方法。说明：Return the resource name.`retriever_desc(self)` 为同步对外方法。说明：Return the retriever desc.`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_DynamicKnowledgeSpaceLoadResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/knowledge.py` 第 115 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginPackResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/plugin.py` 第 18 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`_resource_version(cls)` 为同步内部方法。说明：Return the resource version.`to_configurations(cls,parameters,version)` 为同步对外方法。说明：Convert the parameters to configurations.`from_dict(cls,data,ignore_extra_fields)` 为同步对外方法。说明：Create a new instance from a dictionary.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginToolPack`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/plugin.py` 第 57 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,tool_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`type_alias(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_parameters_class(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`preload_resource(self)` 为异步对外方法。说明：Preload the resource.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `_DynPluginPackResourceParameters`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/plugin.py` 第 81 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentOutputOperator`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/evaluation/evaluation.py` 第 26 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,app_code)` 为同步对外方法。说明：Args:     space_name (str): The space name.     recall_score (Optional[float], optional): The recall score. Defaults to 0.3.`map(self,input_value)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentEvaluatorOperator`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/evaluation/evaluation.py` 第 72 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Evaluator for retriever.方法设计如下。`__init__(self,evaluation_metrics,llm_client)` 为同步对外方法。说明：Create a new RetrieverEvaluatorOperator.`_do_evaluation(self,query,prediction_result,contexts,raw_dataset)` 为异步内部方法。说明：Run evaluation.  Args:     query(str): The query string.     prediction(List[str]): The retrieved chunks from the retriever.     contexts(List[str]): The contexts from dataset.     raw_dataset(Any): The raw data(single row) from dataset.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AgentEvaluator`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/evaluation/evaluation.py` 第 127 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Evaluator for relevancy.  Examples:     .. code-block:: python          import os         import asyncio         from dbgpt.rag.operators import (             EmbeddingRetrieverOperator,             RetrieverEvaluatorOperator,         )         from dbgpt.rag.evaluation import (             RetrieverEvaluator,             RetrieverSimilarityMetric,         )         from dbgpt.rag.embedding import DefaultEmbeddingFactory         from dbgpt_ext.storage.vector_store.chroma_store import ChromaVecto方法设计如下。`__init__(self,operator_cls,llm_client,embeddings,operator_kwargs)` 为同步对外方法。说明：Create a new RetrieverEvaluator.`evaluate(self,dataset,metrics,query_key,contexts_key,prediction_key,parallel_num)` 为异步对外方法。说明：Evaluate the dataset.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppLinkMetric`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/evaluation/evaluation_metric.py` 第 21 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Intent evaluation  metric. Hit rate calculates the fraction of queries where the correct answer is found within the top-k retrieved documents. In simpler terms, it’s about how often our system gets it right within the top few guesses.方法设计如下。`describe(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`sync_compute(self,prediction,contexts,query)` 为同步对外方法。说明：Compute Intent metric. Args:     prediction(Optional[str]): The retrieved chunks from the retriever.     contexts(Optional[str]): The contexts from dataset.     query:(Optional[str]) The query text. Returns:     BaseEvaluationResult: The evaluation result.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IntentMetric`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/evaluation/evaluation_metric.py` 第 73 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Intent evaluation  metric. Hit rate calculates the fraction of queries where the correct answer is found within the top-k retrieved documents. In simpler terms, it’s about how often our system gets it right within the top few guesses.方法设计如下。`describe(cls)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`sync_compute(self,prediction,contexts,query)` 为同步对外方法。说明：Compute Intent metric. Args:     prediction(Optional[str]): The retrieved chunks from the retriever.     contexts(Optional[str]): The contexts from dataset.     query:(Optional[str]) The query text. Returns:     BaseEvaluationResult: The evaluation result.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `TeamMode`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/team/base.py` 第 8 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__new__(cls,value,name_cn,name_en,description,description_en,remark,remark_en)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MultiAgents`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/controller.py` 第 85 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`init_app(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`__init__(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`on_init(self)` 为同步对外方法。说明：Called when init the application.  Import your own module here to ensure the module is loaded before the application starts`after_start(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_dbgpts(self,user_code,sys_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_app(self,app_code)` 为同步对外方法。说明：get app`get_or_build_agent_memory(self,conv_id,dbgpts_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`agent_chat_v2(self,conv_id,new_order,gpts_name,user_query,user_code,sys_code,enable_verbose,stream)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`is_flow_chat(self,gpts_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`app_agent_flow_chat(self,conv_uid,gpts_name,user_query,user_code,sys_code,enable_verbose,stream)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`app_agent_chat(self,conv_uid,gpts_name,user_query,user_code,sys_code,enable_verbose,stream)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`agent_team_chat_new(self,user_query,conv_uid,gpts_app,agent_memory,is_retry_chat,last_speaker_name,init_message_rounds,link_sender,app_link_start,enable_verbose,historical_dialogues,rely_messages)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`chat_messages(self,conv_id,user_code,system_app)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`stable_message(self,conv_id,user_code,system_app)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`gpts_conv_list(self,user_code,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`topic_terminate(self,conv_id)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`get_knowledge_resources(self,app_code,question)` 为异步对外方法。说明：Get the knowledge resources.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppManager`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/app_agent_manage.py` 第 38 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`init_app(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_dbgpts(self,user_code,sys_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_app(self,app_code)` 为同步对外方法。说明：get app`user_chat_2_app(self,user_query,conv_uid,gpts_app,agent_memory,is_retry_chat,last_speaker_name,init_message_rounds,enable_verbose)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`create_app_agent(self,gpts_app,agent_memory,context)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`create_agent_by_app_code(self,gpts_app,conv_uid,agent_memory,context)` 为异步对外方法。说明：Create a conversable agent by application code.  Parameters:     gpts_app (str): The application.     conv_uid (str, optional): The unique identifier of the conversation,         default is None. If not provided, a new UUID will be generated.     agent_memory (AgentMemory, optional): The memory obje在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DbGptsMessage`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/dbgpts.py` 第 9 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`from_dict(d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DbGptsTaskStep`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/dbgpts.py` 第 30 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`from_dict(d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `DbGptsCompletion`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/dbgpts.py` 第 54 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`from_dict(d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MetaDbGptsPlansMemory`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/db_gpts_memory.py` 第 15 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`batch_save(self,plans)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id_and_num(self,conv_id,task_nums)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_todo_plans(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`complete_task(self,conv_id,task_num,result)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`update_task(self,conv_id,task_num,state,retry_times,agent,model,result)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`remove_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MetaDbGptsMessageMemory`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/db_gpts_memory.py` 第 78 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`append(self,message)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_agent(self,conv_id,agent)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_between_agents(self,conv_id,agent1,agent2,current_goal)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_last_message(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsConversationsEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_conversations_db.py` 第 18 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsConversationsDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_conversations_db.py` 第 55 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`add(self,engity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_like_conv_id_asc(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_convs(self,user_code,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`update(self,conv_id,state)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete_chat_message(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsMessagesEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_messages_db.py` 第 29 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsMessagesDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_messages_db.py` 第 95 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`append(self,entity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_agent(self,conv_id,agent)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_between_agents(self,conv_id,agent1,agent2,current_goal)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_last_message(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete_chat_message(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsPlansEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_plans_db.py` 第 9 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsPlansDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_plans_db.py` 第 49 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`batch_save(self,plans)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_task_id(self,task_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_conv_id_and_num(self,conv_id,task_nums)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_todo_plans(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`complete_task(self,conv_id,task_num,result)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`update_task(self,conv_id,task_num,state,retry_times,agent,model,result)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`remove_by_conv_id(self,conv_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppDetail`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 45 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_serialize(self,value)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`from_dict(cls,d,parse_llm_strategy)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`from_entity(cls,entity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsApp`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 111 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_serialize(self,value)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`from_dict(cls,d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`pre_fill(cls,values)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppQuery`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 191 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppResponse`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 203 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppCollection`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 212 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_serialize(self,value)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`from_dict(cls,d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `UserRecentApps`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 241 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`from_dict(cls,d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `UserRecentAppsEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 261 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppCollectionEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 282 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 297 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppDetailEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 337 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_serialize(self,value)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `UserRecentAppsDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 380 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`query(self,user_code,sys_code,app_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`upsert(self,user_code,sys_code,app_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppCollectionDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 472 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`collect(self,app_code,user_code,sys_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`uncollect(self,app_code,user_code,sys_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list(self,query)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `GptsAppDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 536 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`list_all(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list_hot_apps(self,query)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`app_list(self,query,parse_llm_strategy)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_entity_to_app_dict(self,app_info,app_details,hot_app_map,app_collects,parse_llm_strategy,owner_name,owner_avatar_url,recommend_questions)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_group_app_details(self,app_codes,session)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`_group_app_questions(self,app_codes,session)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`native_app_detail(self,app_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`app_detail(self,app_code,user_code,sys_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete(self,app_code,user_code,sys_code)` 为同步对外方法。说明：To delete the application, you also need to delete the corresponding plug-ins and collections.`remove_native_app(self,app_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`create(self,gpts_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`edit(self,gpts_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`update_admins(self,app_code,user_nos)` 为同步对外方法。说明：update admins by app_code`get_admins(self,app_code)` 为同步对外方法。说明：get admins by app_code`publish(self,app_code,user_code,sys_code)` 为同步对外方法。说明：To publish the application so that other users and access it.`unpublish(self,app_code,user_code,sys_code)` 为同步对外方法。说明：To publish the application so that other users and access it.`init_native_apps(self,user_code)` 为同步对外方法。说明："Chat Knowledge", "Chat DB", "Chat Data", "Professional DBA", "Dashboard", "Chat Excel" "chat_knowledge", "chat_with_db_qa", "chat_with_db_execute", "chat_dba", "chat_dashboard", "chat_excel"在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppParamType`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1463 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ModulePlugin`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 28 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`init_app(self,system_app)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`refresh_plugins(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHub`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/plugin_hub.py` 第 26 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self,plugin_dir)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_validate_plugin_code(self,file_path)` 为同步内部方法。说明：Validate plugin code for potentially malicious operations.  Args:     file_path: Path to the Python file to validate  Returns:     bool: True if the code is safe, raises an exception otherwise`install_plugin(self,plugin_name,user_name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`uninstall_plugin(self,plugin_name,user)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`__download_from_git(self,github_repo,branch_name,authorization)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`__build_my_plugin(self,hub_plugin)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`refresh_hub_from_git(self,github_repo,branch_name,authorization)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_sanitize_filename(self,filename)` 为同步内部方法。说明：Sanitize the filename to prevent directory traversal attacks.  Args:     filename: The original filename  Returns:     str: Sanitized filename`_validate_extracted_files(self,directory)` 为同步内部方法。说明：Validate all Python files in the extracted plugin directory.  Args:     directory: The directory containing extracted plugin files  Returns:     bool: True if all files are safe, raises an exception otherwise`upload_my_plugin(self,doc_file,user)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`reload_my_plugins(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_my_plugin(self,user)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ServeEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/models/models.py` 第 16 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__repr__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ServeDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/models/models.py` 第 32 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The DAO class for Agent/chat方法设计如下。`__init__(self,serve_config)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`from_request(self,request)` 为同步对外方法。说明：Convert the request to an entity  Args:     request (Union[ServeRequest, Dict[str, Any]]): The request  Returns:     T: The entity`to_request(self,entity)` 为同步对外方法。说明：Convert the entity to a request  Args:     entity (T): The entity  Returns:     REQ: The request`to_response(self,entity)` 为同步对外方法。说明：Convert the entity to a response  Args:     entity (T): The entity  Returns:     RES: The response在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ServeRequest`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/schemas.py` 第 7 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent/chat request model该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ServerResponse`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/schemas.py` 第 15 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Agent/chat response model该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `Service`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/service/service.py` 第 13 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：The service class for Agent/chat方法设计如下。`__init__(self,system_app,config,dao)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`init_app(self,system_app)` 为同步对外方法。说明：Initialize the service  Args:     system_app (SystemApp): The system app`dao(self)` 为同步对外方法。说明：Returns the internal DAO.`config(self)` 为同步对外方法。说明：Returns the internal ServeConfig.`update(self,request)` 为同步对外方法。说明：Update a Agent/chat entity  Args:     request (ServeRequest): The request  Returns:     ServerResponse: The response`get(self,request)` 为同步对外方法。说明：Get a Agent/chat entity  Args:     request (ServeRequest): The request  Returns:     ServerResponse: The response`delete(self,request)` 为同步对外方法。说明：Delete a Agent/chat entity  Args:     request (ServeRequest): The request`get_list(self,request)` 为同步对外方法。说明：Get a list of Agent/chat entities  Args:     request (ServeRequest): The request  Returns:     List[ServerResponse]: The response`get_list_by_page(self,request,page,page_size)` 为同步对外方法。说明：Get a list of Agent/chat entities by page  Args:     request (ServeRequest): The request     page (int): The page number     page_size (int): The page size  Returns:     List[ServerResponse]: The response在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RecommendQuestion`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/recommend_question.py` 第 11 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`from_dict(cls,d)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`from_entity(cls,entity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RecommendQuestionEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/recommend_question.py` 第 64 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `RecommendQuestionDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/recommend_question.py` 第 93 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`list_questions(self,rq)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`create(self,recommend_question)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`update_question(self,recommend_question)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete_question(self,recommend_question)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`delete_by_app_code(self,app_code)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReportGenerationAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/report_generation_agent.py` 第 14 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Report Generation Agent.  This agent integrates all analysis results and generates structured reports in Markdown format, providing comprehensive insights on metric fluctuations and their root causes.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new ReportGenerationAgent instance.`build(self,is_retry_chat)` 为异步对外方法。说明：Build the agent.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AnomalyDetectionAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/anomaly_detection_agent.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Anomaly Detection Agent.  This agent is responsible for detecting anomalies in business metrics by comparing baseline and current period data, and determining if the metric fluctuations exceed predefined thresholds.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new AnomalyDetectionAgent instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IntentRecognitionAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/intent_recognition_agent.py` 第 89 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`load_resource(self,question,is_retry_chat)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `VolatilityAnalysisAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/volatility_analysis_agent.py` 第 13 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Volatility Analysis Agent.  This agent performs attribution analysis to identify the root causes of metric anomalies by analyzing multi-dimensional data and calculating contribution rates of each dimension.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new VolatilityAnalysisAgent instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MetricInfoAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/metric_info_agent.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责从向量库或索引中按相似度召回候选文档块，是 RAG 管线的召回阶段实现。作者注释或文档字符串指出：Metric Info Agent.  This agent is responsible for retrieving specific metric information by accessing knowledge base resources to obtain detailed information about business metrics.方法设计如下。`__init__(self)` 为同步对外方法。说明：Create a new MetricInfoAgent instance.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppResourceInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_resource_start_assisant_agent.py` 第 24 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Plugin input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppResourceAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_resource_start_assisant_agent.py` 第 40 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：AppResource action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：App action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the plugin action.  Args:     ai_message (str): The AI message.     resource (Optional[AgentResource], optional): The resource. Defaults to         None.     rely_action_out (Optional[ActionOutput], optional): The rely action output.         Defaults to None.     need_vis_render (bool, optio`__get_plugin_view(self,param,app_result,err_msg)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`__get_app_resource_list(self)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`__get_app_resource_of_app_name(self,app_name)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AppStarterAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_resource_start_assisant_agent.py` 第 175 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`desc(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LinkAppAssistantAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_link_assisant_agent.py` 第 10 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `StartAppAssistantAgent`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_start_assisant_agent.py` 第 19 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`prepare_act_param(self,received_message,sender,rely_messages)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_load_thinking_messages(self,received_message,sender,observation,rely_messages,historical_dialogues,context,is_retry_chat,current_retry_counter)` 为异步内部方法。该方法承担对象内部状态迁移或对外协作。`thinking(self,messages,sender,prompt)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`receive(self,message,sender,reviewer,request_reply,silent,is_recovery,is_retry_chat,last_speaker_name,historical_dialogues,rely_messages)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`adjust_final_message(self,is_success,reply_message)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。`act(self,message,sender,reviewer,is_retry_chat,last_speaker_name)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AnomalyDetectionInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/anomaly_detection_action.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Anomaly detection input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `AnomalyDetectionAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/anomaly_detection_action.py` 第 33 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Anomaly detection action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Anomaly detection action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the anomaly detection action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `VolatilityAnalysisInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/volatility_analysis_action.py` 第 16 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Volatility analysis input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `VolatilityAnalysisAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/volatility_analysis_action.py` 第 46 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Volatility analysis action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Volatility analysis action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the volatility analysis action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IntentRecognitionInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/intent_recognition_action.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_dict(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `IntentRecognitionAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/intent_recognition_action.py` 第 39 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_need(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`render_protocal(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`out_model_type(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`ai_out_schema(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`_get_default_next_speakers(self)` 为同步内部方法。该方法承担对象内部状态迁移或对外协作。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MetricInfoInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/metric_info_action.py` 第 13 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MetricInfoAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/metric_info_action.py` 第 41 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Metric info action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Metric info action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the metric info retrieval action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LinkAppInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/app_start_action.py` 第 16 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `StartAppAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/app_start_action.py` 第 31 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`render_protocal(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`out_model_type(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReportGenerationInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/report_generation_action.py` 第 14 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Report generation input model.该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `ReportGenerationAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/report_generation_action.py` 第 28 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Report generation action class.方法设计如下。`__init__(self)` 为同步对外方法。说明：Report generation action init.`resource_need(self)` 为同步对外方法。说明：Return the resource type needed for the action.`render_protocol(self)` 为同步对外方法。说明：Return the render protocol.`out_model_type(self)` 为同步对外方法。说明：Return the output model type.`ai_out_schema(self)` 为同步对外方法。说明：Return the AI output schema.`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。说明：Perform the report generation action.在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LinkAppInput`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/app_link_action.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `LinkAppAction`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/actions/app_link_action.py` 第 23 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`__init__(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`resource_need(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`render_protocal(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`out_model_type(self)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`run(self,ai_message,resource,rely_action_out,need_vis_render)` 为异步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubFilter`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/model/model.py` 第 8 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MyPluginFilter`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/model/model.py` 第 20 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubParam`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/model/model.py` 第 30 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubVO`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/model/model.py` 第 44 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MyPluginVO`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/model/model.py` 第 59 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该类当前没有在源码中解析出额外方法，可能以数据容器或协议接口为主。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/db/plugin_hub_db.py` 第 24 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_vo(cls,entities)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `PluginHubDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/db/plugin_hub_db.py` 第 70 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`add(self,engity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`raw_update(self,entity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list(self,query,page,page_size)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_storage_url(self,storage_url)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_name(self,name)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`count(self,query)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`raw_delete(self,plugin_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MyPluginEntity`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/db/my_plugin_db.py` 第 10 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`to_vo(cls,entities)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造对象名为 `MyPluginDao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/db/my_plugin_db.py` 第 55 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。方法设计如下。`add(self,engity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`raw_update(self,entity)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_user(self,user)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`get_by_user_and_plugin(self,user,plugin)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`list(self,query,page,page_size)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`count(self,query)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。`raw_delete(self,plugin_id)` 为同步对外方法。该方法承担对象内部状态迁移或对外协作。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。建议：为该类定义 2026 年的目标行为、观测指标与淘汰条件，避免无主模块。

该改造函数名为 `_build_openai_tools`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/tool_calling_agent.py` 第 53 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Build an OpenAI ``tools`` array from the agent's ToolPack.  Mirrors the JSON-Schema shape that ``BaseTool.get_prompt(prompt_type="openai")`` produces, but returns the raw ``tools`` array expected by t该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `find_excel_files`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py` 第 18 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：查找指定目录下所有.csv和.xlsx文件，并返回它们的绝对路径  参数:     directory: 要搜索的目录路径  返回:     包含所有.csv和.xlsx文件绝对路径的列表，如果目录不存在则返回空列表该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `read_excel_headers_and_data`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py` 第 261 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：读取Excel文件，返回表头信息和结构化数据（支持指定读取行数）  参数:     file_path: Excel文件路径（.xlsx格式）     read_rows: 可选，指定读取的数据行数（不含表头）。                - 默认为5：仅读取前5行数据                - 设为None或0：读取全部数据                - 设为正整数N：读取前N行该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `data2md`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/excel_table_agent.py` 第 325 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_new_system_message`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py` 第 1454 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return the system message.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_is_list_of_type`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_agent.py` 第 1459 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `participant_roles`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 16 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return a string listing the roles of the agents.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `mentioned_agents`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 25 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return a dictionary mapping agent names to mention counts.  Finds and counts agent mentions in the string message_content, taking word boundaries into account.  Returns: A dictionary mapping agent nam该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `initialize_agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 130 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Initialize the agent manager.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_agent_manager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 138 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return the agent manager.  Args:     system_app (Optional[SystemApp], optional): The system app. Defaults to None.  Returns:     AgentManager: The agent manager.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `scan_agents`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/agent_manage.py` 第 158 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Scan and register all agents.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_content_str`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/base_team.py` 第 15 行。其所在子系统的定位是：Agent 核心运行时，含可对话智能体、动作与角色。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Convert content into a string format.  This function processes content that may be a string, a list of mixed text and image URLs, or None, and converts it into a string. Text is directly appended to t该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `initialize_resource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/manage.py` 第 299 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Initialize the resource manager.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_resource_manager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/manage.py` 第 307 行。其所在子系统的定位是：Agent 资源层，统一知识、数据库、应用与技能资源。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return the resource manager.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_classify_text`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 53 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `classify_agent_error`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 62 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Classify an agent error into a recovery-relevant category.  Args:     cause: An exception (its ``str`` is inspected), a message string (e.g.         a tool failure reason), or ``None``.     default: T该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `is_retryable`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 89 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return True when the error category is worth retrying.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `should_failover_model`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 94 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return True when the error suggests trying a different model.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `format_fail_reason`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/agent_errors.py` 第 104 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Build a structured feedback string for the agent loop.  Keeps the existing ``fail_reason`` → ``observation`` feedback channel but tags it with the classified category so downstream logging / retry log该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `parse_conv_id`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/conv_utils.py` 第 5 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `cmp_string_equal`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/cmp.py` 第 6 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Compare two strings are equal or not.  Args:     a(str): The first string.     b(str): The second string.     ignore_case(bool): Ignore case or not.     ignore_punctuation(bool): Ignore punctuation or该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `remove_request_params`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/mcp_utils.py` 第 20 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `sse_client`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/mcp_utils.py` 第 25 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Client transport for SSE.  `sse_read_timeout` determines how long (in seconds) the client will wait for a new event before disconnecting. All other HTTP operations are controlled by `timeout`.  Yields该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_normalise_transport`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/mcp_utils.py` 第 214 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Lowercase + strip ``-``/``_`` separators so all variants collapse to one key.  ``"Streamable-HTTP"``, ``"streamable_http"`` and ``"streamableHttp"`` all map to ``"streamablehttp"``; ``"SSE"`` and ``"s该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `streamable_http_client`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/mcp_utils.py` 第 227 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Thin wrapper over ``mcp.client.streamable_http.streamablehttp_client``.  The official mcp client yields ``(read, write, get_session_id)``. We drop the session-id callback so the yield shape matches :f该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `mcp_transport_client`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/mcp_utils.py` 第 279 行。其所在子系统的定位是：Agent 工具，含 ReAct 解析、MCP、错误类型。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Dispatch to the right MCP client based on *transport*.  Args:     url: MCP server endpoint URL.     transport: ``"sse"`` (legacy HTTP+SSE) or ``"streamable_http"``         (MCP 2026-03-26 Streamable H该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_skills_agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/agent.py` 第 262 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a SkillsAgent with skills from specified sources.  Args:     skill_sources: List of skill source directories.     context: Agent context.     **kwargs: Additional arguments for SkillsAgent.  Re该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_validate_skill_name`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 100 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Validate skill name.  Requirements: - Max 64 characters - Lowercase alphanumeric and hyphens only (a-z, 0-9, -) - Cannot start or end with hyphen - No consecutive hyphens  Args:     name: Skill name t该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_skill_metadata`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 124 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse YAML frontmatter from SKILL.md content.  Args:     content: Content of the SKILL.md file.     skill_path: Path to the SKILL.md file.     directory_name: Name of the parent directory.  Returns:该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_list_skills_from_directory`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/middleware.py` 第 212 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：List all skills from a directory.  Args:     source_path: Path to the skills directory.  Returns:     List of skill metadata from successfully parsed SKILL.md files.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `initialize_skill`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/manage.py` 第 1126 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Initialize the skill manager.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_skill_manager`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/manage.py` 第 1134 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get the skill manager.  Args:     system_app: System app instance.  Returns:     SkillManager instance.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_registry`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 451 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get the global skill registry.  Returns:     SkillRegistry instance.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `load_skills_from_dir`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/__init__.py` 第 463 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Load skills from a directory into the global registry.  Args:     directory: Directory containing SKILL.md files.     recursive: Whether to search recursively.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_skill_agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/claude_skill/agent.py` 第 145 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a Claude Skill Agent with skills loaded from a directory.  Args:     skill_dir: Directory containing SKILL.md files.     context: Agent context.     **kwargs: Additional arguments for Conversab该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_middleware_agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/agent.py` 第 303 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a MiddlewareAgent.  Args:     profile: Profile configuration.     agent_config: Agent configuration.     agent_context: Agent context.     **kwargs: Additional arguments.  Returns:     Configur该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `example_with_skills`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/example.py` 第 15 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Example of using skills with MiddlewareAgent.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `example_custom_middleware`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/example.py` 第 51 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Example of using custom middleware.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `example_skills_middleware_direct`，定义于 `packages/dbgpt-core/src/dbgpt/agent/middleware/example.py` 第 83 行。其所在子系统的定位是：Agent 中间件。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Example of using SkillsMiddlewareV2 directly.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `run_tool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tool_action.py` 第 162 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Run the tool.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `run_tools_batch`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tool_action.py` 第 280 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Execute multiple tool calls concurrently and aggregate into one output.  A one-element batch is forwarded verbatim to :func:`run_tool`, so batch mode is a strict superset of the legacy single-tool beh该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `find_excel_files`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/insert_action.py` 第 22 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：查找指定目录下所有.csv和.xlsx文件，并返回它们的绝对路径  参数:     directory: 要搜索的目录路径  返回:     包含所有.csv和.xlsx文件绝对路径的列表，如果目录不存在则返回空列表该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `read_excel_headers_and_data`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/insert_action.py` 第 342 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：读取Excel文件，返回表头信息和结构化数据  参数:     file_path: Excel文件路径（.xlsx格式）  返回:     Tuple[表头列表, 数据列表]     - 表头列表: 从Excel第一行读取的列名     - 数据列表: 每个元素是一个字典，键为表头，值为对应单元格数据（空单元格转为None）该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `clean_text`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/websearch_action.py` 第 59 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：清洗文本：移除控制字符、非打印字符，保留纯文本该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_page_content`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/websearch_action.py` 第 74 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：获取指定URL页面的所有文本内容，处理编码并过滤非HTML内容，同时尽量保留原网页的文本格式。 支持中断功能：使用流式下载数据，并在每个块读取时检测 worker 的中断标志。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_bing_search_results`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/websearch_action.py` 第 154 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_current_host_cpu_status`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/host_tool.py` 第 7 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get current host CPU status.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_current_host_memory_status`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/host_tool.py` 第 26 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get current host memory status.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_current_host_system_load`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/host_tool.py` 第 40 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get current host system load.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `baidu_search`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/search_tool.py` 第 14 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Baidu search and return the results as a markdown string.  Please set number of results not less than 8 for rich search results.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_search_to_view`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/search_tool.py` 第 62 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `list_dbgpt_support_models`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/resources/dbgpt_tool.py` 第 9 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：List the supported models in dbgpt.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `add`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_tool_calling_agent.py` 第 20 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Add two numbers.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `multiply`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_tool_calling_agent.py` 第 26 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Multiply two numbers.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_make_agent`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_tool_calling_agent.py` 第 41 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_act_kwargs`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_tool_calling_agent.py` 第 54 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Simulate the kwargs generate_reply passes to act() via prepare_act_param.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_make_resource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_fallback_parse_args.py` 第 7 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `code_interpreter`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_fallback_parse_args.py` 第 13 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Execute Python code.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `html_interpreter`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_fallback_parse_args.py` 第 19 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Render HTML content as a web report.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `add`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_parallel_tool_action.py` 第 18 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Add two numbers.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `multiply`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_parallel_tool_action.py` 第 24 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Multiply two numbers.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `explode`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_parallel_tool_action.py` 第 30 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Always fails.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_pack`，定义于 `packages/dbgpt-core/src/dbgpt/agent/expand/actions/tests/test_parallel_tool_action.py` 第 35 行。其所在子系统的定位是：Agent 总成。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_is_observation_message`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 41 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Check if a message is an Observation (tool output) in ReAct format.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_is_system_message`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 50 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Check if a message has the SYSTEM role.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_detect_round_boundaries`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/compact.py` 第 55 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责解析语言模型的 Thought/Action/Observation 文本协议。作者注释或文档字符串指出：Detect ReAct triplet (Thought/Action/Observation) round boundaries.  Returns a list of rounds, where each round is a list of message indices belonging to that round. System messages are excluded.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `set_current_storage`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 68 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Bind a ToolResultStorage to the current async context.  Called by the agent before tool execution so ``run_tool`` can pick it up.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_current_storage`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 76 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return the ToolResultStorage bound to the current async context, if any.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `tool_result_budget_for_context_window`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 150 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return a ``ToolResultBudgetConfig`` scaled to the active model's context window.  The fixed defaults (100K result / 200K turn chars) are correct for large (200K+ token) models but blind to small ones:该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_safe_filename`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 189 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Return a single safe filename for a tool result id.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `generate_preview`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 207 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Truncate at last newline within *max_chars*. Returns (preview, has_more).该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_build_persisted_message`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/storage.py` 第 220 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Build the ``<persisted-output>`` replacement block.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_agent_resource_prompt_values`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 22 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_load_resource_types`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 34 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_agent_resource_knowledge_values`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 95 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_agent_resource_option_values`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/plan/awel/agent_operator_resource.py` 第 261 行。其所在子系统的定位是：规划子系统，包含 Planner、自动规划与 AWEL 团队布局。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_sys`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py` 第 11 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_human`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py` 第 15 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_ai`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py` 第 19 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_obs`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py` 第 23 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_build_long_conversation`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_manager.py` 第 29 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Build a conversation with many rounds and large observations.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_sys`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py` 第 16 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_human`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py` 第 20 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_ai`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py` 第 24 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_obs`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py` 第 28 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_make_tracker`，定义于 `packages/dbgpt-core/src/dbgpt/agent/core/context/tests/test_compact.py` 第 34 行。其所在子系统的定位是：上下文工程，含预算、压缩、存储与多轮管理。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_strip_routing_prefix`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 58 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责把长文本或代码按窗口与重叠策略切成可嵌入的 Chunk。作者注释或文档字符串指出：Strip the ``mcp__{prefix}__`` routing prefix added by :meth:`ConnectorManager._apply_tool_prefix`.  The prefix format is stable: ``name.split('__', 2)`` reliably yields ``['mcp', '{prefix}', '{origina该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_slugify`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/manager.py` 第 90 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Convert *text* to a lowercase ASCII slug.  Rules: lowercase, replace whitespace and underscores with ``-``, strip characters outside ``[a-z0-9-]``, collapse consecutive dashes.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `auto_authorize`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/task_executor.py` 第 28 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `revoke_authorization`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/task_executor.py` 第 32 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `is_auto_authorized`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/task_executor.py` 第 36 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `resolve_skill_connectors`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/skill_integration.py` 第 20 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Resolve MCPToolPack instances required by a skill.  Reads the ``required_tools`` field from *skill_config* (a list of connector type strings such as ``["yuque", "github"]``) and returns the correspond该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `check_skill_connector_availability`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/skill_integration.py` 第 71 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Check which required connectors are available or missing.  Args:     skill_config (Dict[str, Any]): Skill configuration dict that may         contain a ``required_tools`` key.     connector_manager (C该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_is_function_tool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 23 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_is_tool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 33 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_to_tool_list`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 37 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `json_parse_execute_args_func`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/pack.py` 第 69 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse the execute arguments.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `tool`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 232 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a tool from a function.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_docstring`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 287 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse the docstring of the function.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_args`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 295 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse the arguments of the function.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_args_from_schema`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/base.py` 第 353 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse the arguments from a Pydantic schema.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_patch_preload_resource`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/connector/tests/test_manager.py` 第 300 行。其所在子系统的定位是：连接器资源，负责凭证、目录、确认、回滚与任务执行。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Make MCPToolPack.preload_resource a no-op for tests.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `inspect_zip_for_modules`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 21 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Load the AutoGPTPluginTemplate from a zip file.  Loader zip plugin file. Native support Auto_gpt_plugin  Args: zip_path (str): Path to the zipfile. debug (bool, optional): Enable debug logging. Defaul该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `write_dict_to_json_file`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 46 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Write a dictionary to a JSON file.  Args:     data (dict): Dictionary to write.     file_path (str): Path to the file.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_directory_if_not_exists`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 57 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a directory if it does not exist.  Args:     directory_path (str): Path to the directory. Returns:     bool: True if the directory was created, else False.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `scan_plugin_file`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 78 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Scan a plugin file and load the plugins.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `scan_plugins`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 107 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Scan the plugins directory for plugins and loads them.  Args:     cfg (Config): Config instance including plugins config     debug (bool, optional): Enable debug logging. Defaults to False.  Returns:该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `update_from_git`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 136 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Update plugins from a git repository.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `__fetch_from_git`，定义于 `packages/dbgpt-core/src/dbgpt/agent/resource/tool/autogpt/plugins_util.py` 第 191 行。其所在子系统的定位是：工具资源抽象，封装函数调用、AutoGPT 插件与工具包。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_build_model_request`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm.py` 第 14 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Build model request from input value.  Args:     input_value(str or dict): input value  Returns:     ModelRequest: model request, pass to llm client该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `register_llm_strategy`，定义于 `packages/dbgpt-core/src/dbgpt/agent/util/llm/llm.py` 第 126 行。其所在子系统的定位是：Agent 侧 LLM 客户端与策略。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Register llm strategy.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_write_marker_script`，定义于 `packages/dbgpt-core/src/dbgpt/agent/skill/tests/test_manage.py` 第 12 行。其所在子系统的定位是：智能体技能系统，负责 Skill 加载、参数校验、中间件与执行编排。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 33 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 47 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_detail`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 66 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_export`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 91 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_app_by_code`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 116 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `hot_app_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 127 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 142 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `edit`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 155 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `all_agents`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 169 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `delete`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 181 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `collect`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 196 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `uncollect`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 210 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `team_mode_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 226 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `team_mode_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 235 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `llm_strategies`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 245 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `llm_strategy_values`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 256 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_resources`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 273 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get agent resources, such as db, knowledge, internet, plugin.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `publish`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 300 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `unpublish`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 315 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `init_native_apps`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 330 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `native_scenes`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 345 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `update_admins`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 350 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `query_admins`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 357 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_dbgpts`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/controller.py` 第 369 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/endpoints.py` 第 19 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_detail`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/endpoints.py` 第 36 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_update`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/endpoints.py` 第 44 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_create`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/endpoints.py` 第 52 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `app_delete`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/endpoints.py` 第 60 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `available_llms`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/gpts_server.py` 第 12 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_load_datasource`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/datasource.py` 第 28 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_knowledge_spaces_info`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/resource/knowledge.py` 第 129 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `content_str`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/team/base.py` 第 74 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Converts `content` into a string format.  This function processes content that may be a string, a list of mixed text and image URLs, or None, and converts it into a string. Text is directly appended t该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_build_conversation`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/controller.py` 第 60 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_format_vis_msg`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/controller.py` 第 840 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_agent_from_gpt_detail`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/app_agent_manage.py` 第 198 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get the agent object from the GPTsAppDetail object.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create_agent_of_gpts_app`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/app_agent_manage.py` 第 237 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_app_manager`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/app_agent_manage.py` 第 277 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_team_context`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1271 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：parse team_context to str该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_load_team_context`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1284 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：load team_context to str or AWELTeamContext该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `native_app_params`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1319 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `adapt_native_app_model`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1395 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Adapt native app chat scene.该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `parse_select_param`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/db/gpts_app.py` 第 1457 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `plugin_hub_update`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 48 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_agent_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 70 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `my_agents`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 92 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `agent_install`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 100 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `agent_uninstall`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 114 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `personal_agent_upload`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/hub/controller.py` 第 127 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_service`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 22 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Get the service instance该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_parse_api_keys`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 31 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Parse the string api keys to a list  Args:     api_keys (str): The string api keys  Returns:     List[str]: The list of api keys该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `check_api_key`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 45 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Check the api key  If the api key is not set, allow all.  Your can pass the token in you request header like this:  .. code-block:: python      import requests      client_api_key = "your_api_key"该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `health`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 86 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Health check endpoint该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 100 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Create a new Agent/chat entity  Args:     request (ServeRequest): The request     service (Service): The service Returns:     ServerResponse: The response该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `update`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 117 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Update a Agent/chat entity  Args:     request (ServeRequest): The request     service (Service): The service Returns:     ServerResponse: The response该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `query`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 136 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Query Agent/chat entities  Args:     request (ServeRequest): The request     service (Service): The service Returns:     ServerResponse: The response该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `query_page`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 155 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Query Agent/chat entities  Args:     request (ServeRequest): The request     page (int): The page number     page_size (int): The page size     service (Service): The service Returns:     ServerRespon该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `init_endpoints`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/api/endpoints.py` 第 174 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：Initialize the endpoints该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `setup_and_teardown`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_endpoints.py` 第 20 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `client_init_caller`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_endpoints.py` 第 27 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `setup_and_teardown`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_service.py` 第 17 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `service`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_service.py` 第 24 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `default_entity_dict`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_service.py` 第 31 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `setup_and_teardown`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_models.py` 第 16 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `server_config`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_models.py` 第 24 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `dao`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_models.py` 第 30 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `default_entity_dict`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/chat/tests/test_models.py` 第 35 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `create`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/controller.py` 第 19 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `query`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/controller.py` 第 32 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `update`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/controller.py` 第 55 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `delete`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/app/recommend_question/controller.py` 第 69 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `get_app_resource_list`，定义于 `packages/dbgpt-serve/src/dbgpt_serve/agent/agents/expand/app_resource_start_assisant_agent.py` 第 236 行。其所在子系统的定位是：多智能体应用服务。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

该改造函数名为 `_write_skill_md`，定义于 `tests/unit_tests/agent/test_claude_skill.py` 第 7 行。其所在子系统的定位是：仓库级测试。结合命名与模块归属，可以将其设计职责概括为：负责智能体生命周期、消息交换、工具调用与结果汇总。作者注释或文档字符串指出：源码未提供独立文档字符串，职责需结合文件路径、命名与调用关系理解。该函数应在优化中明确保留、替换或包裹。在整体架构中，它必须遵守依赖倒置：上层场景与服务只依赖 core 接口，具体数据库、向量库、模型提供商由 ext 包注入。生命周期上，若它是组件则参与 SystemApp 的 init/start/stop 钩子；若它是数据模型则通过 DAO 与 REST schema 双向转换；若它是算子则作为 AWEL DAG 节点被 Runner 调度；若它是 Agent 动作则在 ReAct 循环中被解析器触发。实现时需要考虑异步执行、错误重试、流式输出、多租户字段以及国际化文案。测试上应对正常路径、空输入、解析失败、超时与权限拒绝分别覆盖。

